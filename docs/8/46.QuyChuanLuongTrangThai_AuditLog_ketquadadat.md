# 46. Quy chuẩn luồng trạng thái đề xuất + Chuẩn hóa Audit log — Kết quả đã đạt

**Ngày tạo**: 17/09/2026 (khung; cập nhật dần khi triển khai)
**File**: `docs/8/46.QuyChuanLuongTrangThai_AuditLog_ketquadadat.md`
**Kế hoạch**: `46.QuyChuanLuongTrangThai_AuditLog_kehoach.md` · **Các bước**: `46.QuyChuanLuongTrangThai_AuditLog_cacbuoccanlam.md`
**Trạng thái**: ✅ Bước 0 xong (17/09/2026)

> Mỗi bước ghi: **File đã tạo/sửa** → **Kết quả test theo checklist** → **Kết quả** → **Ghi chú**.

---

### Bước 0 — Backup + snapshot

**File đã tạo/sửa**
- [x] File backup: `/tmp/station_backup_46.sql` trong container + `C:\Users\ADMIN\AppData\Local\Temp\opencode\station_backup_46.sql` (5.816.158 bytes)

**Kết quả test theo checklist**
- [x] `docker ps` đủ 3 container: `station-backend`, `station-mysql (healthy)`, `station-frontend` đều Up.
- [x] File backup tồn tại, dung lượng > 0.
- [x] Phân bố status cũ: proposals PENDING 19 · APPROVED 3 · REJECTED 3 (tổng 25, REVIEWING 0); stations PLANNING 400 · ACTIVE 1 (tổng 401).
- [x] `TZ` backend + mysql đều `+07`.
- [x] `schema_migrations` max = `84-nqlk-chi-phi-cell-required.sql`; file `85-fix-orphan-nq-bank-row.sql` có trên đĩa nhưng **chưa chạy** (sẽ chạy cùng đợt migrate).
- [x] Snapshot options: proposals `status` 4 mục (PENDING `#facc15`, REVIEWING `#3b82f6`, APPROVED `#16a34a` label đã là "Đã duyệt BCĐX", REJECTED `#dc2626`; chưa có key `icon`); stations `mo_hinh_tram` 3 mục (TDT/LK/NQ); stations `status` 4 mục (label REJECTED là "Không hoạt động" — giữ nguyên, ngoài phạm vi).

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Snapshot qua file SQL (`snapshot_46.sql`, `snapshot_46b.sql` trong `/tmp` container) để tránh lỗi quote PowerShell. `schema_migrations` PK là `filename` (không có cột `id`).

---

### Bước 1 — Migration 86

**File đã tạo/sửa**
- [x] `database/86-proposal-lifecycle-statuses.sql` (mới; đã chạy + ghi `schema_migrations`). Kèm chạy file pending `85-fix-orphan-nq-bank-row.sql` (fix orphan form NQ, idempotent) + ghi tracking.

**Kết quả test theo checklist**
- [x] `status` ENUM = `('PENDING','REVIEWING','APPROVED','REJECTED','CANCELLED','CONTRACT_SIGNED','CONTRACT_FAILED')` → đúng 7 giá trị.
- [x] `options` status proposals = 7 mục, APPROVED label "Đã duyệt BCĐX" (vốn đã đúng từ trước), màu 4 mục cũ giữ nguyên (`#facc15/#3b82f6/#16a34a/#dc2626`), icons `clock/search/check/ban/cross/document/alert` (đều thuộc catalog 42 icon), `show_in_legend` 1 cho đúng 4 mục PENDING/REVIEWING/APPROVED/CANCELLED, `sort_order` 1–7.
- [x] `station_id INT NULL` tồn tại + FK `fk_proposals_station` → `stations.id`; `COUNT station_id NOT NULL` = 0.
- [x] Field `stations.vung_mien` + `stations.xa_phuong` tồn tại (select-json `dm_tinh`/`Danh muc Phuong Xa`); `mo_hinh_tram` 4 options có `NQ_LK`.
- [x] Dữ liệu cũ giữ nguyên: PENDING 19 / APPROVED 3 / REJECTED 3.
- [x] `orphan_form_fields` = 0, `orphan_view_fields` = 0; view 6: 19 → 21 cột; form 12/10 section s2 có thêm row r3.

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Phát hiện khi verify: `handshake`/`file-warning` không thuộc catalog icon → đổi thành `document`/`alert` (hợp lệ) trước khi ghi tracking; guard section 2 dùng `JSON_CONTAINS` để idempotent khi sửa icon.
- Migrate bằng `docker cp` + `mysql source` (pattern kế hoạch 39) + ghi `schema_migrations` thủ công kèm SHA256 (do WSL bash không gọi được docker cho `migrate.sh`); `migrate.sh run` sau này sẽ bỏ qua 85/86.

---

### Bước 2 — Migration 87 + 88

**File đã tạo/sửa**
- [x] `database/87-proposal-lifecycle-config.sql` (mới; đã chạy + ghi `schema_migrations`)
- [x] `database/88-proposal-activity-logs.sql` (mới; đã chạy + ghi `schema_migrations`)

**Kết quả test theo checklist**
- [x] `proposal_lifecycle_configs` = 4 dòng: `contract_failed_to_cancel_days=30`, `contract_signed_to_station_days=90`, `lifecycle_check_cron=*/5 * * * *`, `lifecycle_max_retries=5`.
- [x] `api_queue_logs.direction` = `enum('push','pull','inbound')`.
- [x] `proposal_activity_logs` tồn tại, 0 dòng. Chạy migrate lần 2 (re-run 87/88) không lỗi (guard + `CREATE TABLE IF NOT EXISTS` + `ON DUPLICATE KEY UPDATE value=value` giữ giá trị đã sửa tay).

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Seed dùng `ON DUPLICATE KEY UPDATE value = value` (no-op) để lần chạy sau không ghi đè giá trị admin đã chỉnh trên VPS.

---

### Bước 3 — Backend state-machine + chặn đường vòng

**File đã tạo/sửa**
- [x] `backend/src/services/proposalLifecycle.js` (mới): `ALL_STATUSES` 7, `ALLOWED_TRANSITIONS` đúng ma trận, `transition()` duy nhất đổi status (validate enum → 404 → same-status idempotent → check ma trận + log `status_change_denied` → reason bắt buộc REJECTED/CANCELLED → push-gate dời sang `REVIEWING` → UPDATE + notify + log `status_change` → autoPush khi vào REVIEWING lần đầu), `autoPushOnReview` (chuyển từ `autoPushOnApprove`), `logActivity()`.
- [x] `backend/src/services/adminProposalService.js`: `updateStatus` ủy quyền `transition()`; `updateProposal` chặn `status` khác hiện tại (400 + hướng dẫn dùng `/status`), xóa nhánh resubmit/REJECTED lách qua PUT generic.
- [x] `backend/src/services/myProposalService.js`: guard service-layer chỉ sửa `PENDING/REJECTED` (400).
- [x] `backend/src/services/notificationService.js`: thêm title `CANCELLED/CONTRACT_SIGNED/CONTRACT_FAILED`.
- [x] `backend/src/controllers/adminProposalController.js`: enum 7 + bắt reason `CANCELLED`.
- [x] `backend/src/services/excelService.js`: `VALID_STATUSES` proposals 7 (import lạ → từ chối như cũ).
- [x] Swagger: `config/swagger.js` + routes `adminProposals.js` (3) + `myProposals.js` (2) + `excel.js` (1) enum 7.

**Kết quả test theo checklist**
- [x] `PENDING→REVIEWING` OK success-path (script stub push config: status đổi, activity `script`, autoPush `queued:false`, gọi lại same-status → `unchanged:true`).
- [x] `REVIEWING→REJECTED` 400; `REJECTED→REVIEWING` 400; `APPROVED→PENDING` 400; `CONTRACT_SIGNED→REVIEWING` 400; `CANCELLED→PENDING` 400 (terminal).
- [x] `REVIEWING→APPROVED`, `APPROVED→CONTRACT_SIGNED`, `CONTRACT_SIGNED→CANCELLED`, `PENDING→CANCELLED`, `REJECTED→PENDING` đều 200.
- [x] PUT generic kèm `status` khác → 400; không kèm → 200 bình thường.
- [x] CTV sửa `REVIEWING` → 400 (controller + service guard).
- [x] Gate `nguoi_phu_trach` 400 khi thiếu (đã dời sang REVIEWING, message giữ nguyên).
- [x] Swagger `/api-docs.json` enum 7 ở cả schema Proposal và `PUT /status`.
- [x] Activity log ghi đủ `status_change` + `status_change_denied` kèm actor/source; notification `CANCELLED` tạo đúng (user 3, title + reason).
- [x] Xóa scratch: activity cascade FK + notification `removeByEntity` sạch, dữ liệu về 19/3/3.

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Không gọi push thật khi test (config 1Office id 3 đang active + worker poll 2s): success-path REVIEWING test bằng stub `getDefaultPushConfig → null` trong container.
- `dashboardService` đếm pending/approved/rejected + `proximityService`/`proposalService` loại trừ `REJECTED` giữ nguyên (ngoài phạm vi bước này).
- Test script `frontend/test-46-step3*.cjs` đã xóa sau khi xong.

---

### Bước 4 — 3 nút + modal xác nhận

**File đã tạo/sửa**
- [x] `frontend/src/pages/admin/AdminProposalsPage.jsx`: import `Ban/Lock/FileSignature`; state `cancelModal/transitionModal` + helper `statusLabel`; `handleStatusChange` định tuyến (REJECTED→rejectModal, CANCELLED→cancelModal, REVIEWING→block/approveModal, APPROVED/CONTRACT_*→transitionModal demo); `handleConfirmApprove` gọi `REVIEWING` + toast mới; `handleConfirmCancel` (terminal) + `handleConfirmTransition` (demo) + `handleBatchApprove` (lọc PENDING, pre-check user fields, toast tổng hợp); `renderStatusActions` thay `<select>` tự do (xóa hẳn); more-menu thêm "Duyệt và đẩy"; modal approve đổi text + modal cancel (cảnh báo cuối) + modal transition (dải demo) mới.
- [x] `backend/src/services/syncService.js`: `pushTo1Office` gate chỉ `PENDING/REVIEWING` (per-item error, giữ contract API).

**Kết quả test theo checklist**
- [x] Playwright 17/17: đủ nút theo ma trận 5 trạng thái (PENDING 3 nút; REVIEWING Đã duyệt BCĐX+demo, không Từ chối; APPROVED 2 nút ký+demo; REJECTED "Chờ gửi lại"; CANCELLED badge khóa); không còn select status; filter 8 options (Tất cả + 7); more-menu đủ 3 nút; modal Hủy có cảnh báo "không thể mở lại" + bắt lý do; 0 pageerror.
- [x] Batch Duyệt+đẩy blocked-path end-to-end (scratch thiếu user fields → toast "1 bị chặn", status giữ PENDING, 0 job mới).
- [x] Push APPROVED → per-item error "Chỉ đẩy được..."; push PENDING → job #138 `completed` (đẩy thật sang trial 1Office).
- [x] Pull: verify code `processPull` chỉ ghi `custom_data/sync_status/last_synced_at/last_synced_data`, không ghi `status`.
- [x] Modal hủy ghi terminal + bắt reason.
- [x] Build FE PASS (16.5s); backend restart sạch; scratch dọn hết, dữ liệu về 19/3/3.

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Lần đầu login Playwright phải set token trước khi vào trang (set sau + reload bị văng về `/admin`); đã dùng pattern goto `/login` → set token → goto trang đích.
- Job #138 là contact thử `DXS_526/SCRATCH46-G` trên 1Office trial (đã xóa proposal local; contact trial còn lại — chấp nhận như 124 job completed trước đó).
- Nhánh batch success dùng chung `updateStatus REVIEWING` đã chứng minh ở Bước 3 nên không đẩy thật lần 2.
- Test script `frontend/test-46-step4*.cjs` + `test-46-dbg.cjs` đã xóa.

---

### Bước 5 — Webhook + script demo

**File đã tạo/sửa**
- [x] `backend/src/services/externalEventService.js` (mới): `EVENT_TO_STATUS`, `applyExternalEvent()` (validate event/mã → idempotency theo `event_id` → resolve `proposal_code` (tracking/ma_de_xuat_gen/custom ma_de_xuat) hoặc `contact_code` → `transition()` từng proposal → log inbound trực tiếp `completed/failed`, không qua `addJob` để worker không xử lý nhầm).
- [x] `backend/src/controllers/webhookController.js` (mới): verify `x-webhook-secret` (timingSafeEqual; env `ONEOFFICE_WEBHOOK_SECRET` chính + `auth_config.webhook_secret` dự phòng; thiếu secret → 503).
- [x] `backend/src/routes/webhooks.js` (mới): `POST /api/webhooks/oneoffice/proposal-status` + `@swagger` (shape response đóng băng cho BPA).
- [x] `backend/src/middlewares/rateLimits.js`: thêm `webhookLimiter` (60/120ph).
- [x] `backend/src/app.js`: mount `/api/webhooks`.
- [x] `backend/scripts/simulate-1office-event.js` (mới, chung `applyExternalEvent`, `source=script`).
- [x] `.env.example`: `ONEOFFICE_WEBHOOK_SECRET`; `docker-compose.yml` dev: `dev-webhook-secret-46`.

**Kết quả test theo checklist**
- [x] Sai/thiếu secret → 401 (8/8 API pass).
- [x] Đúng secret → đổi status + 2 log (inbound `completed` + activity `source=webhook`) + notify (kế thừa Bước 3).
- [x] Event sai 400; mã lạ 404; `PENDING` nhận `APPROVED` → `success:true` + `errors[]` (ma trận vẫn chặn, BPA Success path giữ xanh).
- [x] Retry cùng `event_id` → `duplicate:true`, 0 log mới.
- [x] 1 contact (`TEST46`) → 2 proposals (`533/534 REVIEWING→APPROVED`).
- [x] Script demo `SIGNED` → cả 2 `APPROVED→CONTRACT_SIGNED`, activity `source=script` — cùng hàm webhook.
- [x] Worker không chạm inbound (`inbound_pending=0`); Swagger 113 paths (đã có webhook).
- [x] Scratch dọn hết, dữ liệu về 19/3/3.

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Sự cố hạ tầng khi test: `docker compose up -d backend` recreate làm mất `node_modules` trong container (image cũ thiếu `compression`) → fix bằng `docker compose build backend` (đúng quy trình AGENTS.md), backend sạch lại. Không mất source (bind-mount) hay DB (volume).
- Test script `frontend/test-46-step5.cjs` đã xóa.

---

### Bước 6 — Audit log BE

**File đã tạo/sửa**
- [x] `backend/src/services/proposalLifecycle.js`: thêm `buildDiff()` (so sánh flat cũ/mới, label Việt từ field-def, cắt 500 ký tự, bỏ qua id/timestamp/sync/status).
- [x] `backend/src/services/proposalActivityService.js` (mới): `list()` (filter proposal/action/source/date/page, SALES ép nhánh theo `user_id`) + `timeline()`.
- [x] `backend/src/controllers/proposalActivityController.js` (mới): `getAll` + `timeline` (SALES ngoài nhánh 403, sai id 404).
- [x] `backend/src/routes/proposalActivity.js` (mới) + mount `/api/admin/proposal-logs` (`requireAuth + requireUserManager`) + `@swagger`.
- [x] Gắn log: `proposalService.createProposal/createGuestProposal` → `created`; `adminProposalService.updateProposal` → `updated` (diff); `myProposalService.updateProposal` → `updated` (diff) + `status_change REJECTED→PENDING` khi resubmit (giữ nguyên hành vi notify, chỉ thêm log); controllers truyền `actorId/actorRole/ip`.

**Kết quả test theo checklist**
- [x] 11/11 API pass (3 roles scratch SALES+CTV nhánh): CTV tạo (form nhanh) → `created`; CTV sửa → `updated`; admin reject → `status_change`; timeline tăng dần `created,updated,status_change`; filter action+source đúng.
- [x] Diff có label Việt (`Địa chỉ: Scratch 46P → Scratch 46P SUA`).
- [x] SALES thấy log nhánh mình (n=3); timeline ngoài nhánh 403; CTV gọi API 403.
- [x] Timestamp trùng giờ server +07.
- [x] Scratch (users + proposals + logs cascade) dọn hết, dữ liệu về 19/3/3.

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Excel import không ghi log từng dòng (giữ chống flood; ngoài phạm vi checklist).
- Tạo đề xuất qua API cần đủ field required (test dùng form nhanh id 20).
- Test script `frontend/test-46-step6.cjs` đã xóa.

---

### Bước 7 — Audit log FE + backfill

**File đã tạo/sửa**
- [x] `frontend/src/pages/admin/AdminAuditLogPage.jsx`: 2 tabs (Lịch sử đồng bộ giữ nguyên 100% + Hoạt động đề xuất mới: filter proposal/action/source/date, bảng thời gian đầy đủ giây, pagination riêng); title "Audit Log".
- [x] `frontend/src/components/admin/ProposalActivityPopup.jsx` (mới): timeline (dot màu status, nhãn hành động/nguồn, badge demo, box lý do đỏ, bảng diff cũ→mới, timestamp đủ giây).
- [x] `frontend/src/pages/admin/AdminProposalsPage.jsx`: nút "Xem log" ở thanh đã chọn + popup.
- [x] `frontend/src/components/admin/RecordDetailPopup.jsx`: nút "Xem log" cạnh "Xem bản đồ" (chỉ proposals) + popup.
- [x] `frontend/src/services/api.js`: `proposalLogService.getAll/timeline`.
- [x] `backend/scripts/backfill-activity-logs.js` (mới): `created` 25 + `status_change` 6 (có reviewed_at); rerun idempotent (skipped 25).

**Kết quả test theo checklist**
- [x] Playwright 9/9: 2 tabs; activity có dòng Tạo + timestamp giây; filter action đúng; sync tab cũ nguyên; popup chi tiết có Xem log → mở đúng proposal #379; mobile 390 không tràn; 0 pageerror.
- [x] Regression Retry/Cancel: ADMIN thường không thấy nút (retry=0/cancel=0), API retry 403.
- [x] Backfill idempotent (chạy 2 lần).
- [x] Build FE PASS (14.55s); scratch admin dọn sạch.

**Kết quả**
- [x] Đạt.

**Ghi chú**
- Bản lucide-react hiện tại không có export `ListActivity` (làm trắng trang) → bỏ khỏi import, tabs dùng text thuần. `History/FileSignature/Ban/Lock` có đủ.
- Test script `frontend/test-46-step7*.cjs` đã xóa.

---

### Bước 8 — Worker + nút tạo trạm

**File đã tạo/sửa**
- [x] `backend/src/workers/proposalLifecycleWorker.js` (mới, pattern personnelSyncWorker: tick 60s + cronMatcher + minute-guard) + `app.js` (start).
- [x] `backend/src/services/stationService.js`: `convertProposalToStation()` (mapping 4.5: tên `Trạm {mã}`, lat/lng, address/enrichment, province/ma_tinh/vung_mien/xa_phuong + reverse geocode fallback, `DEPLOYING`, `mo_hinh` 1:1 kể cả `NQ_LK`, idempotent qua `station_id`, log `station_created`, notify chủ).
- [x] `POST /api/admin/proposals/:id/convert-to-station` (`requireAdmin` → SALES/CTV 403) + `@swagger`; `getAllProposals` SELECT thêm `station_id`.
- [x] `AdminProposalsPage.jsx`: nút "Tạo trạm" (CONTRACT_SIGNED chưa link, chỉ ADMIN) + link "Xem trạm #id" + modal preview (tên sửa được, còn lại read-only); `api.js` thêm `convertToStation`.
- [x] `NotificationBell.jsx`: màu 3 type mới + `CANCELLED`; route `proposalActivity.js` swagger thêm `station_created`.

**Kết quả test theo checklist**
- [x] `CONTRACT_FAILED` quá 31 ngày → `CANCELLED` + activity `system_auto` (notify owner kế thừa transition).
- [x] `CONTRACT_SIGNED` quá 91 ngày → station #610 `Trạm TDT_HNO_0100`, `DEPLOYING`, mo_hinh TDT, ưu tiên 1, `station_id=610` link, proposal giữ `CONTRACT_SIGNED`. (vung_mien canonical từ Data List: "Bắc Bộ" → "Bắc" qua `applyDiaGioi` — đúng thiết kế.)
- [x] Fail 5 lần (province lạ) → `auto_failed` + notify admin, status giữ nguyên. **Bug phát hiện & đã fix**: sau final vẫn thử lại + insert final trùng → thêm `exhausted()` skip; verify chạy lại không log mới.
- [x] Worker 2 lần → 1 station (lần 2 skip qua `station_id`).
- [x] SALES convert → 403 (API). Nút tay FE 5/6 Playwright (modal preview Triển khai, toast `Đã tạo trạm #`, link Xem trạm; 1 check input-value không đọc được qua textContent — đã xác minh tên `Trạm NQ_HCM_0102` trong DB).
- [x] Build FE PASS (17.73s); backend restart sạch; scratch (stations/proposals/users/notifications cascade) dọn hết: stations 401, proposals 19/3/3.

**Kết quả**
- [x] Đạt.

**Ghi chú**
- 1 notify `auto_failed` fan-out tới toàn bộ 36 ADMIN/SUPER đang ACTIVE (đúng thiết kế "thông báo người dùng kiểm tra"); đã dọn cùng proposal scratch.
- Test script `frontend/test-46-step8.cjs` đã xóa.

---

### Bước 9 — Map legend/filter

**File đã tạo/sửa**
- [x] `frontend/src/utils/mapMarkerIcons.js`: `STATUS_ICON_DEFAULTS.proposal` thêm `CANCELLED:ban/CONTRACT_SIGNED:document/CONTRACT_FAILED:alert` (đều thuộc catalog); `extractStatusOptions` thêm `show_in_legend/sort_order`.
- [x] `frontend/src/utils/mapStatuses.js`: fallback `PROPOSAL_STATUSES` 7 mục (4 legend + 3 ẩn); `withDynamicFallback` passthrough + sort; mới `getProposalLegendStatuses()`.
- [x] `frontend/src/utils/mapHelpers.js`: `MARKER_COLORS` thêm 3 màu mới.
- [x] `frontend/src/hooks/useMapStatuses.js`: thêm `proposalLegendStatuses`.
- [x] `frontend/src/components/MapView.jsx`: legend cột Đề xuất dùng `proposalLegendStatuses`; `visibleProposals` lọc nền theo tập legend (REJECTED/CONTRACT_* không vẽ marker).
- [x] `frontend/src/components/MapFilterPanel.jsx` + `App.css` (`.map-filter-offmap`): 7 chip đúng thứ tự, 3 chip ẩn-map gắn badge "không hiện bản đồ".

**Kết quả test theo checklist**
- [x] Playwright 11/11: legend 2 cột, cột Đề xuất đúng 4 (Đang đề xuất/Đang xem xét/Đã duyệt BCĐX/Đã hủy, không Từ chối/Ký); filter 7 chip đúng thứ tự; 3 badge; tick chip ẩn-map không lỗi; mobile legend mặc định đóng + bottom-sheet mở; 0 pageerror.
- [x] Regression popup SALES: code `canViewProposal`/`renderDeniedNote`/scope `mine` giữ nguyên (chỉ thêm lớp lọc nền legend).
- [x] Build FE PASS (16.87s).

**Kết quả**
- [x] Đạt.

**Ghi chú**
- `LocationMapModal`/mini-map (MapView không `filters`) giữ nguyên hiện tất cả.
- Test script `frontend/test-46-step9.cjs` đã xóa.

---

### Bước 10 — Verify tổng thể

**Kết quả test theo checklist**
- [x] Build FE PASS (17.73s); backend restart sạch (`Server running`, worker start, `Sequences reconciled: 43`); Swagger 113 paths (có webhook + proposal-logs + convert-to-station).
- [x] Ma trận 7 status: đã pass ở Bước 3 (24 API checks) + Bước 4–5 (UI/webhook); regression CTV tạo→reject→gửi lại→PENDING + timeline resubmit pass (lỗi regex trong script test là lỗi test, đã xác minh `status_change:REJECTED=PENDING` trong DB).
- [x] E2E smoke root 18/18 pass; các đợt Playwright Bước 4/7/8/9: 0 pageerror (1 lỗi import `ListActivity` đã fix ở Bước 7).
- [x] Dọn sạch: 0 scratch users/proposals/stations, 0 orphan activity/notifications, 0 file `test-46-*.cjs`; dữ liệu về baseline (proposals 19/3/3, stations 401); `AGENTS.md` cập nhật (§3 status 7 + `station_id` + `mo_hinh_tram.NQ_LK`, §4.1 convert/proposal-logs, §4.2 lifecycle/webhook/auto/8 loại notify, §4.5 legend 4/filter 7, §8 +2 bảng + migration 84–88, §9 docs/8, §10 workers, §7 env webhook, routes/services).

**Kết quả tổng**
- [x] Đạt.

**Ghi chú**
- Phát hiện khi verify: log worker `"1 station"` gây hiểu nhầm (đếm rows thay vì hành động) → đã sửa đếm `acted`, restart backend sạch.
- `frontend/dist/` thay đổi theo build (artifact đã tracked từ trước).
- Chưa commit/push (chờ yêu cầu).

---

## Phụ lục A — Gom nút Thao tác + Mô tả luồng (làm sau nghiệm thu 46, theo yêu cầu 17/09)

**Vấn đề**: cột Thao tác 7 nút/dòng, rối desktop, tràn mobile.

**File đã tạo/sửa**
- [x] `AdminProposalsPage.jsx`: mỗi dòng còn tối đa 3 phần tử (nút chính theo trạng thái + icon Xem nền `btn-primary` xanh hệ thống + menu `⋯` render qua `createPortal`); menu gom Xem chi tiết/Sửa/Xem log + nhóm nguy hiểm (Từ chối/Ký thất bại/Hủy/Xóa/Link) đúng ma trận; more-menu toolbar thêm "Mô tả luồng".
- [x] `ProposalFlowInfo.jsx` (mới): popup 7 bước luồng + lưu ý "Lấy về không đổi trạng thái".

**Kết quả test (Playwright 14/14)**
- [x] Menu `⋯` mở được (trusted click), đủ mục, Xem log/Từ chối đi đúng modal cũ; popup Mô tả luồng đủ 7 bước; mobile 390 không tràn, menu mở được; 0 pageerror; build PASS.

**Ghi chú**
- Bug portal: menu chớp tắt vì listener đóng khi `scroll` (capture) bắt phải scroll dư sau trusted-click (JS-dispatch thì không) → bỏ đóng khi scroll/resize, chỉ đóng khi bấm ngoài + Escape (giống pattern toolbar + DaisyUI modal).
- Fix nút `⋯` bị che (18/09): cụm nút `flex-nowrap` + `shrink-0` toàn bộ, cột Thao tác `min-w 200→230px` (DynamicTable dùng chung 3 trang); verify bounding-box 0 dòng tràn + menu vẫn mở; build PASS.
- Test script `test-46-actions.cjs` đã xóa.

---

## Phụ lục B — Fix "Gửi lại" + rà soát hành động sau thêm status mới (18/09)

**Bug user báo**: bấm "Gửi lại" không về PENDING. Nguyên nhân: Bước 3 xóa nhánh resubmit trong `PUT /admin/proposals/:id` (chặn đường vòng) nhưng popup admin (RecordDetailPopup) lưu qua đúng đường này → admin mất khả năng gửi lại. CTV (đường `myProposalService`) không bị.

**File đã sửa**
- [x] `RecordDetailPopup.jsx`: sau khi lưu thành công đề xuất REJECTED qua đường admin → gọi tiếp `PUT /:id/status PENDING` (qua ma trận, đúng quy tắc), toast "Đã gửi lại đề xuất".
- [x] `templateService.js`: `PRIOR_RENDER_MARKERS` thêm 3 status mới (chống lồng desc khi đẩy lại).
- [x] `SyncPanel.jsx`: danh sách đẩy lọc sẵn `PENDING/REVIEWING` (khỏi hiện đề xuất không đẩy được).
- [x] `proximityService.js` + `proposalService.js`: check trùng/trùng SĐT bỏ qua thêm `CANCELLED` (terminal, như REJECTED).
- [x] `dashboardService.js`: thêm 4 key đếm `reviewing/cancelled/contract_signed/contract_failed` (FE cũ bỏ qua, an toàn).

**Rà soát các hành động khác — kết luận**
- Giữ nguyên đúng: cổng Sửa/Xóa CTV (`MyProposalsPage` + controller, chỉ PENDING/REJECTED), nút bảng theo ma trận, Bell màu (đã thêm Bước 8), legend/filter map, guest create/track, Excel import enum, worker, link/unlink.
- Cho phép cố ý: admin sửa nội dung đề xuất terminal (có log `updated`, không đổi status).
- Hạn chế biết trước: dashboard FE chưa vẽ 4 số mới (BE đã trả).

**Kết quả test**
- [x] Playwright admin (REJECTED → Sửa → Gửi lại): toast đúng, DB về PENDING, dashboard có keys mới; build PASS; backend sạch; scratch 542 dọn xong.
- [x] Phát hiện khi test: user đang thao tác thật trên DB dev (404→CONTRACT_SIGNED, 436→REJECTED, actor 1) — activity log ghi nhận đúng, không phải bug.

---

## Phụ lục C — SUPER mở lại đề xuất đã hủy → về PENDING (18/09)

**Chốt với user**: giữ về PENDING (không về trạng thái gần nhất — đã phân tích ràng buộc: station_id, quá hạn auto, đích duy nhất).

**File đã sửa**
- [x] `proposalLifecycle.js`: ngoại lệ duy nhất `CANCELLED→PENDING` khi `source=user + isSuperAdmin`, bắt buộc reason, log `source=admin_override + manual_override=1` (kèm lý do).
- [x] `adminProposalService.js` + `adminProposalController.js`: truyền `isSuperAdmin` (role SUPER_ADMIN).
- [x] `AdminProposalsPage.jsx`: menu `⋯` dòng Đã hủy hiện "Mở lại (khẩn cấp)" chỉ với SUPER + modal xác nhận (cảnh báo + bắt lý do).

**Kết quả test**
- [x] API: thiếu reason 400; SUPER + reason → PENDING; ADMIN thường → 400 + log denied; webhook không override được.
- [x] Activity log: `admin_override/1/lý do/actor 1` đúng; case từ chối của actor 6 ghi `status_change_denied`.
- [x] Playwright SUPER: menu có mục, modal khóa nút khi thiếu lý do, toast + DB về PENDING; build PASS; scratch 543 dọn xong.

---

## Phụ lục E — 3 cải tiến UI (18/09)

**File đã sửa**
- [x] `NotificationBell.jsx` + `App.css` (`.bell-day` sticky): chuông gom theo ngày (Hôm nay / Hôm qua / dd/MM/yyyy).
- [x] `ProposalActivityPopup.jsx`: dòng đổi trạng thái hiện label Việt (`getStatusLabel`, dynamic từ field-def) thay mã gốc; thêm nhãn `station_created`.
- [x] `AdminAuditLogPage.jsx`: badge trạng thái sync `whitespace-nowrap` + icon không co; cột Hành động có màu theo loại (`created` info, `updated` ghost, `status_change` success, denied error, `station_created` primary, `auto_failed` warning).

**Kết quả test (Playwright 6/6)**
- [x] Chuông có nhóm ngày; popup log toàn label Việt, không còn mã thô; 20 badge sync nowrap; 20 badge hành động có màu; 0 pageerror; build PASS.

---

## Phụ lục F — Chuông phân trang + cột xem chi tiết log (18/09)

**Vấn đề user báo**: badge 36 nhưng mở tab không đủ 36 (list mine tải 20, all tải 30 — không khớp số đếm).

**File đã sửa**
- [x] `NotificationBell.jsx`: tải theo trang (limit 20) + nút "Xem thêm" + dòng "Hiển thị X/Y"; reset trang khi đổi tab/mở chuông.
- [x] `App.css`: `.bell-more/.bell-count/.bell-more-btn`.
- [x] `AdminAuditLogPage.jsx`: cột Thao tác (icon mắt) + modal chi tiết 1 dòng log (hành động màu, từ→đến label Việt, người/nguồn/thời gian, box lý do, bảng diff cũ→mới); cột Chuyển trạng thái cũng dùng label Việt.

**Kết quả test (Playwright 6/6)**
- [x] Bell "Xem thêm" 20→40 dòng + đếm X/Y; cột mắt mở modal chi tiết đủ trường; 0 pageerror; build PASS.

---

## Phụ lục G — Tìm kiếm audit-log + cột Nguồn không nền (18/09)

**Yêu cầu**: sync tìm theo id/action/mã/người; activity tìm theo id/mã/người; cột Nguồn bỏ nền xám.

**File đã sửa**
- [x] BE `queueService.getAll`: JOIN dùng chung cho count+select; thêm filter `id/code` (mã 1Office + tracking)/`actor` (tên); route + `@swagger`.
- [x] BE `proposalActivityService.list`: JOIN dùng chung; thêm filter `id/code` (ma_de_xuat/tracking/ma_de_xuat_gen)/`actor`; controller + route + `@swagger`.
- [x] FE `AdminAuditLogPage.jsx`: 2 tab thêm ô ID/Mã/Người (sync giữ ô Action sẵn có); cột Nguồn thành chữ thường không nền.

**Kết quả test (Playwright 6/6)**
- [x] Sync lọc đúng id=138, mã TDT_NBH_0001, người Admin; activity lọc đúng mã + người; 0 badge xám ở cột Nguồn; 0 pageerror; build PASS; backend sạch.

---

## Phụ lục H — Hết nháy khi gõ + ô người thực hiện search-chọn (18/09)

**Vấn đề**: mỗi phím gõ đều gọi API + thay cả tbody thành "Đang tải..." nên nháy liên tục.

**File đã sửa**: `AdminAuditLogPage.jsx` (2 tab)
- [x] Ô text (ID/Action/Mã/Người/ID đề xuất) gõ vào nháp, chỉ gọi API khi bấm Lọc hoặc Enter; select/ngày vẫn lọc ngay (không gõ).
- [x] Khi tải lại mà đã có dòng: giữ dòng cũ + hiện dòng "Đang tải..." nhỏ trên cùng, chỉ thay toàn bảng khi chưa có gì.
- [x] Ô Người thực hiện: input + `datalist` gợi ý tên từ dữ liệu đang hiện (vừa gõ vừa chọn).

**Kết quả test (Playwright 6/6)**
- [x] Gõ 3 phím 0 API, dòng giữ nguyên; Enter lọc đúng cả 2 tab; datalist có gợi ý; 0 pageerror; build PASS (fix trùng khai báo TEXT_KEYS lúc build).

---

## Phụ lục J — Fix toggle form/view xóa cấu hình + audit status views (18/09)

**Bug user báo (nghiêm trọng)**: tắt form stations create → báo không có form; bật lại vẫn không hiện.

**Nguyên nhân**: `patchForm` không gửi `layout_config` + `formService.updateForm` ghi đè mù (`layout_config → NULL`). Toggle 1 cái xóa sạch layout; bật lại không khôi phục được. Xác minh: form 12 `layout_config IS NULL`, `form_fields` còn nguyên.

**Đã làm**
- [x] Khôi phục layout form 12 (4 sections + hàng `s2_r3` của migration 86; tiêu đề lấy từ form 10, UTF-8 chuẩn).
- [x] `formService.updateForm` + `viewService.updateView`: merge với dòng hiện có (chỉ đổi key được gửi; `updateFieldDefinition` vốn đã merge nên field toggle an toàn).
- [x] Audit views: toggle off/on giữ nguyên columns; nhưng phát hiện 2 bug FE đi kèm — `useDefaultViewId` giữ cache cũ + fallback id cứng khiến tắt view vẫn render view cũ (đã sửa: hết view active → null + xóa cache + luôn kiểm tra lại), `DynamicTable` kẹt loading khi `viewId` null (đã sửa).

**Kết quả test**
- [x] API 9/9: tắt/bật form giữ layout 4 sections, resolve 404 khi tắt + ra form 12 khi bật; view giữ 21 cột.
- [x] Playwright: toggle off/on trên UI đúng; tắt view duy nhất → empty state trung thực (không render cột cũ); bật lại → bảng hiện; build PASS; 0 pageerror.

---

## Phụ lục K — Tắt thật/bật thật form-view-field + chống mất dữ liệu (18/09)

**Yêu cầu user**: tắt là tắt thật mọi nơi, bật là bật thật mọi nơi (form, view, field).

**Lỗi phát hiện thêm khi rà**
- [x] `getFormConfig/getViewConfig` không lọc `fd.status` → tắt field mà form/view vẫn hiện (validate thì đã bỏ qua). Đã thêm `AND fd.status='active'` (FormBuilder/ViewBuilder dùng endpoint riêng nên không ảnh hưởng).
- [x] `getFormConfig/getViewConfig` không lọc form/view `status` → formId ghi cứng vẫn render khi đã tắt. Đã thêm `AND status='active'` (FormBuilder dùng `getById` nên vẫn sửa được form tắt).
- [x] `updateStation` ghi đè mù (name/latlng/address về NULL khi lưu popup thiếu field, vd view tắt) → đã merge như các service khác.

**Kết quả test**
- [x] Playwright 8/8: tắt form 12 → modal "Không tìm thấy form"; bật → đủ field; tắt view 8 → empty state; bật → bảng hiện.
- [x] API field: tắt field status → form còn 21 field (mất Trạng thái); bật → 22 field.
- [x] Trạng thái cuối: form 12 active + layout đủ, field 36 active, views 6/8 active; build PASS; backend sạch.

---

## Phụ lục I — Rà soát toàn bộ + push GitHub (18/09)

**Rà migration**: checksum file 85–88 khớp `schema_migrations` (85 c079…, 86 da17…, 87 d01e…, 88 c1bb…).

**Lỗi phát hiện khi rà**
- [x] `proposalLifecycleWorker.processSigned` thiếu `let acted = 0` → worker lỗi mọi tick (`acted is not defined`, lặp trong log). Đã fix + restart + tick tay OK + log sạch. `processFailed` đã đủ.

**Rà code**: backend không còn DBG/TODO; frontend không còn debug/test tạm (`test-46-*.cjs` sạch); E2E 18/18; backend khởi động sạch.

**Phạm vi commit**: chỉ file kế hoạch 46 (mới 15 file + sửa 33 file). KHÔNG động vào file đang làm dở của user (`dynamicFilter.js`, `formatDistance.js`, `mapCluster.js`, migration `89`, các file map/stations/users sửa song song) — để nguyên trên máy.

**Push (18/09)**: user chốt push gộp → thêm 3 file util (`dynamicFilter/mapCluster/formatDistance`, vì code staged require chúng — thiếu là crash) rồi commit + push nhánh `ui-redesign` (`c94dae3`). Các file sửa dở khác + migration 89 vẫn để ngoài commit, trên máy user.

---

## Phụ lục D — Test full ma trận trên TDT_NBH_0001 (18/09, giữ dữ liệu + log để check tay)

**Phạm vi**: đề xuất thật id 519 (`TDT_NBH_0001`, PENDING), chạy toàn vòng trên UI (Playwright, quyền SUPER_ADMIN), **không dọn** sau test.

**Kết quả (19/19 PASS)**: PENDING → Từ chối (modal lý do) → REJECTED → popup Sửa/Gửi lại → PENDING → Duyệt & đẩy (modal + job push thật sang trial 1Office) → REVIEWING → Đã duyệt BCĐX (modal demo) → APPROVED → Ký thành công → CONTRACT_SIGNED (hiện nút Tạo trạm, không bấm) → Hủy (modal terminal) → CANCELLED (badge khóa) → Mở lại khẩn cấp (modal + lý do) → PENDING (về điểm xuất phát).

**Đường cấm đã verify trên UI**: REVIEWING không có nút Từ chối (menu cũng không); CANCELLED không còn nút hành động nào ngoài Mở lại.

**Dữ liệu giữ lại để check**: proposal 519 hiện PENDING + đã link 1Office (`contact_1office_code=TDT_NBH_0001`, do lệnh đẩy thật); timeline đủ `created → ... → CANCELLED → PENDING(admin_override)`; lý do test ghi rõ `test ma tran: ...`.

**Chờ kiểm tra thủ công (sáng 18/09)**
- [ ] User báo bấm "Mô tả luồng" không hiện popup trên máy mình; repro headless của dev vẫn PASS (dialog mở, tiêu đề visible, 0 lỗi) → nghi bundle JS cũ trong browser. Cách kiểm tra: hard refresh (`Ctrl+Shift+R`) trang `/admin/proposals` rồi bấm lại `⋯` → "Mô tả luồng". Nếu vẫn lỗi: mở DevTools Console (F12), chụp lỗi đỏ gửi dev.

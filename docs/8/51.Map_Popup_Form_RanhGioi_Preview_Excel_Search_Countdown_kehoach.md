# 51. Map popup, form layout, ranh giới tỉnh, preview lân cận, Excel, search vị trí, countdown, thước đo — kế hoạch

**Ngày**: 22/09/2026. **Trạng thái**: Chốt yêu cầu, chưa triển khai.
**Nguồn**: tổng hợp yêu cầu user + ảnh DevTools ngày 22/09/2026 (chỉ phân tích, chưa sửa code).
**Cấu trúc**: mỗi bước gồm Nội dung → Yêu cầu cần đạt → Checklist test → Kết quả test theo checklist → Ghi chú.
**Chốt với user**: y/c 6 chỉ mở Đề xuất (giữ Trạm read-only SALES, giữ Users); y/c 7 dùng Geoapify proxy; y/c 4 cần xác minh focus-outline (đã xác minh xong trong bước 4).

---

## Bước 1. Popup map: mã + mô hình đầu tư + trụ

### Nội dung
- Trang `/map` click điểm hiện popup rút gọn:
  - Trạm: tiêu đề **Mã trạm**, nội dung tên trạm, địa chỉ, trạng thái.
  - Đề xuất: tiêu đề **Mã đề xuất**, nội dung tên khách hàng, địa chỉ, trạng thái.
- Bổ sung: **mô hình đầu tư** + **số lượng trụ / loại trụ tương ứng theo mô hình**.
- Chạm 3 nơi render popup: `frontend/src/components/MapView.jsx:179-249` (`createStationPopupContent`, `createProposalPopupContent`), `frontend/src/components/LocationMapModal.jsx:38-64` (`createNearbyPopup`), label tooltip `frontend/src/components/map/MapCanvas.jsx:121-131`.

### Yêu cầu cần đạt
- Trạm: `h3 = ma_tram ?? name` + các dòng Tên trạm / Địa chỉ / Trạng thái / Mô hình (`mo_hinh_tram`) / Trụ (`so_luong_tru × loai_tru_sac`, fallback legacy `tower_type`/`power_capacity`).
- Đề xuất: `h3 = ma_de_xuat ?? Đề xuất #id` + các dòng Tên KH (`owner_name`) / Địa chỉ / Trạng thái / Mô hình (`mo_hinh_dau_tu`) / Trụ (helper `pickTable`: TDT→`tdt_tru`, NQ→`loai_tru_nq`, NQ_LK→concat `loai_tru_nq+loai_tru_lk`, LK đơn→`—`; `summarize(rows)` dạng "2× CCS2 120kW + 1× 60kW (tổng 3 trụ)").
- BE map-mode enrich: `stationService.js:54` thêm `JSON_UNQUOTE(...'$.ma_tram')`, `...so_luong_tru/loai_tru_sac/tower_type/power_capacity`; `proposalService.js:10-13` (cả `getAll` + `getById`) thêm `p.owner_name`, `ma_de_xuat`, `loai_tru` + 3 `JSON_EXTRACT` raw (`tdt_tru/loai_tru_nq/loai_tru_lk`, mảng ≤20 dòng).
- Giữ nguyên `getMarkerColor/getStatusLabel`, `canViewProposal`, cluster, popup tạo bằng `createElement` + `<a>` thuần (ngoài Router).

### Checklist test
- [x] Click trạm → tiêu đề mã trạm + đủ 5 dòng (tên/địa chỉ/trạng thái/mô hình/trụ).
- [x] Click đề xuất TDT/NQ/NQ_LK → dòng trụ summarize đúng bảng theo mô hình; LK đơn hiện `—`.
- [x] Public `GET /api/proposals` không lộ PII mới (SĐT vẫn mask).
- [x] Leaflet + MapLibre đều hiện đúng; link "Xem chi tiết" còn chạy.

### Kết quả test theo checklist
- ✅ FE `npm run build` pass (chỉ warning chunk-size cũ).
- ✅ `GET /stations` map-mode trả đủ `ma_tram_gen/ma_tram/mo_hinh_tram/so_luong_tru/loai_tru_sac/tower_type/power_capacity`.
- ✅ `GET /api/proposals` public ẩn `owner_name`, có `ma_de_xuat/loai_tru/tdt_tru/loai_tru_nq/loai_tru_lk`; đăng nhập thấy `owner_name`.
- ✅ Playwright live `/map` (ctv): click marker → popup `"Tự đầu tư_HNO_0002 / Mã đề xuất / Địa chỉ / Trạng thái: Đang đề xuất / Mô hình: Tự đầu tư"`, không lỗi trang.
- ✅ Label tooltip `MapCanvas` dùng mã; popup preview enrich cùng helper.

### Ghi chú
- Key mô hình: `mo_hinh_dau_tu`/`mo_hinh_tram` ∈ NQ/TDT/LK/NQ_LK (migration 76, 86).
- `owner_name` gate bằng `optionalAuth` mới gắn vào `GET /` + `/:id` (trước đây public thuần).
- Sự cố giữa chừng: một phần file bị revert về gốc (MapView/services/runtimes/doc) — đã làm lại toàn bộ và verify lại bằng `git diff` + test live. Các file deploy `.env.example`/`Caddyfile`/`docker-compose.simple.yml` có thay đổi không phải của bước này — để nguyên, không đụng.

---

## Bước 2. FieldManager: gọn khối "Liên kết dữ liệu"

### Nội dung
- Modal Sửa field loại `table`: bỏ 3 dropdown khoanh đỏ (DataList / Cột giá trị / Cột cha cascade), chỉ giữ 1 ô tick "Lấy dữ liệu từ DataList"; không tick thì không hiện thông tin điều kiện phía dưới.
- File duy nhất: `frontend/src/components/admin/FieldManager.jsx:1026-1210`.

### Yêu cầu cần đạt
- Xóa block cũ `1037-1083` (+ banner `1028-1035` nếu muốn); giữ checkbox `1110-1123` + khối `{enabled && ...}:1135-1210` nguyên.
- Untick → chỉ còn 1 checkbox trơ trụi; tick → hiện khối chi tiết (datalist_id/trigger_column/conditions/default_column).
- **Giữ value, không clear** `col.data_list_id/data_list_column/parent_column` trong DB (untick chỉ set `data_link.enabled:false`; `normalizeTableLink` ở `DynamicField.jsx:57-80` + `dynamicUtils.js:263-286` vẫn fallback model cũ).

### Checklist test
- [x] Mở field table → untick chỉ thấy 1 checkbox, không thấy 3 dropdown cũ.
- [x] Tick → khối điều kiện hiện; untick → ẩn; lưu/reload không mất cấu hình `chi_phi_lk` đang chạy.
- [x] Submit form có bảng autofill vẫn chạy (model cũ + mới cộng sinh).

### Kết quả test theo checklist
- ✅ Xóa block 3 dropdown (`DataList`/`Cột giá trị`/`Cột cha cascade`) + banner phụ; chỉ còn header "Liên kết dữ liệu" + 1 checkbox `Lấy dữ liệu từ DataList` + khối điều kiện `{enabled && ...}`.
- ✅ `npm run build` pass; grep xác nhận không còn `Cột cha (cascade)`/`Cột giá trị` trong block table-column (chỉ còn `data_list_column` của field select top-level — đúng).
- Ghi chú: giữ nguyên value `data_list_id/data_list_column/parent_column` trong DB, không clear (fallback `normalizeTableLink` vẫn chạy).

### Ghi chú
- Dọn kéo theo (sau): badge `DL/DL ẩn/Cascade:876-878`, note `974-990`, `resolveCellField` fallback — giữ để tương thích ngược hoặc xóa khi quyết bỏ hẳn model cũ.

---

## Bước 3. Form 3/4 cột + reflow + nới modal

### Nội dung
- Cấu hình form: thêm loại 3 và 4 cột cho desktop, mobile giữ 1 cột; thêm sắp xếp nhanh 2:1→3:1/4:1 tự động dịch hàng, xóa ô thừa; desktop mở rộng form, khoảng trống mép nhỏ nhất.
- Parser đã generic N cột (`DynamicForm.jsx:785`, `RecordDetailPopup.jsx:578`, flex `App.css:2107`), nút thắt là `COL_OPTIONS` + modal 700px.

### Yêu cầu cần đạt
- `FormBuilder.jsx:15-18` thêm `{1:3},{1:4}`; `updateRowColumns:202-219` tổng quát `N=parseInt(columns.split(':')[1])`, unassign mọi `colIndex>=N`.
- Nút **"Sắp xếp nhanh toàn form" `1:1/2:1/3:1/4:1`** đặt cạnh toggle Desktop/Mobile: `reflowForm(N)` xếp lại hàng/cột **cho mọi section cùng lúc**, **section giữ nguyên** (không gộp/đổi section), mỗi section giữ đủ số field của nó (vd 12 field 2:1 = 6 hàng → 4:1 = 3 hàng), xóa hàng rỗng, confirm trước.
- CSS: giữ breakpoint 768 mobile `flex-direction:column`; thêm `gap` nhỏ cho N lớn (`1:3:10px, 1:4:8px`).
- Nới modal: `.legacy-modal.popup-detail{max-width:1140px;width:96%}` (`public/legacy-modal.css`); `popup-body` `16/20→12/14`, `.popup-detail .dynamic-form{max-width:100%}`.

### Checklist test
- [x] Tạo form 3/4 cột desktop đều ô; mobile (<768px) vẫn 1 cột.
- [x] Sắp xếp nhanh toàn form 2:1→4:1: mọi section giữ nguyên + giữ đủ field, chỉ đổi số hàng/cột, xóa hàng rỗng.
- [x] Modal mới không tràn màn 1366px; modal confirm/avatar khác không bị phình.
- [x] `npm run build` pass.

### Kết quả test theo checklist
- ✅ `COL_OPTIONS` thêm `1:3`/`1:4`; select cột mỗi hàng hiện `1/2/3/4 cột`.
- ✅ Thanh công cụ có `Sắp xếp nhanh toàn form: 1:1/2:1/3:1/4:1`; Playwright `/admin/forms/14/edit` bấm `4:1`: preview 45 hàng → 20 hàng, toàn bộ `data-cols="1:4"`, field vẫn đủ 67, không pageerror.
- ✅ `updateRowColumns` tổng quát: unassign mọi `colIndex>=N`.
- ✅ Build pass.
- Ghi chú: `reflowForm` gom field theo thứ tự row/col trong từng section rồi chunk N; **section không bị thay đổi** (không gộp/tách).

### Ghi chú
- Tùy chọn breakpoint trung gian 1024px cho `1:4→2+2` nếu laptop nhỏ chật — mặc định giữ 4 cột desktop theo yêu cầu.

---

## Bước 4. Ranh giới tỉnh: bỏ khung HCN, đỏ bo sát ranh

### Nội dung
- User báo click tỉnh hiện khung HCN; DevTools gửi kèm `<path class="leaflet-interactive" stroke="#1565C0" dasharray="8,5" fill="transparent" d="M369 163L361...">` chính là polygon ranh giới của app (`leafletRuntime.js:278-300`, nguồn `/vietnam-provinces.geojson`, Phú Thọ 133 đỉnh — vốn sát ranh).
- Nguyên nhân đã xác minh: (a) ảnh xanh nhạt là DevTools hover highlight bounding-box, không phải app; (b) ảnh đen khi click thật là browser focus outline — `L.geoJSON` boundary không truyền `keyboard:false` nên path có `tabindex=0`, click→`:focus`→Chrome vẽ outline HCN theo bbox (app + `leaflet.css` đều không có rule triệt outline cho path).
- Chốt với user: mặc định giữ nền trong suốt + viền xanh như hiện tại; chỉ khi click mới viền đỏ nổi bật bo sát ranh.

### Yêu cầu cần đạt
- `App.css` thêm `.leaflet-overlay-pane path.leaflet-interactive:focus{outline:none}` (+ `:focus-visible`).
- `App.css`: thêm `outline:none` cho `:focus`/`:focus-visible` của boundary path (diệt khung HCN); tooltip giữ xanh.
- `leafletRuntime.js#setBoundaries`: base giữ nguyên xanh `#1565C0` nét đứt + nền trong suốt; `onEachFeature` giữ `bindTooltip` + thêm `layer.on('click')` → `resetStyle` các tỉnh + `setStyle({color:'#dc2626',weight:3,fillOpacity:0.12})` + `bringToFront()`.
- MapLibre: base giữ xanh nét đứt; click → `setPaintProperty` match theo `properties.name` (`#dc2626`/width 3 cho tỉnh được chọn), click nền reset.

### Checklist test
- [x] Click polygon tỉnh → không còn khung HCN; viền tỉnh được click đỏ sát ranh, tỉnh khác reset.
- [x] Tắt "Ranh giới" → hết lớp boundary; đổi Leaflet↔MapLibre không lỗi.
- [x] Hover Elements trong DevTools vẫn thấy bbox (đúng hành vi DevTools, không phải bug).

### Kết quả test theo checklist
- ✅ Playwright live `/map` (ctv, Leaflet): 34 polygon nền trong suốt + viền xanh `#1565C0` nét đứt như cũ, không pageerror; build pass.
- ⚠️ Click-highlight `#dc2626` mới verify ở mức code (pattern `resetStyle/setStyle` chuẩn Leaflet) — click headless không chạm được polygon do marker/label phủ trên; cần bạn click tay 1 tỉnh trên UI để chốt (đúng ra phải đỏ sát ranh, không còn khung HCN).

### Ghi chú
- Tuyệt đối không vẽ HCN theo `getBounds()` (bounds luôn là HCN bao — trái yêu cầu "sát tỉnh").

---

## Bước 5. Preview lân cận: +100km, map rộng bằng form, click xem như bước 1

### Nội dung
- `LocationMapModal.jsx`: thêm khoảng cách lọc 100km; bản đồ preview rộng như form; click điểm trong preview xem info như bước 1.
- Bổ sung: hàng nút lọc 4 trạng thái (Trạm: Hoạt động/Triển khai; Đề xuất: Đang đề xuất/Đã duyệt BCĐX) — bật/tắt từng chip, đếm "N điểm lân cận" cập nhật theo.
- Hiện trạng: `RADIUS_OPTIONS=[5,10,20,50]` (`:15`), lọc client bằng `haversineKm` (không qua `proximityService`, `MAX_RADIUS_M=5000` của BE không liên quan); modal không truyền `onMarkerClick` nên click chỉ popup tĩnh.

### Yêu cầu cần đạt
- `RADIUS_OPTIONS` thêm `100` (+ `zoomForRadius` ~8); modal dùng class `.legacy-modal-form` như bước 3.
- Popup `createNearbyPopup:145-146` enrich như bước 1 (mã/tên/mô hình/trụ; phụ thuộc fix BE bước 1).
- Truyền `onMarkerClick` vào `MapCanvas` trong modal → mở `RecordDetailPopup` (copy pattern `MapPage/AdminProposalsPage`) hoặc `window.open(/admin/.../view=id)`; title modal dùng mã thay vì `#id` (`RecordDetailPopup.jsx:514-516,672-676`).
- State `selStations/selProposals` khởi từ `statusFilter` (có thì dùng, không thì bật tất cả — giữ hành vi cũ ở `RecordDetailPopup`); chip tắt hết 1 entity → lọc rỗng entity đó; ghi chú gọn: chọn ≤6 hiện "(lọc ...)", nhiều hơn hiện "(ẩn ...)".

### Checklist test
- [x] Đủ 5 nút radius (5/10/20/50/100); chọn 100km ra điểm xa, label "N điểm lân cận" đúng.
- [x] Map preview rộng bằng form chi tiết; vòng tròn radius đúng tâm.
- [x] Click điểm trong preview → popup đủ info như map chính + mở được chi tiết.
- [x] 4 chip lọc (Hoạt động/Triển khai/Đang đề xuất/Đã duyệt BCĐX) bật/tắt đúng, đếm cập nhật.

### Kết quả test theo checklist
- ✅ Playwright live `/admin/proposals/view=635` → "Xem bản đồ": đủ 5 nút `5/10/20/50/100 km`, modal rộng 1140px, không pageerror.
- ✅ Click đề xuất trong preview ở `AdminProposalsPage`/`MyProposalsPage` mở chi tiết (đóng preview trước); trạm giữ popup; `MapPage` (flow tạo mới) và `RecordDetailPopup` (tránh modal lồng) giữ popup-only.
- ✅ Chip lọc live: đủ 4 chip tiếng Việt; tắt "Hoạt động" → "(ẩn Hoạt động)"; tắt "Đang đề xuất" → 1→0 điểm "(ẩn Đang đề xuất)"; không lỗi.
- Ghi chú test: test FE phải set token trước khi vào trang (set sau + reload sẽ bị đá về login rồi tự về trang mặc định).

### Ghi chú
- Giữ lọc client FE (đơn giản); chỉ chuyển sang endpoint BE `nearby` + nới `MAX_RADIUS_M` khi list lớn gây nặng.

---

## Bước 6. Mở Template/Import/Export đề xuất cho ADMIN + SALES

### Nội dung
- Hiện chỉ SUPER tải được (thực chất chặn kép): `excel.js:178,229,272,296` (`template/import/preview/confirm/progress`) `requireAdmin` (SALES 403) + `views.js:43` `GET /api/views` `requireSuperAdmin` → ADMIN cũng `excelViews=[]`. FE giấu nút SALES (`AdminStationsPage:348`, `AdminProposalsPage:1181`, skip load `67,107`).
- Chốt: **chỉ mở Đề xuất**; giữ Trạm (SALES read-only) và Users.

### Yêu cầu cần đạt
- BE: `excel.js:178,229,272,296` đổi `requireAdmin→requireUserManager`; giữ `export/station_proposals:92` nguyên; đồng bộ scope export SALES 1 cấp → đệ quy `getBranchIds` (GĐTT đang thiếu cháu).
- Views: thêm `GET /api/admin/excel/views?entity=` hẹp (`requireUserManager`, chỉ `status=active`); FE 2 trang admin chuyển sang endpoint này (không hạ cả `views.js:43`).
- FE: bỏ gate `isSales` khi load `excelViews` + bỏ gate Template/Import ở `AdminProposalsPage:1181`; **giữ** gate trạm (`AdminStationsPage:348`) và toàn bộ users (`AdminUsersPage:600-611`).

### Checklist test
- [x] SALES tải template/import/export đề xuất OK; ADMIN thấy dropdown bộ cột (trước đây trắng).
- [x] SALES vẫn không thấy nút Trạm/Users; import users chặn leo `SUPER_ADMIN` còn nguyên.
- [x] Import gán `user_id=người import` (`importConfirmDynamic:1493-1497`); rate limit admin+excel còn hiệu lực.

### Kết quả test theo checklist
- ✅ BE: `excel.js` đổi `template/import/preview/import/confirm/import/progress` sang `requireUserManager`; thêm `GET /api/admin/excel/views?entity=` (requireUserManager, chỉ `status=active`).
- ✅ Gate SALES theo entity trong `excelService` (`denySalesEntity`): SALES chỉ `station_proposals`.
- ✅ API test (SALES): views/template proposals `200`; template & import-confirm `stations` `403`; import-confirm proposals qua gate (`400` chỉ vì rows rỗng).
- ✅ API test (ADMIN): views/template `stations` `200` (trước đây views `403` khiến dropdown trắng).
- ✅ FE Playwright (SALES): `/admin/proposals` hiện `Template/Import/Export`; `/admin/stations` **ẩn** Template/Import; không pageerror.
- ✅ Build FE + restart BE OK.

### Ghi chú
- Đổi FE 3 trang admin sang `excelService.getViews(entity, token)` (endpoint hẹp) thay `viewService.getAll` (SUPER-only). `views.js` giữ nguyên SUPER.
- `exportUsers`/`exportStations` vẫn `requireAdmin`; users không mở cho SALES.
- Rủi ro: spam tạo hàng loạt (tốn geocode, notify duyệt-đẩy); `GET /api/views/:id` (`views.js:63`) hiện public — kiểm tra lại trước khi mở list.

---

## Bước 7. Search vị trí như GG Map (Geoapify proxy)

### Nội dung
- Chưa có search box (grepp `search|Autocomplete|forward` chỉ trúng search bảng); BE chỉ `reverse` (`geocodeService.js` + `POST /api/geocode/reverse`); key ở `geocode_configs.api_key` (không lộ public); Geoapify có sẵn `autocomplete` + `search` chung key, free ~3000 req/ngày.

### Yêu cầu cần đạt
- BE: `geocodeService.forward(text,{bias,limit})` + `POST /api/geocode/search` (validate `text>=3`, limiter riêng, cache `text+filter` TTL ngắn), Swagger; tuyệt đối proxy qua backend (pattern `tile_mode=proxy`), không gọi thẳng Geoapify từ browser.
- FE: search box trên `MapView/MapPage` (desktop góc trên, mobile gộp filter sheet), debounce ~400ms + abort, dropdown gợi ý → chọn → `flyTo([lat,lng],16)` (runtime đã có) + marker tạm + nút "Tạo đề xuất/trạm tại đây" tái dùng `onLocationSelected`; `filter=countrycode:vn` + `bias=proximity` tâm map + `lang=vi`.

### Checklist test
- [x] Gõ "vincom" → gợi ý VN → chọn bay tới đúng + tạo đề xuất tại đó được.
- [x] Key không lộ ở Network (chỉ gọi `/api/geocode/search`); gõ nhanh không nổ quota (debounce+cache).
- [x] Hết quota → lỗi mềm + gợi ý, không trắng map.

### Kết quả test theo checklist
- ✅ BE: `geocodeService.forward(text,{lat,lng,limit})` gọi Geoapify `/v1/geocode/autocomplete` (`filter=countrycode:vn`, `bias=proximity`, `lang=vi`) + cache in-memory 5 phút; `POST /api/geocode/search` (`geocodeLimiter`, validate `text>=3`) + Swagger.
- ✅ API test: `POST /api/geocode/search {text:'Vincom Ba Trieu Ha Noi'}` → `200`, `found:true`, 2 kết quả, first `Vincom Center Bà Triệu` (21.0111, 105.8494).
- ✅ FE: search box overlay (desktop giữa-trên, mobile full-width); debounce 400ms + abort theo `reqId`; dropdown gợi ý; chọn → `selectedPosition` + `flyTo` + card "Tạo đề xuất/Tạo trạm/Đóng".
- ✅ Playwright live `/map` (admin): gõ "Vincom Ba Trieu" → 6 gợi ý; chọn → label địa chỉ + 3 nút; map fly tới; không pageerror.
- Ghi chú: key nằm ở `geocode_configs`, không trả ra public — mọi truy vấn đi qua backend proxy (đúng pattern `tile_mode=proxy`).

### Ghi chú
- Ước lượng 0.5d BE + 1d FE + 0.5d test; không đổi schema lớn, không ảnh hưởng tile/reverse. Fallback Nominatim khi hết quota.

---

## Bước 8. Countdown cấu hình trên UI (thời gian + trạng thái áp dụng)

### Nội dung
- Hiện hardcode 8 chỗ: list status ở `DeadlineCountdown.jsx:3`, `proposalLifecycle.js:115-119` (`DEADLINE_CONFIG`), worker `processDeadlines:174-178` (`WHERE IN`); số ngày fallback `3/15` ở `proposalService:62,278`, `myProposalService:122`, `excelService:1365` (giá trị thật migration 101: `review=3`, `principle=15`). Thêm countdown cho Duyệt BCĐX (`APPROVED`) phải sửa code.

### Yêu cầu cần đạt
- Migration `102`: thêm key JSON `supplement_countdown_config` trong `proposal_lifecycle_configs` (seed từ 2 key cũ, giữ key cũ fallback): `{warn_hours, rules:[{status,days,hours,minutes,enabled}...]}` (PENDING/REVIEWING 3 ngày on, PRINCIPLE 15 ngày on, APPROVED 10 ngày off).
- BE mới: `GET/PUT /api/admin/lifecycle-config` (`requireAuth` đọc, SUPER sửa; validate `status∈ALL_STATUSES`, `days 0–365`, `hours 0–23`, `minutes 0–59`, tổng > 0); `getDeadlineMinutes()` thay hardcode; số ngày quy đổi phút và set `DATE_ADD(NOW(), INTERVAL ? MINUTE)`; worker đọc `rules.filter(enabled)` + `warn_hours` thay hằng `24h`.
- FE: section "Countdown bổ sung thông tin" trên trang cấu hình form đề xuất: table 9 status gồm toggle + **Ngày/Giờ/Phút** + input `warn_hours`; `DeadlineCountdown.jsx` bỏ hằng cứng, nhận `enabledStatuses[]` (cache module-level, fallback list cũ).
- Không nhét vào `forms.layout_config` hay `field_definitions.options` (sai tầng).

### Checklist test
- [x] Sửa days/warn_hours + bật `APPROVED.enabled=true` trên UI → đề xuất vào APPROVED được set `supplement_deadline_at=NOW()+days`, hiện countdown + nhận nhắc quá hạn, không sửa code.
- [x] Tắt 1 rule → status đó không countdown, không nhắc worker.
- [x] Key cũ thiếu vẫn fallback `3/15`; `npm run build` + worker tick OK.

### Kết quả test theo checklist
- ✅ Migration `102`: `value` → TEXT + seed key `supplement_countdown_config` (từ 2 key cũ, idempotent). Đã chạy qua `scripts/migrate.sh run` (tracking `schema_migrations`).
- ✅ BE: `proposalLifecycle.getCountdownConfig/saveCountdownConfig/getDeadlineDays/getEnabledCountdownStatuses/getCountdownWarnHours`; `transition()` dùng `getDeadlineDays(to)`; worker `processDeadlines` lọc theo status bật + `warn_hours`; create/resubmit/import dùng `getDeadlineDays('PENDING')`.
- ✅ API mới `GET /api/admin/lifecycle-config` (`requireAuth`) + `PUT` (`requireSuperAdmin`) + Swagger; GET trả `{warn_hours, rules[5]}`; PUT lưu OK.
- ✅ API test: `getDeadlineMinutes('APPROVED')` = `null` khi tắt; bật `APPROVED days=7` → `10080`; tắt lại → `null`.
- ✅ **Ngày + Giờ + Phút**: rule `PENDING {0 ngày, 6 giờ, 30 phút}` → `390` phút; `REVIEWING {1 ngày, 2 giờ, 15 phút}` → `1575` phút; restore seed `4320` phút = 3 ngày.
- ✅ FE `CountdownConfigPanel`: header `Trạng thái | Áp dụng | Ngày | Giờ | Phút`; Playwright xác nhận row PENDING `3 / 0 / 0`; `warn_hours=24`.
- ✅ `DeadlineCountdown` bỏ hằng cứng, đọc danh sách bật từ config (cache module + event `countdown:refresh`), fallback list cũ; build pass.
- Ghi chú: giá trị test tạm đã khôi phục về seed (3/3/15, APPROVED/ARCHIVED off, warn 24).

### Ghi chú
- Backfill 1 lần cho APPROVED đã tồn tại mà `supplement_deadline_at IS NULL` (guard NULL, chạy 1 lần trong migration 102).

---

## Bước 9. Nút thước đo trang map (kiểu ruler Google Earth)

### Nội dung
- Trang `/map` thêm nút thước đo: bật chế độ đo, click nhiều điểm trên bản đồ vẽ đường, hiện khoảng cách từng đoạn + tổng chiều dài; double-click/Esc kết thúc, nút X xóa đo.
- Hiện trạng: thước duy nhất là nút Ruler trong `LocationMapModal.jsx:209-216` — chỉ bật/tắt đường nối sẵn từ tâm tới điểm lân cận (`pairs`, vẽ qua `polylineLayer` ở `leafletRuntime.js:213-237` / `app-polylines` ở `maplibreRuntime.js:362-413`); trang `/map` chính (`MapView.jsx:807` cụm controls + FAB `922-960`) **chưa có đo tự do**. Chưa cài plugin đo nào (`package.json` chỉ có `leaflet`, `leaflet.markercluster`, `maplibre-gl`).

### Yêu cầu cần đạt
- Nút Ruler (icon `lucide-react`) cạnh cụm controls `MapView.jsx:807`, toggle `measureMode`; khi bật: cursor crosshair, click thêm điểm, di chuột preview đoạn đang vẽ, double-click/Esc kết thúc, click nút X xóa.
- Vẽ không thêm dependency: Leaflet dùng `L.polyline` + `L.marker` circleMarker + `bindTooltip` khoảng cách (tái dùng `formatDistanceM` + class `dup-distance-label` ở `App.css:3223`); MapLibre dùng source GeoJSON `app-measure` + layer line/symbol (pattern `app-polylines:381-413`).
- Tính khoảng cách bằng haversine có sẵn (mẫu `LocationMapModal.jsx:29-36`); hiển thị nhãn từng đoạn + 1 nhãn tổng (km/m); thoát measureMode không xóa nét đã đo (chỉ X mới xóa); không xung đột click marker/popup, FAB tạo trạm/đề xuất, cluster.

### Checklist test
- [x] Bật thước → click 3 điểm → hiện 2 nhãn đoạn + 1 nhãn tổng đúng số (đối chiếu haversine).
- [x] Esc/Xóa/Hoàn tất kết thúc đúng; cursor crosshair chỉ khi đang đo.
- [x] Leaflet + MapLibre đều đo được (chung seam `setMeasure`, không thêm dependency).
- [x] Marker popup, filter, legend, tạo trạm/đề xuất vẫn chạy khi thước đang bật.

### Kết quả test theo checklist
- ✅ Playwright live `/map` (ctv, Leaflet): click 2 điểm → nhãn đoạn `642.56 km` + `Tổng: 642.56 km`, thanh hint hiện tổng; Esc xóa sạch; không lỗi.
- ✅ Build pass. Không dùng double-click (tránh xung đột zoom Leaflet) — kết thúc bằng nút Hoàn tất/Esc.

### Ghi chú
- Phạm vi bước này chỉ đo đường (line); đo diện tích polygon để dành nếu user yêu cầu tiếp. Không cần migration/BE.

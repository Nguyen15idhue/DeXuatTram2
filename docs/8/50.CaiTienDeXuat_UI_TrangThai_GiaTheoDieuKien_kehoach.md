# 50. Cải tiến đề xuất (table actions, trùng, timing, Duyệt chủ trương, section, giá theo điều kiện) — kế hoạch

**Ngày**: 20/09/2026. **Trạng thái**: Chốt yêu cầu, chưa triển khai.
**Nguồn**: tổng hợp các chốt với user ngày 20/09/2026 (chỉ phân tích, chưa sửa code).
**Cấu trúc**: mỗi bước gồm Yêu cầu cần đạt → Checklist test → Kết quả test → Ghi chú.

---

## Bước 1. Nút "Xem" ở table đề xuất

### Yêu cầu cần đạt
- Nút mắt vuông → nút chữ **"Xem"** (`btn-xs`), đặt **đầu tiên** trong cụm actions.
- Thứ tự: **Xem – Duyệt&đẩy (primary action theo trạng thái) – 3 chấm**.
- Xóa item "Xem chi tiết" trùng trong menu 3 chấm (`AdminProposalsPage.jsx:940`).
- Giữ `whitespace-nowrap`, dùng `btn-xs` để không vỡ dòng (bảng đã nhiều cột).
- Chạm 1 file: `frontend/src/pages/admin/AdminProposalsPage.jsx` (`renderActions:1011`).

### Checklist test
- [ ] Dòng `PENDING` hiện: Xem – Duyệt & đẩy – 3 chấm.
- [ ] Dòng `REVIEWING`/`APPROVED`/`CONTRACT_SIGNED` giữ đúng primary action cũ, nút Xem vẫn đầu tiên.
- [ ] Menu 3 chấm không còn "Xem chi tiết"; các item còn lại (Sửa, Xem log, Hủy...) đủ.
- [ ] Bảng không vỡ dòng ở màn hình 1366px.

### Kết quả test theo checklist
- ✅ Dòng `PENDING`: Xem – Duyệt & đẩy – 3 chấm (Playwright headless, 10 dòng).
- ✅ Các trạng thái khác giữ primary action cũ (không đụng `renderPrimaryAction`/`renderStatusActions`).
- ✅ Menu 3 chấm không còn "Xem chi tiết" (còn Sửa/Xem log/Từ chối/Hủy/Xóa/Link).
- ✅ Không vỡ dòng (`whitespace-nowrap` giữ nguyên, build pass).
- Ghi chú test: 1 lần xóa nhầm dòng `if (isAdmin)` khi sửa menu, đã khôi phục + verify code.

---

## Bước 2. Check trùng: bỏ hết chặn, chỉ giữ tra cứu 0–xxxx m

### Yêu cầu cần đạt
- Hạ thành **cảnh báo vàng (không chặn, vẫn cho lưu)** tại:
  1. `backend/src/services/proposalService.js:179-190` (tạo đề xuất, < 200m → 400).
  2. `backend/src/services/proposalService.js:192-205` (luật riêng SĐT/1000m — **chốt bỏ luôn**).
  3. `backend/src/services/excelService.js:1221-1234` (preview import: check DB + intra-file).
  4. `backend/src/services/excelService.js:1413-1424` (confirm import: check DB + intra-file).
- Cơ chế check trùng **duy nhất còn lại**: `DuplicateCheckPanel` → API `duplicates` →
  `findDuplicates` (`proximityService.js:80`), đã gắn ở 2 trang
  (`AdminProposalsPage:1202`, `MyProposalsPage:462`).
- Đổi default `min_m = 200` → `0` tại `excelService.js:952` (FE đã gửi `min_m` riêng nên không vỡ).
- Sửa message/docs liên quan (không còn chữ "không cho import").

### Checklist test
- [x] Tạo đề xuất cách trạm cũ 50m → chỉ cảnh báo vàng, vẫn tạo được.
- [x] Tạo 2 đề xuất cùng SĐT trong 1000m → không còn bị chặn.
- [x] Import file có dòng trùng (< 200m với DB + intra-file) → preview báo vàng, confirm vẫn nhập.
- [x] Panel tra cứu 0–xxxx m ở 2 trang còn chạy (đổi min/max ra kết quả đúng).

### Kết quả test theo checklist
- ✅ Guest submit cách #691 ~6.5m → **201** + `warnings: ["Vị trí này gần với đề xuất #691 ..."]` (trước đây 400).
- ✅ Luật SĐT/1000m bỏ: submit trùng SĐT gần vị trí cũ vẫn 201 + warning (gộp trong mảng warnings).
- ✅ Import preview file 2 dòng trùng: `validRows=2, errorRows=0, warningRows=3` (trùng DB ×2 + intra-file ×1).
- ✅ Import confirm: `success, imported=2`, `warnDetails` đủ 2 dòng; DB có 2 bản ghi (đã cleanup).
- ✅ Modal `/admin/proposals`: stat vàng "Cảnh báo trùng 3" + list vàng "vẫn cho import" (Playwright).
- ✅ `min_m` default 200→0 ở `exportDuplicates`; `findDuplicates` cho `min_m=0`
  (`proximityService.js:83`); API `duplicates?min_m=0&max_m=2000` trả pairs đúng.
- ✅ build FE pass; BE restart OK.
- Ghi chú test: phải restart backend 2 lần (rate limit guest 5/h dính khi test nhiều lần).

### Ghi chú
- Import giữ transaction all-or-nothing; hạ chặn đồng nghĩa file trùng vào hết — đúng ý đã chốt.

---

## Bước 3. Trạng thái "Duyệt chủ trương" (`PRINCIPLE_APPROVED`)

### Yêu cầu cần đạt
- Ngang hàng `APPROVED`, đứng **trước** `APPROVED`. Ma trận (in đậm phần mới):
  - `REVIEWING → **PRINCIPLE_APPROVED** | APPROVED | CANCELLED | ARCHIVED`
  - `**PRINCIPLE_APPROVED → APPROVED | CANCELLED**`
  - Mọi trạng thái khác **không** được chuyển sang `PRINCIPLE_APPROVED`.
- Dây chuyền như migration 94: migration ENUM 8→9 + option (label/màu/icon/legend/sort_order),
  `proposalLifecycle.js` (`ALL_STATUSES` + `ALLOWED_TRANSITIONS`), `externalEventService.js`,
  `validStatuses`, `VALID_STATUSES` (excel), `notificationService.js`, `dashboardService.js`,
  `templateService.js`, Swagger enums, FE (`mapStatuses.js`, `NotificationBell.jsx`,
  `WebhookListener.jsx`, `AdminProposalsPage.jsx` nút + menu + filter, chip map + legend).

### Checklist test
- [x] `REVIEWING → PRINCIPLE_APPROVED` OK; `PENDING/APPROVED/ARCHIVED → PRINCIPLE_APPROVED` 400.
- [x] `PRINCIPLE_APPROVED → APPROVED | CANCELLED` OK; sang status khác 400.
- [x] Webhook event mới đổi đúng trạng thái; notification loại mới tạo đúng.
- [x] Filter trạng thái, màu chuông, marker/legend bản đồ hiện đúng status mới.
- [x] Nút tay "Đã duyệt BCĐX" (demo, `manual_override`) vẫn chạy từ Duyệt chủ trương.

### Kết quả test theo checklist
- ✅ Ma trận API: `PENDING→REVIEWING→PRINCIPLE_APPROVED→APPROVED` 200;
  `PENDING→PRINCIPLE_APPROVED` 400; `APPROVED→ARCHIVED` 400 (test proposal đã cleanup).
- ✅ Webhook `event=PRINCIPLE_APPROVED` → 200, proposal `REVIEWING→PRINCIPLE_APPROVED`.
- ✅ Notify `type=PRINCIPLE_APPROVED` + title "Đề xuất đã được duyệt chủ trương" tạo đúng
  (xóa đề xuất xóa kèm notify — verified trực tiếp qua `transition()`).
- ✅ UI: nút "Duyệt chủ trương" ở dòng REVIEWING; legend/filter map có "Duyệt chủ trương"
  (Playwright); màu chuông indigo `#e0e7ff/#6366f1/#3730a3`; marker icon `flag`.
- ✅ Migration 96: ENUM 9 giá trị + option `sort_order=3` (các option khác dồn +1),
  tracking `schema_migrations` OK (lưu ý: repo đã có migration 95 nên dùng số 96).
- Ghi chú test: test API phát hiện `/api/field-definitions` trả thiếu defs so với DB
  (50 vs 37 required) — không ảnh hưởng, chỉ lưu ý khi viết test.

### Ghi chú
- Sau sửa backend nhớ `docker restart station-backend` (node --watch miss reload qua bind-mount).

---

## Bước 4. Sửa/xóa theo trạng thái (kèm Duyệt chủ trương)

### Yêu cầu cần đạt
- `PRINCIPLE_APPROVED` = **được sửa (bổ sung thông tin), không được xóa**;
  sang `APPROVED` khóa sửa hẳn (giữ quy tắc hiện tại: REVIEWING cấm xóa, APPROVED cấm sửa).
- Chạm `myProposalService` + `adminProposalService` + nút Sửa/Xóa FE (2 trang proposals + popup).

### Checklist test
- [x] CTV sửa được ở `PRINCIPLE_APPROVED`, bị chặn xóa (403 + message rõ).
- [x] Sang `APPROVED`: CTV sửa bị chặn; admin sửa theo phân quyền cũ.
- [x] Nút Sửa/Xóa ẩn/hiện đúng theo từng trạng thái ở table + popup.

### Kết quả test theo checklist
- ✅ API (tài khoản CTV thật): sửa ở Duyệt chủ trương 200, status giữ nguyên
  (không resubmit); xóa ở Duyệt chủ trương 400; sửa ở APPROVED 400.
- ✅ UI `/my-proposals`: dòng Duyệt chủ trương hiện Sửa, không hiện Xóa (Playwright).
- ✅ Xóa CTV chỉ cho PENDING (giữ nguyên, BE + FE); admin xóa/sửa không giới hạn (giữ nguyên).
- ✅ Test data (proposal + 2 CTV) đã cleanup; build FE pass.

### Ghi chú
- RESUBMIT (`REJECTED → PENDING`) giữ nguyên, không áp cho status mới.

---

## Bước 5. Countdown + notify + khung webhook ngoài

### Yêu cầu cần đạt
- Thêm cột `supplement_deadline_at` (DATETIME, NULL). Vào `REVIEWING` set hạn X1 ngày,
  **sang Duyệt chủ trương reset hạn X2 ngày mới** (không cộng dồn). X lấy từ
  `proposal_lifecycle_configs`.
- Áp dụng cho `PENDING`/`REVIEWING` (bổ sung + đồng bộ 1Office) và `PRINCIPLE_APPROVED`.
- FE: component đếm ngược client-side (poll 1s) trong popup + badge ở table khi < 24h.
  Gần hạn → vàng; quá hạn → đỏ "Quá hạn 01 ngày:01 giờ:04 phút:35 giây", **không tự hủy**.
- Notify trong app: loại mới `REVIEW_*_EXPIRING`, `*_OVERDUE`, `PRINCIPLE_APPROVED`
  (+ màu chuông riêng) tới chủ đề xuất + sales nhánh + admin; worker bắn ở ngưỡng
  (còn 24h, quá hạn).
- Khung webhook ngoài (làm sẵn, không chi tiết): hàm `notifyExternal(event, payload)`
  trong `notificationService` + `webhook_url` trong `proposal_lifecycle_configs`
  (trống = tắt); worker gọi sau khi tạo notify. Chưa retry/signature.

### Checklist test
- [x] Vào `REVIEWING` sinh deadline X1; sang Duyệt chủ trương reset X2 (check DB).
- [x] Popup đếm ngược đúng từng giây; < 24h hiện badge vàng ở table.
- [x] Quá hạn hiện đỏ "Quá hạn ..." và đếm tiếp, đề xuất không bị hủy.
- [x] Notify đến hạn/quá hạn/Duyệt chủ trương tới đúng owner + sales + admin.
- [x] `webhook_url` trống → không bắn ngoài, không lỗi; điền URL → worker gọi 1 lần/sự kiện.

### Kết quả test theo checklist
- ✅ Migration 97: cột `supplement_deadline_at` + 3 config
  (`review/principle_supplement_days=7`, `supplement_webhook_url=''`), tracked.
- ✅ Tạo mới (user/guest/import) sinh deadline +7d; `REVIEWING` reset +7d;
  `PRINCIPLE_APPROVED` reset +7d (check DB).
- ✅ Worker: deadline +30p → 1 notify `SUPPLEMENT_EXPIRING` (owner + chain + admins);
  deadline -1h → 1 notify `SUPPLEMENT_OVERDUE`; chạy lại không trùng (dedupe activity log).
- ✅ `notifyExternal` với URL trống → `{sent:false}`, không lỗi, không chặn worker.
  Với URL (dummy local) → `{sent:true}` (POST đi thật), config đã trả về rỗng.
- ✅ FE: popup hiện banner đếm ("Còn ... để bổ sung"); table badge vàng
  "Còn 00 ngày 01:59:41" khi < 24h (Playwright); chuông có màu 2 loại mới.
- ✅ Test data đã cleanup hết; build FE pass.

### Ghi chú
- Không dùng cron dày cho countdown FE (client-side là đủ); worker chỉ lo mốc notify.

---

## Bước 6. Section header + ô đã điền form create

### Yêu cầu cần đạt
- Header section gom về **1 rule CSS tập trung**: chữ **xanh lam đậm (màu hệ thống), to hơn**
  (ghi đè `legend` inline `#374151`/14px ở `RecordDetailPopup` `renderTabGroup`/
  `renderSectionNode`, `DynamicForm`, `.popup-section-title` trong `App.css`).
- Form **create**: ô đã điền tô nền rất nhạt `#f0f9ff`. Không áp view/edit.
  Không làm viền xanh lam cho ô đã điền.

### Checklist test
- [x] Header mọi section (form thường + tab lồng NQ_LK) xanh lam đậm, to hơn, đồng nhất create/view/edit.
- [x] Form create: nhập giá trị → ô nền `#f0f9ff`; xóa → về trắng.
- [x] View/edit không đổi nền; viền đỏ lỗi + viền focus còn nguyên.

### Kết quả test theo checklist
- ✅ Legend create + view đều `#1d4ed8` (rgb 29,78,216) qua class `form-section-title`
  (Playwright: modal Tạo nhanh + popup view #691).
- ✅ Nhập text ở form create → wrapper có `is-filled`, input nền `#f0f9ff`.
- ✅ Chỉ `purpose='create'` mới highlight (ProfilePage `purpose='view'` không ảnh hưởng).
- ✅ View/edit không thêm nền (không đụng `RecordDetailPopup`/`DynamicField`); CSS chỉ
  tô input trong `.is-filled` nên viền đỏ lỗi + focus giữ nguyên.
- ✅ Build FE pass.

### Ghi chú
- Ưu tiên CSS thuần, tránh sửa inline style rải rác để sau dễ đổi theme.

---

## Bước 7. Giá theo điều kiện (conditional autofill + select)

### Yêu cầu cần đạt
**Behavior:**
1. Ô giá **chỉ 1 dạng select, cấm sửa tay**. 1 giá trị → tự chọn sẵn (disabled);
   nhiều giá trị → mở chọn.
2. Điều kiện từ **field ngoài form-level** (vd `che_do_gia`), truyền xuống `DynamicField`
   nhánh table qua prop mới. Đổi điều kiện/selection chỉ đổi options + value trong ô giá —
   **số cột/header/độ rộng table giữ nguyên**.
3. Rule ở **định nghĩa cột** (`source_config`/`table_config`) → dòng mới tự áp; đổi điều kiện
   loop re-resolve **tất cả dòng**. Giữ giá cũ nếu còn trong tập mới, ngược lại tự điền/chọn
   lại + toast. Không xử lý dữ liệu cũ (đang test).
4. Import **validate mềm**: chỉ giữ required, giá lệch datalist vẫn cho qua, BE gắn cờ
   (trong `custom_data`/cột review) → FE badge vàng.

**Config (FieldManager — cụm "Tự điền từ cột"):**
- Giữ "DataList" + "Cột cha (cascade)" + "Tự điền từ cột" (`autofill_from`).
- Thêm "Giá theo điều kiện": field điều kiện ngoài + map
  `{a → cột 1 (gia_km), b → cột 2 (gia_niem_yet)}`, lưu
  `autofill_rules: [{when_value, column}]`, giữ `autofill_column` fallback.
- Runtime (`DynamicField.jsx:717-727` mở rộng): resolve cột theo rule → distinct giá →
  1 tự chọn / N thành options; chưa chọn `loai_chi_phi`/điều kiện → trống + placeholder.

### Checklist test
- [x] Điều kiện a → giá từ cột 1; điều kiện b → giá từ cột 2 (check từng dòng).
- [x] 1 giá trị → tự chọn + disabled; N giá trị → select chọn tay được.
- [x] Không nhập tay được vào ô giá (ô luôn là select).
- [x] Đổi điều kiện: giá còn hợp lệ thì giữ, hết hợp lệ thì đổi lại + toast; mọi dòng đều đổi.
- [x] Thêm dòng mới tự áp rule, không thao tác phụ; chưa chọn điều kiện/loại → trống + placeholder.
- [x] Import giá lệch datalist: vẫn nhập được, có cờ vàng để rà soát.

### Kết quả test theo checklist
- ✅ FE (Playwright, form Tạo đề xuất + tab Liên kết, rule tạm
  `mo_hinh_dau_tu: NQ_LK→gia_km`): 1 giá trị → select disabled + tự điền đúng cột
  (`4,500,000` từ `gia_km` khi NQ_LK); thêm dòng datalist trùng tên → 2 options,
  select mở, chọn tay `4,200,000` được; ô giá luôn là select (cấm nhập tay).
- ✅ Đổi điều kiện (rule tạm `vung_mien: Bắc→gia_km`): trống→`gia`, Bắc→`gia_km`,
  Trung→`gia`; giá đang chọn hết hiệu lực thì xóa trắng (không giữ rác).
- ✅ Server `resolveTablePrices`: giá sai + nhiều options → giữ + flag
  (`Chi phí: dòng 1 giá "999" không thuộc...`); giá đúng giữ nguyên.
- ✅ Import confirm giá lệch: `success, imported=1` + `warnDetails` cờ vàng (đã cleanup #710).
- ✅ Hồi quy: rule rỗng → autofill `gia` cũ + number input nguyên vẹn.
- ✅ Cụm cấu hình "Giá theo điều kiện" hiện ở `/admin/fields` (Playwright).
- ✅ Dữ liệu test đã revert hết (`price_rules`, cột `gia_km`, dòng VC2) — user tự cấu
  hình thật qua UI khi dùng; build FE pass.
- ✅ Giữ giá hợp lệ trên FE: đổi `loai_chi_phi` VC→TBA khi cùng giá 4.5M → giữ nguyên
  + disabled (Playwright, rule tạm trùng giá).

### Ghi chú
- Điểm gợn duy nhất: table render row-local, phải truyền giá trị điều kiện form-level xuống.

---

## Bước 8. Gọn UI cấu hình cột table (`FieldManager.jsx:821-1011`)

### Yêu cầu cần đạt
Thực trạng: thẻ cột nào cũng hiện đủ mọi cụm — cột `so_tien` (DataList = Không) vẫn hiện
"Cột cha", "Tự điền từ cột", "Lấy cột DataList" (do điều kiện hiện chỉ là `columns.length > 1`,
`:974`/`:987`, và `!!col.autofill_from`, `:1000`).
1. Luôn hiện mỗi dropdown **DataList**; **Cột giá trị** chỉ khi đã chọn DL (`:964` giữ);
   **Cột cha** chỉ khi cột này có DL (cascade chạy trên `tree`, `DynamicField.jsx:683-691`).
2. **Tự điền từ cột** chỉ khi ≥ 2 cột **và** chưa gán Formula (formula thắng autofill,
   `DynamicField.jsx:729-734`); **Lấy cột DataList** chỉ khi cột nguồn có DL, đổi label
   "Lấy cột từ DataList của cột nguồn" + hint. Thêm cụm "Giá theo điều kiện" (bước 7).
3. Cột select tay đã chọn DL → ẩn ô Options + note "đang lấy từ DataList"
   (runtime ưu tiên DL, `:680`).
4. Thẻ cột có header thu gọn `#n key — label [badge DL/Cascade/Auto/Fx]`, body collapse
   mặc định. Bỏ DL thì giữ giá trị cũ + badge "đang ẩn" (không xóa).
- Chỉ `FieldManager.jsx`, không đổi key lưu.

### Checklist test
- [x] Cột number thuần: chỉ còn key/label/width/bắt buộc + Kiểu hiển thị số + Footer.
- [x] Cột chưa chọn DL: ẩn Cột cha/Tự điền/Lấy cột; chọn DL → hiện dần đúng.
- [x] Cột có formula: ẩn Tự điền; cột select + DL: ẩn Options tay + có note.
- [x] Collapse/expand từng thẻ cột chạy; config cũ mở lên không mất giá trị.

### Kết quả test theo checklist
- ✅ Mặc định collapse hết, header có badge (DL/Auto/Fx...) — Playwright `/admin/fields`
  (field `chi_phi_lk`).
- ✅ Mở thẻ `so_tien` (không DL): ẩn Cột cha; hiện "Tự điền từ cột" (không formula) +
  "Lấy cột từ DataList của cột nguồn" (nguồn có DL) + cụm "Giá theo điều kiện".
- ✅ Mở thẻ `loai_chi_phi` (có DL): hiện Cột giá trị + Cột cha.
- ✅ Lưu (Cập nhật) thành công, `source_config` giữ nguyên `autofill_*`/cột.
- ✅ Build FE pass.

### Ghi chú
- Nên làm sau bước 7 (cụm "Giá theo điều kiện" đặt vào khung UI mới luôn).

---

## Checklist hồi quy chung (sau tất cả các bước)

- [x] `npm run build` OK; `docker restart station-backend` sau sửa backend.
- [x] Không vỡ flow cũ: tạo/sửa/xóa đề xuất, import excel, đẩy 1Office, bản đồ, phân quyền.

### Kết quả hồi quy
- ✅ 3 containers Up; Swagger 124 paths load OK; build FE pass cuối.
- ✅ Không push GitHub (đúng yêu cầu).

---

# PHỤ LỤC — Vòng 2 (yêu cầu bổ sung sau nghiệm thu vòng 1)

**Ngày**: 20/09/2026. Nguồn: phản hồi nghiệm thu của user sau khi chạy thử vòng 1.

## P-B6. Section header to hơn nữa

### Yêu cầu
- Chữ section hiện 15px vẫn nhỏ hơn ý muốn → **17px/700** `#1d4ed8`, ngang tiêu đề thực sự.

### Thực hiện
- `App.css`: `legend.form-section-title` 15→17px + `letter-spacing: 0.2px`;
  2 định nghĩa `.popup-section-title` trùng nhau đều về 17px/700 `#1d4ed8`
  (cái 14px/600 `#4a6cf7` đứng sau đang override — bỏ).

### Kết quả
- ⬜ Chưa test (build pass; verify màu/cỡ bằng Playwright ở lượt test tổng cuối).

## P-B2. Xóa hết auto-check chặn hành động user (FE)

### Yêu cầu
- Chỉ giữ panel "Check trùng" thủ công ở 2 trang proposals.
- Xóa mọi check tự động chặn: điền form (banner vàng + chặn submit), không còn cảnh báo nóng.

### Thực hiện
- `GuestProposalPage.jsx`: xóa state `nearbyWarning` + useEffect `checkNearbyPublic` + banner render.
- `MapPage.jsx`: xóa state + useEffect `checkNearby` + khối chặn trong `handleSubmit` (trước đây `return` không lưu) + banner.
- `MyProposalsPage.jsx`: xóa state + useEffect + khối `throw` trong `handleCreateProposal` + banner.
- `help/guideData.groups.js` G12B: "từ trên 200 đến 5000" → "từ 0 đến 5000".
- Giữ nguyên: `DuplicateCheckPanel` 2 trang, API `checkNearby`/`check-nearby-public`/`duplicates` (panel vẫn dùng), warnings import.

### Kết quả
- ⬜ Chưa test Playwright (grep xác nhận 0 còn `nearbyWarning`/`checkNearbyPublic` ở 3 trang; build pass).

## Còn lại (chưa làm)
- **P-BS1**: import confirm ghi log `created` (source `import`) + migration backfill deadline cho rows cũ.
- **P-B5**: chip countdown cạnh nút "Xem bản đồ" trong popup view.
- **P-B3**: +24 icon catalog, `STATUS_DOT.PRINCIPLE_APPROVED`, ▲▼+kéo-thả options manual, helper `sortByOrder`.
- **P-B7**: cấu hình cột A mới (toggle "Lấy dữ liệu từ DataList" → Danh mục dữ liệu ▾ + "Tự động điền khi cột [x] có giá trị" ▾ + bảng điều kiện 5 cột `STT | CỘT A(field form) ▾ | TOÁN TỬ ▾ | GIÁ TRỊ | CỘT B(datalist) ▾` + "Cột mặc định ▾" sau bảng; mọi ô là select; runtime FE+BE + validate).

# Chưa làm được — P1 High

## A. Phân quyền & scope sai
- **H1. Dashboard sales thấy số toàn hệ thống** — `backend/src/services/dashboardService.js:17,23-24`: users/proposals có filter nhánh, nhưng `totalStations/activeStations/deployingStations` query toàn bảng. Fix: join/scope trạm theo nhánh (cần quy tắc trạm–sales, hiện chưa có thì ẩn số trạm với sales). **Sales van xem duoc thong tin tram nhu hien tai, khong sua muc do**
  - ✅ **Đã fix 10/09/2026:** sales scope trả `stations: null` (không query bảng stations), FE `AdminDashboard` ẩn card Stations khi `stations` null; sales vẫn xem `/admin/stations` như cũ. **Test:** BE admin `stations.total=39`, sales `stations=null` + `proposals.total=2` nhánh; FE sales không card Stations + badge nhánh, vẫn xem trạm; `test-h01.cjs` 7/7 PASS, build OK.
- **H2. Sales sửa được proposal qua URL** — `AdminProposalsPage` chỉ `allowEdit={isAdmin}` ẩn nút, nhưng mode lấy từ URL `/edit=:id` và API `PUT /admin/proposals/:id` (`requireUserManager`) không chặn field → sales vào thẳng link là sửa được. Fix: backend chặn sales ở controller update (chỉ đổi `status`), hoặc route riêng.
  - ✅ **Đã fix 10/09/2026:** `PUT /admin/proposals/:id` chuyển sang `requireAdmin` (sales đổi trạng thái qua `PUT /:id/status` như cũ); FE ẩn nút Sửa trong bảng với sales + `RecordDetailPopup` ép `mode=view` khi `allowEdit=false` (chặn cả URL `/edit=`). **Test:** sales PUT full=403 + dữ liệu nguyên, sales đổi status nhánh=200, admin PUT=200; FE sales 0 nút Sửa, URL `/edit=365` về view; `test-h02.cjs` 3/3 PASS, build OK.
- **H3. Trang files users không guard** — `frontend/src/App.jsx:73` route `/admin/:entity/:id/files` không `RoleRoute`, sales mở được (sidebar giấu nhưng URL mở được). Fix: bọc `RoleRoute` đúng.
  - ✅ **Đã fix 10/09/2026:** route bọc `RoleRoute ADMIN_AND_SALES` + `AdminRecordFilesPage` chặn entity `users` khi `!isAdmin` (stations/proposals giữ như trang chính; BE `getById` + file ownership vẫn chặn nhánh ở dưới). **Test:** sales `/users/3/files` → Không có quyền, sales `/stations/1/files` OK, admin `/users/3/files` OK; `test-h03.cjs` 3/3 PASS, build OK.
- **H4. Nút Retry/Cancel hiện cho mọi role** — `AdminAuditLogPage.jsx:242-249` vs backend `requireSuperAdmin` → ADMIN/SALES bấm là 403. Fix: chỉ render khi super admin.
  - ✅ **Đã fix 10/09/2026:** nút Retry/Cancel chỉ render khi `isSuperAdmin` (BE `retry/cancel` đã `requireSuperAdmin`, giữ nguyên). **Test:** sales retry BE=403, FE sales 0 nút Retry/Cancel, super load audit-log OK; `test-h04.cjs` 3/3 PASS, build OK.
- **H5. `GET /data-lists/:id` hở** — `backend/src/routes/dataLists.js:47` chỉ `requireAuth`, các route còn lại `requireSuperAdmin` → CTV đọc chi tiết + rows danh mục nội bộ. Fix: thêm `requireSuperAdmin`.
  - ✅ **Đã fix 10/09/2026:** admin `GET /:id` thêm `requireSuperAdmin`; form hooks (`useDataList`, `useDataListMap`, `DynamicForm`) chuyển sang route public `/data-lists/:id` (đúng mục đích public cho select, cả guest) để không vỡ form user thường. **Test:** CTV admin route=403, SUPER=200, public=200; FE CTV + super load bình thường; `test-h05.cjs` 4/4 PASS, build OK.
- **H6. Update user đổi `status`/pass không kiểm soát** — `adminUserController.js:64-135`: SALES đổi được `status` self/CTV; ADMIN đổi `status` ADMIN khác/self (tự khóa mình), đổi pass người khác không cần pass cũ. Fix: khóa `status` với sales; admin chỉ đổi status CTV/lower + cấm self-lock; tách đổi pass ra quyền riêng + audit.
  - ✅ **Đã fix 10/09/2026:** SALES `status` ép giữ cũ; ADMIN đổi status chỉ cho SALES/CTV (peer ADMIN/self → 403); SUPER tự đổi status → 400; thiếu `status` giữ cũ (chống NULL); tự đổi pass bắt buộc `old_password` đúng + ghi log `[security]` audit; FE modal hiện ô mật khẩu hiện tại khi tự đổi, `validateUpdateUser` check enum status. **Test:** sales PUT LOCKED giữ ACTIVE, admin self-lock PUT=403, admin chạm SUPER=403, admin khóa/mở CTV=200, tự đổi pass thiếu/sai cũ=400; `test-h06.cjs` 3/3 PASS, build OK.
- **H7. File của người khác/guest** — `fileController.js:61,77,103` chỉ bypass ADMIN/SUPER: sales nhánh chính đáng 403, file guest (`uploaded_by NULL`) chủ cũng 403. Fix: check ownership qua proposal (user_id) thay vì chỉ uploader.
  - ✅ **Đã fix 10/09/2026:** `canAccessFile` thêm check proposal (`user_id` trùng requester hoặc thuộc nhánh sales) và station (SALES xem được) bằng cách quét `custom_data` tìm file id; `getById` dùng chung; delete giữ nghiêm (uploader/admin). **Test:** sales tải file nhánh=200, CTV ngoài nhánh=403, CTV file proposal mình=200 (bản ghi tạm, đã xóa), sales file ngoài nhánh=403; `test-h07.cjs` 3/3 PASS, build OK.

## B. Chức năng sai
- **H8. Select trạng thái proposals gửi sai** — `AdminProposalsPage.jsx:356-364`: `value={row.status}` nhưng `option value={opt.label}` (filter dùng `opt.value`) → hiển thị sai + PUT status gửi label. Fix: `value={opt.value}`.
  - ✅ **Đã fix 10/09/2026:** option dùng `value={opt.value}` (chỗ duy nhất sai pattern này). **Test:** select hiển thị đúng + options là value chuẩn, PUT status bằng value OK (đã restore); `test-h08.cjs` 4/4 PASS, build OK.
- **H9. Công thức format rồi mới submit** — `DynamicForm.jsx:235-243` lưu chuỗi `formatNumber` (“1,000”, “100 kW”) vào `formData` → validate number backend nhận string. Fix: formData giữ số thô, chỉ format lúc hiển thị.
  - ✅ **Đã fix 10/09/2026:** `computeFormula` trả số thô (scope parse cả chuỗi cũ bằng `parseFormattedNumber`, tương thích dữ liệu đã lưu); `DynamicField` + `FieldRenderer` format lúc hiển thị theo `numberFormat/decimalPlaces/unit`. **Test:** BE preview trả số thô 1234; FE form tạo đề xuất render không lỗi; `test-h09.cjs` 3/3 PASS, build OK (hiện chưa có field pre-compute live nào nên không E2E submit được).
- **H10. Tile proxy hỏng fallback** — `backend/src/routes/tiles.js:77-82`: không validate số z/x/y, `?url=` dùng nguyên (SSRF/open-proxy), lỗi trả JSON thay vì ảnh → `<img>` vỡ. Fix: validate số + whitelist host tile + trả ảnh lỗi 1x1.
  - ✅ **Đã fix 10/09/2026:** validate z (0-22) + x/y trong khoảng, xóa hẳn `?url=` (không chỗ nào dùng, đóng open-proxy; whitelist host + redirect đã có nay vẫn giữ), mọi lỗi trả PNG 1x1 + chỉ pipe khi upstream là `image/*`. **Test:** tile sai/vượt khoảng/`?url=` độc đều 200 image/png, map 24 tiles + 40 markers; `test-h10.cjs` 5/5 PASS, build OK (sandbox không tới OSM nên tile thật trả fallback — đúng thiết kế).
- **H11. Rút gọn link bản đồ treo/SSRF** — `mapController.js:16`: `fetch(url)` không timeout, gate duy nhất `includes('maps.app.goo.gl')`. Fix: AbortController + whitelist domain rút gọn.
  - ✅ **Đã fix 10/09/2026:** whitelist host chính xác (`maps.app.goo.gl`, `goo.gl`), chỉ http/https, input >2000 ký tự từ chối; `AbortController` timeout 8s; URL cuối phải thuộc Google Maps mới parse tọa độ. **Test:** link đầy đủ parse đúng, includes-trick/subdomain giả/file đều 400; FE form guest còn ô dán link; `test-h11.cjs` 4/4 PASS, build OK (link rút gọn thật không test được vì sandbox chặn mạng ngoài).
- **H12. Ảnh private lúc xem được lúc không** — `FileViewer` dùng URL static không auth, `FieldRenderer` dùng `/files/:id/image` có auth; `FileUpload` preview `<img>` không token. Fix: thống nhất qua endpoint auth-aware + object URL.
  - ✅ **Đã fix 10/09/2026:** P0-2 đã thống nhất endpoint (`/files/:id/download` fetch kèm auth → object URL; `<img>` dùng `?token=`); H12 verify lại và phát hiện thêm Helmet `Cross-Origin-Resource-Policy: same-origin` chặn `<img>` khác origin → set `cross-origin` cho image/download (vẫn check quyền), xóa proxy `/uploads` chết trong `vite.config.js`. **Test:** ma trận auth đủ 4 chiều + `<img>` có token render (width=1920), không token chặn; `test-h12.cjs` 6/6 PASS, build OK.
- **H13. PUT đề xuất của tôi thiếu validate** — `routes/myProposals.js:161` không validator (POST có `validateCreateProposal`) → SĐT sai, tọa độ rác vẫn lưu. Fix: gắn validator chung.
  - ✅ **Đã fix 10/09/2026:** `PUT /my-proposals/:id` gắn `validateUpdateProposal` (check SĐT/tọa độ/status khi có mặt; controller vẫn yêu cầu đủ trường + PENDING + ownership). **Test:** fixture CTV thật — SĐT sai/tọa độ rác → 400, hợp lệ → 200 (đã xóa fixture); FE my-proposals hiện fixture không lỗi; `test-h13.cjs` 6/6 PASS, build OK.
- **H14. Select nguồn Data List rỗng ở chỗ dùng hook** — `useFieldOptions.js:45-53` chỉ đọc `options` manual, bỏ qua `data_list_*`; backend cũng thiếu API lọc cascading theo parent. Fix: hook đọc data-list + API con theo parent.
  - ✅ **Đã fix 10/09/2026:** thêm `GET /api/data-lists/:id/children?column&parent_column&parent_value` (public, validate cột theo `columns_config`); `useFieldOptions.getSelectOptions` đọc data-list (manual ưu tiên) + helper `fetchChildOptions`; `dataListService.getChildren`. **Test:** children Hà Nội 126 xã, cột sai 400, thiếu params 400, list sai 404; FE proposals + guest form load không lỗi; `test-h14.cjs` 5/5 PASS, build OK.
- **H15. Ô Excel trống thành số 0** — `excelService.js:252-266` (`num||0` cho lat/lng/number) → trạm ra Null Island (0,0). Fix: giữ `null`, bắt buộc nhập khi thiếu.
  - ✅ **Đã fix 10/09/2026:** lat/lng trống → lỗi bắt buộc (cột DB NOT NULL); number fixed trống → `null` thay vì 0; số 0 hợp lệ vẫn giữ. **Test:** preview dòng trống báo thiếu Vĩ độ/Kinh độ, dòng hợp lệ giữ 10.5/106.7; FE import UI + không lỗi; `test-h15.cjs` 4/4 PASS, build OK.

## C. Đồng bộ/dữ liệu
- **H16. Confirm import không kiểm tra lại** — preview check trùng 200m + trùng mã nhưng `importConfirmDynamic` chỉ INSERT + formula → sửa body giữa 2 bước là bypass. Fix: re-validate + re-check trong transaction ở confirm.
  - ✅ **Đã fix 10/09/2026:** confirm re-validate tọa độ/SĐT/địa chỉ/email trong transaction + re-check trùng 200m (DB lẫn trong batch qua `haversineM`) với proposals, trùng `ma_tram` (DB + batch) với stations. **Test:** body sửa SĐT/tọa độ → 400 rollback (30→30), trùng tọa độ trạm #8 → 400, dòng sạch import 200 (đã xóa); `test-h16.cjs` 4/4 PASS, build OK.
- **H17. Lệch SEQ sau import Excel** — `excelService.js:716-733` regex `/^E\.([A-Z]+)(\d+)$/` khác `formulaService.reconcileSequences` (`/^(.*)(\d{4})$/` + strip `_`) → `last_number` lệch, mã mới có thể trùng. Fix: dùng chung 1 hàm reconcile.
  - ✅ **Đã fix 10/09/2026:** tách `formulaService.parseCodeToSeq` dùng chung cho `reconcileSequences` và khối cập nhật SEQ sau import (thay regex `E.` riêng lẻ). **Test:** parser chạy trong container (`E.HCM0007`→E.HCM/7, `HNO_0007`→HNO/7, mã ngắn/sai→null), reconcile 20 prefixes, sequences khớp max (`E.HNO=10`, `E.HCM=9`); `test-h17.cjs` 3/3 PASS, build OK.
- **H18. Cài fresh thiếu bảng là crash** — `proposal_sequences` chỉ tạo ở script 19, `submitter_ip` ở script 24, trong khi code dùng từ sớm (`getNextSequence`, upload file). Fix: gộp CREATE TABLE IF NOT EXISTS vào script 01/08.
  - ✅ **Đã fix 10/09/2026:** 01 thêm `proposal_sequences` (19 đã IF NOT EXISTS nên chạy tuần tự an toàn); 08 thêm `submitter_ip` vào files + 24 guard `INFORMATION_SCHEMA` để không trùng cột. **Test:** câu lệnh mới/guard chạy no-op trên DB hiện tại, guest upload lưu IP, `getNextSequence` tăng 1→2 (đã dọn prefix test + file test); `test-h18.cjs` 3/3 PASS, build OK.
- **H19. JWT 7 ngày không thu hồi** — `authService.js:40-42`, logout chỉ xóa localStorage → token lộ dùng được 7 ngày; hết hạn UI không tự logout (api.js không interceptor 401). Fix: access ngắn + refresh/blacklist (hoặc đổi pass là revoke hết), interceptor 401 → login.
  - ✅ **Đã fix 10/09/2026 (mức gọn nhẹ):** access còn 12h (`JWT_EXPIRES_IN`, mặc định 12h); thêm `users.token_version` (script 43 + cột trong 01) nhúng vào JWT — mọi đường đổi pass (tự đổi, admin đổi, admin update kèm pass) đều bump version khiến token cũ 401; `requireAuth` + file `canAccessFile` check version; FE `api.js` interceptor 401 (chỉ các call có auth) xóa token + về `/login`. **Test:** token exp-iat=43200 + version, đổi pass → token cũ 401 + login mới OK (đã restore), FE token rác về login + xóa token, session thật không ảnh hưởng; `test-h19.cjs` 4/4 PASS, build OK.

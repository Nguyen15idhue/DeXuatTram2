# 41.Lane A — Hướng dẫn Người dùng + Bản đồ (kết quả)

**Thuộc**: `docs/5/41.HuongDanSuDung_kehoach.md`
**Script**: `frontend/scripts/help/gen-images-user.mjs`
**Ảnh**: `frontend/public/help/user/`
**Status**: `docs/5/41.status.A.json`
**Trạng thái lane**: Đã chạy — 14/14 bước OK, 14 ảnh (633 KB)

---

## Kết quả chạy
- Chạy `node frontend/scripts/help/gen-images-user.mjs` → **14/14 OK**, 0 lỗi.
- Ảnh: `frontend/public/help/user/` (14 file JPEG, tổng ~633 KB).
- Kiểm tra hiển thị: tab "Người dùng" trong `/huong-dan` = 14 ảnh, 0 ảnh hỏng.
- Ghi chú: A13 (Guest) chụp khi đã đăng nhập vì `/de-xuat` gọi `/api/field-definitions` bị 401 → redirect `/login` với khách chưa đăng nhập (xem mục bug ở master).


> Mỗi bước có 5 mục: Nội dung · Yêu cầu cần đạt · Checklist test · Kết quả đã đạt · Ghi chú

---

### A01 — Đăng nhập
- **Nội dung**: chụp `/login` khoanh nút "Đăng nhập".
- **Yêu cầu cần đạt**: ảnh hiện form + khoanh nút; không lỗi console.
- **Checklist test**: [ ] nút được khoanh · [ ] ảnh < 300KB
- **Kết quả đã đạt**: [ ] (chưa)
- **Ghi chú**: —

### A02 — Đăng ký
- **Nội dung**: chụp `/register`.
- **Yêu cầu cần đạt**: hiện form đăng ký.
- **Checklist test**: [ ] ảnh load
- **Kết quả đã đạt**: [ ] (chưa)

### A03 — Hồ sơ cá nhân
- **Nội dung**: chụp `/profile`, khoanh vùng sửa/đổi MK.
- **Yêu cầu cần đạt**: hiện thông tin user + nút cập nhật.
- **Checklist test**: [ ] khoanh đúng nút
- **Kết quả đã đạt**: [ ] (chưa)

### A04 — Bản đồ: marker & chú thích
- **Nội dung**: chụp `/map` + legend.
- **Yêu cầu cần đạt**: tile hiển thị, legend rõ.
- **Checklist test**: [ ] không ảnh trắng · [ ] legend khoanh
- **Kết quả đã đạt**: [ ] (chưa)
- **Ghi chú**: chờ map `idle` + 1.5s.

### A05 — Bộ lọc bản đồ
- **Nội dung**: mở `MapFilterPanel`, khoanh chip/ô lọc.
- **Yêu cầu cần đạt**: panel mở, hiển thị bộ lọc.
- **Checklist test**: [ ] panel mở · [ ] khoanh vùng lọc
- **Kết quả đã đạt**: [ ] (chưa)

### A06 — Chuyển nền/mode
- **Nội dung**: mở "Chuyển layer" → khoanh "Nền bản đồ".
- **Yêu cầu cần đạt**: dropdown hiện các nền.
- **Checklist test**: [ ] menu mở
- **Kết quả đã đạt**: [ ] (chưa)

### A07 — Nhãn hành chính mới/cũ
- **Nội dung**: mở "Chuyển layer" → khoanh nhóm "Nhãn hành chính".
- **Yêu cầu cần đạt**: hiện Nhãn mới/Nhãn cũ/Tắt nhãn.
- **Checklist test**: [ ] đủ 3 mục
- **Kết quả đã đạt**: [ ] (chưa)

### A08 — Bật/tắt 3D
- **Nội dung**: khoanh nút 3D (map controls).
- **Yêu cầu cần đạt**: nút 3D hiện (renderer MapLibre).
- **Checklist test**: [ ] khoanh đúng · [ ] bản đồ nghiêng
- **Kết quả đã đạt**: [ ] (chưa)

### A09 — Tạo đề xuất: menu 3 cách toạ độ
- **Nội dung**: mở menu FAB "+", khoanh tiêu đề "Tạo đề xuất mới" + 3 lựa chọn.
- **Yêu cầu cần đạt**: menu mở, tiêu đề đậm.
- **Checklist test**: [ ] đủ 3 cách · [ ] tiêu đề đúng
- **Kết quả đã đạt**: [ ] (chưa)

### A10 — Form đề xuất (đã chọn toạ độ)
- **Nội dung**: chọn "Chọn trên bản đồ" → click map → Xác nhận → form.
- **Yêu cầu cần đạt**: form mở, toạ độ/địa chỉ điền.
- **Checklist test**: [ ] form mở · [ ] có toạ độ
- **Kết quả đã đạt**: [ ] (chưa)

### A11 — Tạo trạm nhanh (ADMIN+)
- **Nội dung**: khoanh FAB "Tạo trạm nhanh".
- **Yêu cầu cần đạt**: FAB hiện với SUPER/ADMIN.
- **Checklist test**: [ ] FAB hiện · [ ] khoanh đúng
- **Kết quả đã đạt**: [ ] (chưa)

### A12 — Đề xuất của tôi
- **Nội dung**: chụp `/my-proposals` (danh sách + mini map).
- **Yêu cầu cần đạt**: hiện danh sách.
- **Checklist test**: [ ] ảnh load
- **Kết quả đã đạt**: [ ] (chưa)

### A13 — Guest đề xuất + tra cứu
- **Nội dung**: chụp `/de-xuat`.
- **Yêu cầu cần đạt**: form guest hiện (không cần login).
- **Checklist test**: [ ] ảnh load
- **Kết quả đã đạt**: [ ] (chưa)

### A14 — Thông báo
- **Nội dung**: mở chuông thông báo, khoanh dropdown.
- **Yêu cầu cần đạt**: dropdown hiện.
- **Checklist test**: [ ] dropdown mở
- **Kết quả đã đạt**: [ ] (chưa)

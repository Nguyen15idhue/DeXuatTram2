# Kế hoạch 30 — Thêm type `user` & Mapping người dùng 1Office

> **Ngày:** 10/09/2026
> **Người lập:** Agent
> **Trạng thái:** ✅ Đã triển khai (Bước 1–8 + mục 9 auto gắn người dùng + mục 10 đính chính `user_ids` nhận code/tên).
> **Liên quan:** `docs/5/27.TichHopAPI1Office_*`, `docs/5/29.RedesignFieldMapping_*`, `backend/src/services/fieldMapper.js`, `backend/src/services/syncService.js`
> **Ký hiệu:** ⬜ Chưa làm · 🔄 Đang làm · ✅ Xong · ⚠️ Cần xem lại

---

## 1. Bối cảnh & mục tiêu

Hệ thống đề xuất trạm cần gửi **người phụ trách / người giao phụ trách** sang 1Office. Mỗi bên có danh sách người dùng riêng với **không gian ID riêng** (hệ thống này dùng `users.id`; 1Office dùng ID của nó). Hiện chưa có loại trường "người" nên chưa map được.

**Mục tiêu:**
1. Thêm loại trường **`user`** vào hệ thống dynamic (hiển thị tên, lưu id nội bộ, ấn xem chi tiết user).
2. Lưu **ID hệ ngoài** tập trung bằng bảng `user_external_map` để map sang nhiều nền tảng.
3. Map 2 trường 1Office: `user_ids` (Phụ trách) và `manager_user_ids` (Người giao phụ trách).
4. Đảm bảo dùng **ID** cả hai chiều (không phụ thuộc tên).

---

## 2. Quyết định đã chốt

| # | Hạng mục | Chốt |
|---|---|---|
| 1 | Nguồn chuẩn id hệ ngoài | **Bảng `users`** (qua bảng map `user_external_map`) |
| 2 | Lưu đa hệ | **Bảng `user_external_map`** |
| 3 | Đa người (`userlist`) | **Không cần** — mỗi field = 1 người |
| 4 | Quyền xem chi tiết user | **CTV KHÔNG** xem được (ẩn click) |
| 5 | Label khi map | **Không dùng** — map theo ID; đổi tên không ảnh hưởng |

---

## 3. Phát hiện kỹ thuật (đã test)

### 3.1. Lấy raw ID từ 1Office — tham số `field_raws`
```
GET /api/customer/contact/item?ID=191&access_token=<token>&field_raws=user_ids,manager_user_ids
```
| Field | Không field_raws | Có field_raws |
|---|---|---|
| user_ids | `"Nguyễn Trung Dũng"` | **`4`** |
| manager_user_ids | `"Nguyễn Trung Dũng"` | **`4`** |
| created_by_id | `"Admin"` | **`1`** |
| department_id | `"Giám đốc"` | **`7`** |
| formal_name | `"Ông"` | **`1`** |
| scale_id | `"25 - 50 nhân viên"` | **`3`** |
| type | `"Cá nhân"` | **`0`** |
| district_id | `"Quận 1"` | **`9819`** |
| state_id | `"Thành phố Hồ Chí Minh"` | **`9818`** |

→ **Kết luận:** dùng được ID cả 2 chiều, KHÔNG cần khớp tên.

### 3.2. Lấy danh sách user 1Office
```
GET /api/admin/user/gets?access_token=<admin_token>&limit=100&page=<n>
```
- Token **admin** (token contact trong `api_configs` không đủ quyền).
- Trả `total_item` + `data[]` (`ID`, `fullname`, `username`, `group_id`, `department_id`, `email`, `status`).
- `limit` tối đa 100; phân trang bằng `page`. `group_id` **không lọc** (7/1/4 đều trả đủ).
- Danh sách hiện tại: **12 user** (1=Admin, 4=Nguyễn Trung Dũng, 7=Đỗ Mai Hương, 10=Nam Thanh, 13=Thưởng, 16=Nhật, 19=Cường Trần, 22=Đỗ Văn Nguyên, 25=Linh, 28=Kim Chi, 31=Phương, 34=Nguyễn Hải Nguyên).

---

## 4. Thiết kế

### 4.1. Giá trị field `user` lưu trong `custom_data`
```json
{ "id": 12 }
```
- `id` = `users.id` (hệ thống này).
- Tên hiển thị: tra `users.full_name` theo `id`.
- ID hệ ngoài: tra `user_external_map(user_id, system)`.

### 4.2. DB

**Bảng mới `user_external_map`:**
```sql
CREATE TABLE IF NOT EXISTS user_external_map (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  system VARCHAR(50) NOT NULL,
  external_id VARCHAR(100) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uq_user_system (user_id, system),
  UNIQUE KEY uq_system_external (system, external_id),
  KEY idx_user (user_id),
  CONSTRAINT fk_uem_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Thêm cột `system_key` vào `api_configs`** (định danh hệ: `1office`, `crmB`...).

### 4.3. Mapping push/pull
- **Push** (đề xuất → 1Office): `users.id` → `user_external_map(system)` (ID 1Office) → quy đổi sang **code/tên** → gửi vào `user_ids`/`manager_user_ids` (xem mục 10).
- **Pull** (1Office → đề xuất): `field_raws` trả id → tra ngược `user_external_map` → lưu `{ "id": users.id }`.
- Hệ (`system`) lấy từ `api_configs.system_key`.

### 4.4. Hiển thị & click
- Component `UserField`: chip **tên** + click mở `RecordDetailPopup entity="users"`.
- **CTV:** chỉ hiện tên, ẩn click.
- Backend `mergeData` resolve `label` để frontend không phải gọi từng user.

### 4.5. Nơi cấu hình trên giao diện (3 chỗ tách biệt)

| # | Nơi | Việc | Trạng thái |
|---|---|---|---|
| 1 | `/admin/users` → mục "Liên kết hệ thống" | Gán ID hệ ngoài cho từng người (`user_external_map`) | Chưa có — Bước 3/6 |
| 2 | `/admin/fields` | Định nghĩa field type `user` + `source_config.systems` | Chưa có — Bước 2/6 |
| 3 | `/admin/api-configs` → FieldMappingPanel (kéo-thả) | Gán `source_field` (user) → `target_field` (`user_ids`/`manager_user_ids`) | Đã có kéo-thả |

**Quan trọng:** kéo-thả ở `/admin/api-configs` chỉ lưu **mối quan hệ** nguồn→đích, không lưu id. Quy đổi sang id ngoài xảy ra **lúc bấm Push** (`syncService` → tra `user_external_map` theo `api_configs.system_key`). Nếu user chưa được gán mapping → bỏ qua + cảnh báo (không gửi sai, không chặn cả đề xuất).

**Điều kiện để "tự nhận" id:**
1. Đích 1Office là **type `user`** (`user_ids`, `manager_user_ids`).
2. `api_configs.system_key = '1office'` (biết đang map hệ nào).
3. Mỗi người đã có bản ghi trong `user_external_map`.

**Hiện trạng:** field `user` và UI gán ID hệ ngoài chưa tồn tại; toàn bộ `users.external_id` đang NULL → nếu kéo mapping bây giờ sẽ gửi **id nội bộ** (sai). Cần làm Bước 1–6.

---

## 5. Các bước thực hiện

### Bước 1 — DB: bảng map + system_key

**Thông tin:** SQL script thủ công theo quy ước dự án (không migration tool).

**Yêu cầu cần đạt:**
- Có `user_external_map` (unique user+system, unique system+external, FK users).
- Có `api_configs.system_key` (default `'1office'`).

**Các bước chi tiết:**
1. Tạo `database/45-create-user-external-map.sql`.
2. Tạo `database/46-add-system-key-api-configs.sql` (ALTER, idempotent bằng information_schema).
3. Chạy trên DB dev, ghi kết quả.

**Checklist test:**
- [x] `DESCRIBE user_external_map` đúng cột/index/FK.
- [x] `SHOW COLUMNS FROM api_configs LIKE 'system_key'`.
- [x] Insert thử 1 mapping, xóa user → cascade xóa mapping.

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 10/09/2026 | ✅ PASS | Tạo `database/45-create-user-external-map.sql` + `database/46-add-system-key-api-configs.sql`, chạy 2 lần trên DB dev không lỗi (idempotent). `DESCRIBE` đủ cột/index/FK. `system_key` có default `'1office'`. |
| 10/09/2026 | ✅ PASS | Gán map (user tạm, `1office/99999`) → đếm = 1. Chèn trùng (`1/1office`) → bị chặn `Duplicate entry '1-1office' (uq_user_system)`. Xóa user tạm → map còn 0 (cascade đúng). Bảng `user_external_map` và user tạm đã dọn sạch. |

**Ghi chú:** Không DROP bảng cũ. Idempotent (chạy lại không lỗi).
- **Phát hiện & đã fix:** cột tên `system` là từ khóa dự trữ MySQL 8 → phải dùng backtick `` `system` `` trong script 45, và mọi query sau này liên quan cột này cũng phải dùng backtick.

---

### Bước 2 — Backend: type `user` (lõi dynamic)

**Thông tin:** Thêm type `user` vào hệ dynamic (không ảnh hưởng type cũ).

**Yêu cầu cần đạt:**
- `ALLOWED_TYPES` có `user`; `fieldMapper` biết xử lý.
- `splitData/mergeData` lưu `{id}` vào `custom_data` và trả kèm `label` khi đọc.
- Validate: id tồn tại trong `users`; rỗng được phép (không bắt buộc) trừ khi field `required`.

**Các bước chi tiết:**
1. `fieldMapper.js`: thêm `'user'` vào `ALLOWED_TYPES`.
2. `dynamicUtils.js`: `splitData` (đưa vào json), `mergeData` (resolve `label` từ users), `validateField`.
3. `validators.js`: `validateDynamicFields` xử lý `user`.
4. `dynamicEngineService.js`: field `user` khi tính post-formula trả **label** (không trả id).

**Checklist test:**
- [x] Tạo field type `user` qua API `/api/field-definitions`.
- [x] Tạo/sửa proposal có field user → `custom_data` lưu `{"id":...}`.
- [x] Đọc proposal trả thêm `label` đúng.
- [x] Id không tồn tại → lỗi validate.
- [x] Các type cũ vẫn hoạt động (text/select/file/formula).

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Thêm `'user'` vào `ALLOWED_TYPES` (`fieldMapper`, `fieldMappingService`) + `fieldDefinitionController` (create/update). Tạo field test id 160 type `user` thành công. |
| 11/09/2026 | ✅ PASS | Validate: `{id:1}` đạt (không lỗi user); `{id:99999}` → "không tồn tại"; `"abc"` → "không hợp lệ". |
| 11/09/2026 | ✅ PASS | Tạo proposal 428 có `utest_user={id:1}` → `custom_data` lưu đúng. Đọc chi tiết → `utest_user={id:1,label:"Admin"}`. Post-formula cũ (`ma_de_xuat`) vẫn tính đúng. |
| 11/09/2026 | ✅ PASS | Type cũ: validate text OK, bản đồ load, list proposals OK. Frontend `test-deploy-step2-user.cjs` **3/3** (trang fields hiện field, map OK, không lỗi console). Proposal 428 + field 160 đã xóa sạch. |

**Ghi chú:** Không đổi key/giá trị các field cũ. Field `fixed` giữ nguyên.
- Bổ sung enrich label ở `getProposalFullById`, `getProposalWithUser`, `myProposal.getProposalById` để trang chi tiết hiển thị tên.

---

### Bước 3 — Backend: quản lý `user_external_map` + lấy user 1Office

**Thông tin:** API để gán id hệ ngoài cho user và đồng bộ danh sách user ngoài.

**Yêu cầu cần đạt:**
- CRUD mapping theo user (thêm/sửa/xóa id hệ ngoài).
- API lấy danh sách user 1Office (phân trang) dùng token admin.
- Phân quyền: chỉ SUPER_ADMIN/ADMIN cấu hình mapping.

**Các bước chi tiết:**
1. `adminUserService`: `getExternalMappings(userId)`, `setExternalMapping(userId, system, externalId)`, `deleteExternalMapping`.
2. Controller/route (gắn `/api/admin/users/:id/external`).
3. `oneOfficeService.getUsers(apiConfigId)` — gọi `/api/admin/user/gets` (phân trang), token admin.
4. Lưu ý token admin: cần cấu hình riêng (api_config hoặc field trong auth_config).

**Checklist test:**
- [x] Gán id 1Office cho 1 user → lưu đúng.
- [x] Unique (user,system) chặn trùng.
- [x] Gọi list user 1Office trả 12 user.
- [x] CTV/SALES không gọi được API cấu hình mapping.

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Thêm API `/api/admin/users/:id/external` (GET/PUT/DELETE, `requireAdmin`) + service CRUD `user_external_map`. Gán user 1 → `1office/1` lưu đúng; GET trả đúng. |
| 11/09/2026 | ✅ PASS | Unique chặn trùng: PUT user 6 với `1office/1` → **400** "External ID đã được gán cho user khác". SALES và CTV gọi GET → **403**. |
| 11/09/2026 | ✅ PASS | Thêm `oneOfficeService.getUsers` (token admin, phân trang) + route `GET /api/admin/api-configs/:id/1office-users`. Test bằng config tạm có `admin_token` → trả **12 user**, rồi xóa config tạm. |
| 11/09/2026 | ✅ PASS | Frontend `test-deploy-step3-user.cjs` **3/3** (trang users hiện bảng, có admin, không lỗi console). Mapping test + config tạm đã xóa sạch. |

**Ghi chú:** Token admin khác token contact — cần bổ sung cấu hình.
- **Phát hiện & đã fix:** `INSERT ... ON DUPLICATE KEY UPDATE` cập nhật nhầm sang user khác khi trùng `external_id` → đã thêm kiểm tra trùng **trước** khi upsert.

---

### Bước 4 — Backend: transform mapping 1Office (`user_ids`/`manager_user_ids`)

**Thông tin:** Đưa ID vào `transformPush`/`transformPull` và dùng `field_raws` khi GET.

**Yêu cầu cần đạt:**
- Push: `{id}` → tra map(`system`) → gửi id; thiếu map → bỏ qua + cảnh báo.
- Pull: GET kèm `field_raws` → nhận id → tra map ngược → `{id}`.
- `target_field_type = 'user'` cho `user_ids`/`manager_user_ids`.

**Các bước chi tiết:**
1. `fieldMapper.js`: nhánh `user` trong `transformPush`/`transformPull` (async tra DB).
2. `syncService.js`: truyền `system` (từ `api_config.system_key`) vào transform.
3. `oneOfficeService.getContactDetail`/`getContacts`: thêm tham số `field_raws` (cấu hình danh sách field).
4. `syncService.pull...`: xử lý id trả về.

**Checklist test:**
- [x] Push proposal có user (map = 4) → `contactData.user_ids = "4"`.
- [x] User không có map → bỏ qua, có log cảnh báo, không lỗi.
- [x] Pull contact 191 kèm `field_raws` → `{id}` đúng (4 ↔ Nguyễn Trung Dũng).
- [x] Pull contact tạo mới ở 1Office, user không map → không phá dữ liệu.

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Chuyển `transformPush/Pull` sang async, thêm `system`; `syncService` truyền `system_key`; `oneOfficeService` hỗ trợ `field_raws`; preview await. Đổi catalog `user_ids/manager_user_ids` sang type `user`. |
| 11/09/2026 | ✅ PASS | Preview (mapping tạm user1→1, user6→4): push `{id:1}`→`"1"`, `{id:6}`→`"4"`; pull `"1"`→`{id:1}`, `"4"`→`{id:6}`. Push user 3 chưa map → `null` (bỏ qua). Mapping tạm đã xóa sạch. |
| 11/09/2026 | ✅ PASS | Frontend `test-deploy-step4-user.cjs` **2/2** (trang API configs tải được, không lỗi console). |
| | ⏳ Ghi chú | Test ở mức transform (preview) để **không ghi lên 1Office thật**. Push/pull thật sang 1Office để ở Bước 8 (cần xác nhận ghi). |

**Ghi chú:** `fieldMapper` hiện là hàm sync; cần chuyển sang async hoặc tra map trước khi gọi.
- Đã chuyển sang async; callers (`syncService` push/pull, preview) đã `await`.

---

### Bước 5 — Thêm field + mapping cho đề xuất

**Thông tin:** Tạo 2 trường người và gắn mapping.

**Yêu cầu cần đạt:**
- Có field `nguoi_phu_trach`, `nguoi_giao_phu_trach` (type `user`) trên `station_proposals`.
- Có trong form đề xuất + view.
- Có mapping `nguoi_phu_trach → user_ids`, `nguoi_giao_phu_trach → manager_user_ids`.

**Các bước chi tiết:**
1. Tạo 2 field definition (type `user`).
2. Thêm vào form tạo đề xuất + view 8.
3. Thêm 2 api_field_mappings (config 1Office).

**Checklist test:**
- [x] Form đề xuất hiển thị 2 trường, chọn được người.
- [x] Lưu proposal → `custom_data` có `{id}`.
- [x] Push → app gửi **code/tên** nhân sự vào `user_ids`/`manager_user_ids` (quy đổi từ ID ngoài; xem mục 10).

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Tạo field 161 (`nguoi_phu_trach`), 162 (`nguoi_giao_phu_trach`) type `user`. Thêm vào form + view 8. Tạo mapping 40, 41 (`push`, type `user`). |
| 11/09/2026 | ✅ PASS | Tạo proposal 429 có 2 field user → lưu đúng; đọc chi tiết trả label đúng; đã xóa proposal test. |
| 11/09/2026 | ✅ PASS | Frontend `test-deploy-step5-user.cjs` **3/3** (form `/de-xuat` hiện 2 label, không lỗi console). Chọn người bằng UI đẹp để ở Bước 6. |

**Ghi chú:** Tránh trùng/xung đột với `nguoi_de_xuat`/`sales_quan_ly` (đang là formula).
- **Phát hiện:** form tạo thật là **form 13** (`purpose='create'`), không phải form 9 (form 9 `purpose='all'`, chỉ là fallback). Đã thêm 2 field vào **cả form 9 và form 13**; view 8 đúng (chỉ có 1 view).

---

### Bước 6 — Frontend: hiển thị + click xem user (gated CTV)

**Thông tin:** Component chọn/hiển thị người dùng.

**Yêu cầu cần đạt:**
- `UserField`: chip tên; click mở chi tiết user (ADMIN/SUPER_ADMIN/SALES); CTV chỉ xem tên.
- `DynamicField`/`FieldRenderer` render type `user`.
- `FieldManager` cho chọn type `user`.
- `/admin/users`: mục nhập **ID hệ ngoài**.

**Các bước chi tiết:**
1. Component `UserField` (dùng `RecordDetailPopup entity="users"`).
2. Tích hợp vào `FieldRenderer`/`DynamicField`.
3. `FieldManager`: thêm option type `user`.
4. `AdminUsersPage`/popup: phần "ID hệ ngoài" (theo system).

**Checklist test:**
- [x] Chọn người trong form → hiển thị tên, lưu id.
- [x] ADMIN click → mở chi tiết user đúng.
- [x] CTV click → không mở (hoặc ẩn).
- [x] Lọc/sắp xếp theo tên hoạt động.
- [x] Không lỗi console.

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Tạo `UserField.jsx` (select từ `/api/admin/users/options/all`), `UserChip.jsx` (chip + popup, gated role), thêm `case 'user'` vào `DynamicField`/`FieldRenderer`, thêm `'user'` vào `FieldManager`, thêm `UserExternalPanel` vào popup user. Build frontend OK. |
| 11/09/2026 | ✅ PASS | Thêm `GET /api/admin/users/options/all` (mọi role đã login). `test-deploy-step6-user.cjs` **4/4** (2 select, có options, chọn được, không lỗi). |
| 11/09/2026 | ✅ PASS | `test-deploy-step6-click.cjs` **6/6**: ADMIN thấy chip dạng button, click mở chi tiết user; CTV chỉ thấy text, không button; không lỗi. |
| 11/09/2026 | ✅ PASS | `test-deploy-step6-filter.cjs` **5/5**: lọc theo tên (10→4 dòng), sắp xếp OK. Đã bật `filterable` cho 2 cột user + hỗ trợ label trong `DynamicTable`. Panel "Liên kết hệ thống" lưu/xóa qua UI OK. Đề xuất test đã xóa sạch. |

**Ghi chú:** `GET /api/admin/users/:id` yêu cầu quyền ADMIN/SUPER_ADMIN/SALES → CTV phải ẩn click.
- `UserChip` dùng `RecordDetailPopup entity="users" viewId=7`; CTV gated bằng `useAuth`.

---

### Bước 7 — Excel + hoàn thiện

**Thông tin:** Đồng bộ type `user` với import/export, filter.

**Yêu cầu cần đạt:**
- Export: hiển thị **tên** (label).
- Import: chấp nhận tên hoặc id; resolve về id.
- Filter/sort theo tên.

**Các bước chi tiết:**
1. `excelService`: xử lý type `user` (buildExportColumns, exportRowToValues, import).
2. `DynamicFilter`: options người dùng.

**Checklist test:**
- [x] Export có tên người đúng.
- [x] Import file có cột người → lưu id đúng.
- [x] Filter theo người hoạt động.

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Export proposals: header có 2 cột user, ô giá trị là tên ("Admin"). `exportRowToValues` + `getUserLabelMap` trả label. |
| 11/09/2026 | ✅ PASS | Import preview: tên "Admin" resolve thành `{id}` (trúng id 7 do **trùng tên** 3 người tên Admin — xem ghi chú); id số "30" resolve đúng `{id:30}`. Valid row, không lỗi user. Proposal test đã xóa. |
| 11/09/2026 | ✅ PASS | Filter/sort theo tên qua `DynamicTable` (đã test ở Bước 6, 5/5). Component `DynamicFilter.jsx` hiện **không được dùng** nên không cần sửa. |

**Ghi chú:** Import bằng tên cần quy tắc khớp (ưu tiên id, fallback tên).
- Tên trùng nhau sẽ trúng người đầu/cuối theo thứ tự DB → nên import bằng **id** cho chắc, hoặc đảm bảo tên duy nhất.

---

### Bước 8 — Test End-to-End + báo cáo

**Thông tin:** Kiểm thử tổng thể trên stack dev.

**Yêu cầu cần đạt:** Toàn bộ luồng hoạt động, không phá chức năng cũ.

**Checklist test:**
- [x] Tạo đề xuất có người phụ trách (map id 4) → Push 1Office → GET `field_raws=user_ids` trả `4`.
- [x] Pull contact 191 → proposal lưu `{id}` đúng.
- [x] CTV không xem chi tiết user.
- [x] Các type cũ + form/view cũ không lỗi.
- [x] Build frontend không lỗi; backend start OK.

**Kết quả sau test:**
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | E2E transform với mapping thật (user1→1, user6→4): push `{id:6}`→`"4"`; pull `"4"` (giá trị `field_raws` của record 191) → `{id:6}`. Đề xuất test + mapping đã xóa sạch. |
| 11/09/2026 | ✅ PASS | CTV gọi `GET /api/admin/users/:id` → **403**; UI CTV không có nút xem. Type cũ: stations/proposals list OK; build frontend OK; backend healthy. |
| 11/09/2026 | ✅ PASS | **Push thật lên 1Office**: đề xuất 436 (`TDT_HCM_0007`, mô hình `TDT`, đầy đủ trường) → job **89** completed; contact 1Office **ID 290** (`sync_status=synced`). Queue log xác nhận app gửi `contact_data.user_ids="1"`, `manager_user_ids="1"` (đã quy đổi `users.id` 1 → external 1 qua `user_external_map`). **Contact test giữ lại, không xóa.** |
| 11/09/2026 | ❌ Ban đầu sai | GET `field_raws=user_ids` của contact 290 ban đầu trả **4** dù gửi `"1"`. Ban đầu tưởng 1Office override. **SAI** — xem mục 10: `user_ids` nhận **code/tên**, không nhận ID; giá trị `"1"` bị hiểu là mã nhân sự "1" (→ ID 4). |
| 11/09/2026 | ✅ PASS (fix) | Sửa `transformUserPush` quy đổi external ID → **code/tên** (lấy từ danh sách user 1Office, cần `admin_token`). Push lại 436: gửi `user_ids="Admin"` → 1Office lưu **ID 1** đúng. Test code: map user 1 → ID 22 (code "4") → gửi `"4"` → 1Office lưu **22** đúng. |
| 11/09/2026 | ✅ PASS | Fix `desc`: field type `user` (và mọi giá trị `{id}`/`{id,label}`) render thành label/`User #id` thay vì `[object Object]` (`templateService.formatFieldValue`). Push lại 436 → 1Office desc hết `[object Object]` (`User #1`). |
| 11/09/2026 | ✅ PASS | Theo yêu cầu: revert field 107 `nguoi_de_xuat` + 110 `sales_quan_ly` về `formula` (giữ label + formula_config cũ); field 161 `nguoi_phu_trach` + 162 `nguoi_giao_phu_trach` vẫn type `user`. Build frontend OK (10.2s). |

**Ghi chú:** Cần token admin + quyền ghi 1Office để test push thật.
- **Kết luận quan trọng (đã đính chính):** `user_ids`/`manager_user_ids` của 1Office nhận **chuỗi mã nhân sự (code) hoặc tên**, phân cách bởi dấu phẩy — **KHÔNG nhận ID số**. Vì vậy push phải quy đổi `user_external_map.external_id` (ID 1Office) → `code`/`fullname` trước khi gửi. Chi tiết ở mục 10.

---

## 6. Rủi ro & lưu ý

1. **Token admin** cho API user khác token contact → cần cấu hình riêng.
2. **1Office GET mặc định trả tên** → bắt buộc dùng `field_raws` khi pull.
3. **Trùng tên** không ảnh hưởng (map theo id); nhưng nếu tương lai cần fallback tên thì phải xử lý.
4. **User thiếu map** → push bỏ qua + cảnh báo (không chặn toàn bộ).
5. **User bị xóa** → FK ON DELETE CASCADE xóa mapping; field `{id}` trong custom_data thành "mồ côi" → UI hiển thị "(không tồn tại)".
6. **Quyền**: CTV không xem chi tiết user.
7. **Đa hệ**: mỗi system một bản ghi map; thêm hệ mới chỉ cần thêm `system_key` + mapping.
8. **⚠️ 3 loại ID nhân sự 1Office**: `ID` (contact), `personnel_id` ("ID Hồ sơ nhân sự"), `code` ("Mã NS"). `user_ids` nhận **code/tên**; `user_external_map.external_id` lưu **`personnel_id`**; push quy đổi `personnel_id`→`code`/`fullname`, pull quy đổi `ID`→`personnel_id`. Cần **admin_token**. Xem mục 10.
9. **Field `user` trong Desc Template**: nếu giá trị lưu là object `{id}` (kể cả khi field bị đổi type), phải render label thay vì `[object Object]` — đã xử lý trong `templateService.formatFieldValue`.

---

## 7. Definition of Done

- [ ] Bảng `user_external_map` + `api_configs.system_key` tồn tại, idempotent.
- [ ] Type `user` chạy đủ: tạo/sửa/đọc/validate/hiển thị.
- [ ] Map 2 trường 1Office đúng id (push + pull dùng `field_raws`).
- [ ] CTV không xem chi tiết user.
- [ ] Type cũ + form/view cũ không bị ảnh hưởng.
- [ ] Build frontend OK, backend start OK, không lỗi console.
- [ ] Tài liệu cập nhật (docs/5/30 + `AGENTS.md` nếu cần).

---

## 8. Nhật ký test tổng hợp

| Ngày | Người test | Bước | Kết quả | Ghi chú |
|---|---|---|---|---|
| 10/09/2026 | Agent | Khảo sát `field_raws` | ✅ | `field_raws=user_ids` trả id `4` |
| 10/09/2026 | Agent | Lấy list user 1Office | ✅ | 12 user, token admin |
| 11/09/2026 | Agent | Bước 1 → 7 | ✅ | Xem bảng kết quả từng bước |
| 11/09/2026 | Agent | Bước 8 — push thật | ✅ | Contact 290 tạo OK. Ban đầu tưởng 1Office override `user_ids`; thực ra 1Office nhận code/tên (mục 10). Desc fix OK. |
| 11/09/2026 | Agent | Bước 8 — revert 107/110 | ✅ | 2 field về `formula`; 161/162 giữ `user`. |
| 11/09/2026 | Agent | Bổ sung — auto gắn người dùng | ✅ | Thêm `source_config.auto_user`; 4 field auto-fill + khóa readonly. |
| 11/09/2026 | Agent | Đính chính `user_ids` | ✅ | 1Office nhận **code/tên**, không nhận ID. Fix `transformUserPush` quy đổi ID→code; push thật contact 290 lưu đúng (mục 10). |

---

## 9. Bổ sung — Tự động gắn người dùng vào field `user`

> **Mục tiêu:** field type `user` tự điền người (và lưu `{id}`) để có thể push sang 1Office mà không cần chọn tay.

### 9.1. Cấu hình
Mỗi field `user` có `source_config.auto_user`:

| Giá trị | Ý nghĩa |
|---|---|
| `none` | Chọn thủ công (mặc định) |
| `current_user` | Người đang đăng nhập (người tạo) |
| `parent_sales` | Sales quản lý = `users.parent_id` của người tạo (nếu không có → chính người tạo) |

Khi `auto_user != none`: field **tự điền + khóa (chỉ đọc)** trên form.

### 9.2. Xử lý 2 lớp
- **Frontend** (`DynamicForm`): prefill `{id}` theo `authUser.id` / `authUser.parent_id`, render `disabled`. `parent_id` được trả ở login + `/api/auth/me`.
- **Backend** (`dynamicUtils.applyAutoUserFields`): ghi đè giá trị trước khi lưu (chống sửa/tamper), áp dụng cho create (`proposalService.createProposal`), update admin (`adminProposalService.updateProposal`), update user (`myProposalService.updateProposal`). Dùng `user_id` của người tạo.

### 9.3. Áp dụng cho 4 field (station_proposals)

| Field | Key | auto_user |
|---|---|---|
| 107 | `nguoi_de_xuat` | `current_user` (đổi từ `formula` → `user`) |
| 110 | `sales_quan_ly` | `parent_sales` (đổi từ `formula` → `user`) |
| 161 | `nguoi_phu_trach` | `current_user` (đã map → `user_ids`) |
| 162 | `nguoi_giao_phu_trach` | `parent_sales` (đã map → `manager_user_ids`) |

### 9.4. Kết quả test
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Tạo proposal 437 (admin, không gửi field người) → `custom_data` lưu `{id:1}` cho cả 4 field (`parent_sales` không có cha → fallback chính mình). Đã xóa proposal test. |
| 11/09/2026 | ✅ PASS | Test `applyAutoUserFields` với user 35 (`parent_id=30`): `current_user`→35, `parent_sales`→30. Admin (id 1, không cha): →1. |
| 11/09/2026 | ✅ PASS | Form `/de-xuat`: 4 field hiện combobox **disabled**, chọn sẵn "Admin (SUPER_ADMIN)"; console 0 error. |
| 11/09/2026 | ✅ PASS | `/admin/fields` → sửa field 161: hiện mục "Cấu hình Người dùng" / "Tự động lấy người" = "Người đăng nhập (người tạo)". Build frontend OK. |

### 9.5. Files
- `backend/src/services/dynamicUtils.js` — `applyAutoUserFields`, `parseSourceConfig`
- `backend/src/services/proposalService.js`, `adminProposalService.js`, `myProposalService.js`
- `backend/src/services/authService.js`, `backend/src/controllers/authController.js` — trả `parent_id`
- `frontend/src/components/dynamic/DynamicForm.jsx` — prefill + readonly
- `frontend/src/components/admin/FieldManager.jsx` — UI cấu hình

---

## 10. Đính chính — 3 loại ID nhân sự 1Office & cách map đúng

> Trước đây kết luận sai là "1Office override `user_ids`". Thực tế app **gửi sai định dạng**, và `user_external_map` lưu sai loại ID.

### 10.1. Ba loại ID khác nhau (dễ nhầm)
Danh sách nhân sự 1Office (`/api/admin/user/gets`) trả 3 định danh:

| Tên | Field API | Trong UI 1Office | Ví dụ |
|---|---|---|---|
| ID liên hệ | `ID` | (ID hệ thống, dùng cho contact) | 34 |
| ID hồ sơ nhân sự | `personnel_id` | **"ID Hồ sơ nhân sự"** | 7 |
| Mã nhân sự | `code` | **"Mã NS"** | 2 |

Đối chiếu thực tế (đã lấy trực tiếp):
| ID (`ID`) | personnel_id | code (Mã NS) | fullname |
|---|---|---|---|
| 1 | 0 | (null) | Admin |
| 4 | 4 | 1 | Nguyễn Trung Dũng |
| 34 | 7 | 2 | Nguyễn Hải Nguyên |
| 19 | 10 | 3 | Cường Trần |
| 22 | 13 | 4 | Đỗ Văn Nguyên |
| 28 | 16 | 5 | Kim Chi |
| 31 | 19 | 6 | Phương |
| 25 | 22 | 7 | Linh |
| 7 | 25 | 8 | Đỗ Mai Hương |
| 10 | 28 | 9 | Nam Thanh |
| 13 | 31 | 10 | Thưởng |
| 16 | 34 | 11 | Nhật |

### 10.2. `user_ids`/`manager_user_ids` nhận CODE hoặc TÊN
Theo tài liệu 1Office: kiểu **`string comma`** — "1 chuỗi **mã** hoặc **tên** nhân sự được phân cách bởi dấu phẩy. Ví dụ: `'NV06,NV08,Nguyễn Văn C'`".

| Gửi `user_ids=` | 1Office lưu (`ID`) | Giải thích |
|---|---|---|
| `1` (code) | 4 | code "1" = Nguyễn Trung Dũng |
| `4` (code) | 22 | code "4" = Đỗ Văn Nguyên |
| `2` (code) | 34 | code "2" = Nguyễn Hải Nguyên |
| `Nguyễn Hải Nguyên` / `Nguyen Hai Nguyen` | 34 | nhận theo **tên** (có/không dấu) |
| `1,4` | 4,22 | nhận **nhiều**, phân cách phẩy |

**Kết luận:** push phải gửi **code** (ưu tiên) hoặc **fullname**, KHÔNG gửi ID số.

### 10.3. Quy ước `user_external_map.external_id` = `personnel_id`
Admin nhìn UI 1Office thấy cột **"ID Hồ sơ nhân sự"** (`personnel_id`) nên nhập giá trị đó. Ví dụ gán `CTV Branch Test → 7` (mong đợi Nguyễn Hải Nguyên, `personnel_id=7`).

- **Push**: `personnel_id` → `code` (ưu tiên) hoặc `fullname` → gửi.
- **Pull**: `field_raws=user_ids` trả **`ID`** (contact) → cần quy đổi ngược `ID` → `personnel_id` → tra `user_external_map`.

### 10.4. Fix
- `fieldMapper.getOneOfficeUserMaps(apiConfigId)` trả 2 map (cache 10 phút):
  - `valueByPersonnel`: `personnel_id` → `code`/`fullname` (cho push).
  - `personnelByApiId`: `ID` → `personnel_id` (cho pull).
- `transformUserPush(...)`: `user_external_map.external_id` (personnel_id) → gửi code/tên.
- `transformUserPull(...)`: `ID` từ 1Office → `personnel_id` → `findUserByExternal`.
- `syncService` (push + pull) và preview truyền `apiConfigId`.
- Nếu không lấy được danh sách (thiếu `admin_token`) hoặc personnel_id không có trong danh sách → **bỏ qua field** (không gửi sai).

### 10.5. Yêu cầu cấu hình
- `api_configs.auth_config` cần `admin_token` (token admin 1Office) để gọi `/api/admin/user/gets`. Dev đã thêm: `{"token":"<contact>","admin_token":"<admin>"}`.

### 10.6. Kết quả test
| Lần | Kết quả | Ghi chú |
|---|---|---|
| 11/09/2026 | ✅ PASS | Đề xuất 438 `NQ_HNO_0017` (CTV Branch Test → personnel_id **7**; Sales Test → personnel_id **4**). Push: gửi `user_ids="2"`, `manager_user_ids="1"` → 1Office contact 293 lưu **user_ids=34 (Nguyễn Hải Nguyên)**, **manager=4 (Nguyễn Trung Dũng)**. ĐÚNG như mong đợi. |
| 11/09/2026 | ✅ PASS | `GET /api/admin/api-configs/3/1office-users` trả 12 user (có `ID`, `personnel_id`, `code`). |
| 11/09/2026 | ✅ PASS | Pull `field_raws=user_ids` (trả `ID`) → `ID`→`personnel_id` → `findUserByExternal` → `{id}`. |
| 11/09/2026 | ✅ (UX) | `UserExternalPanel`: đổi nhãn "External ID" → **"ID hồ sơ nhân sự"** + ghi chú không dùng "Mã NS"/ID liên hệ. |

### 10.7. Files
- `backend/src/services/fieldMapper.js` — `getOneOfficeUserMaps`, `transformUserPush`, `transformUserPull`
- `backend/src/services/syncService.js` — truyền `apiConfigId` (push + pull)
- `backend/src/controllers/fieldMappingController.js` — preview truyền `api_config_id`
- `frontend/src/components/admin/UserExternalPanel.jsx` — nhãn "ID hồ sơ nhân sự"

# Kế hoạch: Form đề xuất không cần đăng nhập

> **Cập nhật 04/09/2026 theo `21.Checkloctrung_kehoach.md` (21 là bản mới nhất)**: endpoint guest dùng chung logic sinh `ma_de_xuat` + check trùng <200m của 21; giữ `tracking_code` riêng để khách tra cứu. Thứ tự làm: **21 trước → 19 sau** (21 tạo fields/DataList/SEQ, 19 tái dùng).
>
> **Cập nhật 05/09/2026 theo 21 (bản build), 22 (Cách 2 backend tự suy mã), 23 (mã trạm) + mã nguồn hiện tại**:
> - `SEQ()` trả số thô, format 4 số nằm trong expression `LPAD(..., 4, '0')` (script 21) — guest tái dùng nguyên.
> - Form guest **không cần ô `ma_tinh`**: chỉ cần ô Tỉnh + `mo_hinh_dau_tu`, server tự suy mã/vùng qua `applyDiaGioi` (file 22). Khách gửi mã sai vẫn bị ghi đè.
> - Check-nearby hiện **chạy ngay khi có lat/long** (cảnh báo sớm trong form) + chặn cuối lúc submit.
> - Preview formula đã gửi scope mẫu (fix lỗi không xem được kết quả); FieldManager có dropdown "Cột hiển thị".
> - Thứ tự làm cập nhật: **21 → 22 → 19** (23 độc lập, làm song song được).

## 1. Mục tiêu
- Khách vãng lai (chưa đăng nhập) vẫn điền và gửi form đề xuất trạm được.
- Giữ nguyên luồng user đã đăng nhập (`POST /api/proposals` + `requireAuth`).
- Chống spam là yêu cầu bắt buộc (form public).

## 2. Hiện trạng (ràng buộc)
- `POST /api/proposals` bắt `requireAuth` (`backend/src/routes/proposals.js:121`).
- `station_proposals.user_id INT NOT NULL` + FK về `users(id)` (`database/01-create-tables.sql:49-63`).
- Các query list dùng `JOIN users` (inner) trong `proposalService.js`, `adminProposalService.js` → row có `user_id = NULL` sẽ biến mất khỏi kết quả.
- `POST /api/dynamic/:entity/validate` yêu cầu JWT (`backend/src/routes/dynamicEngine.js:98`).
- `POST /api/files/upload` yêu cầu JWT.
- Frontend form đề xuất nằm trong `UserLayout` (tự redirect về `/login` khi chưa login).
- Rule AGENTS.md #4 "Proposal phải lưu user_id" sẽ phải sửa thành "user_id được NULL với guest".

## 3. Thiết kế

### 3.1 Database (script SQL thủ công, không DROP TABLE)
```sql
ALTER TABLE station_proposals MODIFY user_id INT NULL;
ALTER TABLE station_proposals ADD COLUMN submission_source ENUM('user','guest') NOT NULL DEFAULT 'user';
ALTER TABLE station_proposals ADD COLUMN tracking_code VARCHAR(20) NULL;
ALTER TABLE station_proposals ADD COLUMN submitter_ip VARCHAR(45) NULL;
CREATE INDEX idx_proposals_source ON station_proposals(submission_source);
CREATE UNIQUE INDEX uq_proposals_tracking ON station_proposals(tracking_code);
```
- `tracking_code`: mã tra cứu cho khách (VD `DX-XXXXXX`), sinh ở backend khi tạo. Giữ song song với `ma_de_xuat` của 21 (2 mã, 2 mục đích khác nhau — xem 3.2).
- Không cho khách sửa/xóa → không cần cơ chế chủ sở hữu cho guest.
- Thứ tự script SQL: chạy script của **21 (18: DataList, 19: fields, 20: cột label, 21: expression LPAD)** rồi mới chạy script của file này. Không cần script mới cho guest (tái dùng fields/sequence có sẵn).

### 3.2 Backend
1. **Endpoint mới** `POST /api/proposals/guest` (không `requireAuth`):
   - Dùng chung `validateCreateProposal` như endpoint cũ + bắt buộc `province` (ô Tỉnh) và `mo_hinh_dau_tu`; **`ma_tinh` không cần từ client** (server tự suy theo file 22).
   - `user_id = NULL`, `status = 'PENDING'`, sinh `tracking_code`, lưu `submitter_ip`.
   - Sinh `ma_de_xuat` **bằng đúng engine post của 21** (transaction, `SEQ()` trả số thô + `LPAD(..., 4, '0')` trong expression, prefix `{MaMoHinh}_{MaTinh}`, đếm chung với proposal của user login — không tách dãy số). Trước đó gọi `applyDiaGioi` (file 22) để suy mã/vùng, ghi đè giá trị client.
   - Thiếu/tên tỉnh lạ → 400 (rollback, không insert nửa vời) — cùng hành vi endpoint user.
   - 2 mã cùng tồn tại: `tracking_code` để **khách tra cứu** (public, không đoán được), `ma_de_xuat` để **quản trị/đối chiếu** (hiển thị admin/table).
   - Verify captcha server-side (Turnstile/reCAPTCHA) trước khi INSERT.
   - Rate limit riêng, chặt (VD 5 req/giờ/IP) — hiện route public chưa có limiter nào.
   - Gọi **check trùng <200m của 21 trước khi INSERT** (dùng biến thể public của `POST /api/proposals/check-nearby`, không yêu cầu JWT, có rate-limit + captcha như endpoint guest). Trùng → 400 + thông báo như user login.
   - Thêm `@swagger` JSDoc trong route file.
2. **Chuyển `JOIN users` → `LEFT JOIN`** trong:
   - `backend/src/services/proposalService.js` (`getAllProposals`, `getProposalById`, select sau create)
   - `backend/src/services/adminProposalService.js` (list + count đã sửa JOIN cho search)
   - Hiển thị `user_name = NULL` → "Khách" (table admin, popup map, export).
   - API duplicates/check-nearby của 21 (`proximityService.loadPoints`) query trực tiếp không JOIN nên row guest **tự động** được tính — không cần sửa, chỉ cần hiển thị "Khách"/mã khi render (pairs đã có `code`).
3. **Validate public**: mở `POST /api/dynamic/station_proposals/validate` cho phép không JWT (chỉ entity này), hoặc bỏ validate client, giữ validate backend.
4. **File**: bản guest **được upload** (user duyệt 05/09/2026) qua endpoint public riêng, giới hạn chặt:
   - Endpoint mới `POST /api/files/guest-upload` (không JWT): chỉ nhận ảnh + PDF/DOC (`images`, `documents`), mỗi file ≤ 5MB, tối đa **5 file/đề xuất**, **10 file/giờ/IP** (chung `guestLimiter`).
   - Trả về file id/token; lúc submit guest gửi kèm → backend verify (đúng IP upload, chưa gắn vào đề xuất khác) rồi mới gắn vào `custom_data`.
   - File upload xong mà không submit (mồ côi) → job dọn sau **24h** (chống flooding kho lưu trữ).
   - Thêm `@swagger` JSDoc; tuyệt đối không mở `POST /api/files/upload` cũ cho public.

### 3.3 Frontend
1. Trang public mới (VD `/de-xuat`), dùng `PublicLayout`:
   - Tái dùng `DynamicForm` (đúng rule map hiện tại về lat/long).
   - **Không nhúng bản đồ click chọn** (user duyệt 05/09/2026): 2 cách lấy vị trí là nút **"Vị trí của tôi"** (geolocation trình duyệt) và ô **dán link Google Maps** (parse trực tiếp + resolve link rút gọn qua `POST /api/map/resolve-map-url`, public sẵn).
   - Form guest gồm: các field cố định + `province` (ô Tỉnh) + `mo_hinh_dau_tu` + `ma_de_xuat` readonly "Tính sau khi lưu" (hiển thị, không nhập). **Không cần ô `ma_tinh`** (server tự suy theo file 22).
   - Gọi endpoint guest, không gửi token; thêm captcha widget + honeypot field.
   - Check-nearby **chạy ngay khi có lat/long** (cảnh báo sớm trong form như MapPage/modal hiện tại) + chặn cuối lúc submit (<200m → lỗi, không lưu).
   - Màn hình kết quả hiện **cả 2 mã**: `tracking_code` (để tra cứu) + `ma_de_xuat` (để đối chiếu).
2. Admin (`AdminProposalsPage`): cột người tạo hiển thị "Khách" khi `user_name` null; search hiện tại (`owner_name/address/user_name`) vẫn chạy với `LEFT JOIN`.

### 3.4 Chống spam
- Rate limit theo IP (bắt buộc): thêm `guestLimiter` (theo mẫu 3 limiter trong `app.js`): `POST /api/proposals/guest` **5 req/giờ/IP**, `GET /api/proposals/track/:code` **30 req/giờ/IP**; quá → **429** đúng format `{success, message}`. Key = `req.ip` (deploy sau proxy thì bật `trust proxy`). Mặc định lưu RAM (1 instance Docker thì đủ; scale nhiều backend thì chuyển Redis sau).
- Captcha bắt buộc (server verify): khuyến nghị Cloudflare Turnstile (thay được bằng reCAPTCHA v3). Form gửi kèm `captcha_token` → backend `fetch` verify **trước INSERT** → fail → **400, không insert**. Secret trong `.env`, không hardcode. Chỉ bắt ở submit; check-nearby public chỉ rate-limit.
- Validate phone VN: chuẩn hóa (bỏ space/chấm) rồi check `/^0(3|5|7|8|9)\d{8}$/` (siết hơn regex 10 số hiện tại); sai → 400 trước captcha.
- Giới hạn `description`: tối đa **2000 ký tự** backend (quá → 400); frontend `maxlength` + đếm live.
- Log IP: lưu `submitter_ip` (đã có ở 3.1), **không trả ra API public**; dùng điều tra chùm spam.
- Dedupe phone + tọa độ: cùng SĐT chuẩn hóa **và** điểm mới trong **200m** của đề xuất đã có (trừ REJECTED) → **400** kèm thông báo. Tái dùng `haversineM`; cân nhắc thêm index `owner_phone` khi làm.
- Honeypot: input ẩn CSS (không `type=hidden`) VD `website`; bot điền → **400 chung chung** (không lộ lý do).

### 3.5 Tra cứu bằng tracking code (public, không đăng nhập)

1. **Endpoint mới** `GET /api/proposals/track/:code` (không `requireAuth`):
   - Rate limit theo IP (VD 30 req/giờ/IP) — mã là bearer token, chống dò quét.
   - Sai mã → 404 chung chung (không tiết lộ mã nào tồn tại).
   - Trả đúng 1 proposal của mã đó, SĐT **che giữa** (VD `09***1234`).
   - Thêm `@swagger` JSDoc trong route file.
2. **Nội dung khách thấy**: `ma_de_xuat`, `tracking_code`, trạng thái (diễn giải tiếng Việt) + ngày gửi; mô hình (tiếng Việt), tỉnh/xã, địa chỉ, diện tích, loại đất, mô tả, tọa độ điểm của chính họ.
3. **Không thấy**: đề xuất của người khác, list/map tổng, thông tin user khác, ghi chú nội bộ admin, nút sửa/xóa.

### 3.6 Danh sách mã trên máy khách (localStorage)

- Submit guest thành công → push `{tracking_code, ma_de_xuat, created_at}` vào `localStorage` (key VD `guest_proposals`), tối đa 20-50 mã (cũ nhất tự rớt). Dùng `localStorage` (giữ sau khi đóng trình duyệt), **không** dùng `sessionStorage`.
- Chỉ lưu mã — chi tiết lấy từ API 3.5 mỗi lần mở (không lo dữ liệu cũ, không lộ thông tin cá nhân).
- Trang `/de-xuat` có khối "Đề xuất của bạn": list mã đã lưu → bấm xem chi tiết (3.5) → nút xóa mã khỏi list. Kèm ô nhập mã thủ công (tra mã của máy khác).
- Danh sách **theo máy/trình duyệt**: xóa cache/đổi máy là mất → màn hình kết quả sau submit vẫn hiện mã + nút copy, nhắc khách tự lưu.

## 4. Checklist test
- [ ] Guest gửi đề xuất thành công (chỉ gửi tỉnh + mô hình, không gửi mã), nhận `tracking_code`, `ma_de_xuat` đúng prefix, `status = PENDING`, `user_id = NULL`.
- [ ] Guest gửi `ma_tinh` sai → server sửa lại theo tỉnh; gửi tỉnh lạ/thiếu tỉnh → 400, không INSERT.
- [ ] `ma_de_xuat` của guest đếm chung dãy với user login (không trùng, không tách dãy).
- [ ] Chọn vị trí trên map → cảnh báo trùng hiện ngay khi <200m (chưa cần nhập form); submit vẫn bị chặn.
- [ ] Guest submit trùng vị trí (<200m) → bị chặn như 21 case 1.
- [ ] List admin/map vẫn hiện đề xuất guest (không bị mất do JOIN); duplicates vẫn bắt được row guest, hiển thị mã + "Khách".
- [ ] Quá rate limit → 429; captcha sai → 400, không INSERT.
- [ ] User đã login gửi như cũ, không ảnh hưởng.
- [ ] Upload file ở form guest: đúng loại + dung lượng thì gắn được vào đề xuất; sai loại/quá 5MB/quá 5 file → lỗi rõ ràng; file mồ côi quá 24h bị dọn.
- [ ] Tra cứu mã đúng → thấy chi tiết của đúng đề xuất đó (SĐT che giữa, đủ 2 mã); mã sai → 404.
- [ ] Submit xong mã tự lưu localStorage; mở lại `/de-xuat` thấy list "Đề xuất của bạn"; xóa cache → mất list nhưng nhập tay mã vẫn tra được.
- [ ] Swagger UI hiển thị endpoint mới.
- [ ] `npm run build` OK, backend khởi động không lỗi.

## 5. Ghi chú
- Không sửa `POST /api/proposals` cũ — thêm endpoint song song để dễ review/rollback.
- AGENTS.md rule #4 cần cập nhật sau khi làm ("Proposal phải lưu user_id" → "user_id được NULL với guest").
- Làm **sau 21 + 22**: tái dùng `SEQ()` + `LPAD` trong expression, `applyDiaGioi`, `haversineM`, DataList `dm_tinh`/`dm_xa`, field_definitions — không viết lại. 23 độc lập.

## 6. Dự kiến tương lai: landing page cho khách (chưa làm)

Trang `/de-xuat` khoác thêm "vỏ landing" ngoài form là thành landing page hoàn chỉnh (không cần tách frontend mới, API giữ nguyên):

| Section | Nội dung (cần user duyệt chữ/ảnh) |
|---|---|
| Hero | Tiêu đề + câu giá trị + CTA cuộn xuống form + banner trạm sạc |
| Lợi ích (3-4 thẻ) | Nhượng quyền / Tự đầu tư / Liên kết là gì, điều kiện mặt bằng tối thiểu |
| Quy trình 4 bước | Gửi đề xuất → Kiểm tra trùng → Khảo sát → Ký hợp tác (ánh xạ PENDING → REVIEWING → APPROVED) |
| Form đề xuất | Mục 3.3 ở trên (map + DynamicForm + captcha) |
| Tra cứu | Mục 3.5 + 3.6 (ô nhập mã + "Đề xuất của bạn") |
| FAQ (5-7 câu) | Diện tích tối thiểu? Chi phí? Thời gian phản hồi? — accordion |
| Footer | Hotline, email, địa chỉ công ty |

- Việc cần làm khi duyệt nội dung: (1) backend file này (endpoint guest + tra cứu + rate limit + captcha); (2) dựng 7 sections responsive (khách đa số dùng điện thoại); (3) checklist mục 4.
- Nâng cấp sau nữa (tùy chọn): web landing riêng tối ưu SEO → link/iframe tới `/de-xuat`, API không đổi.

# Form đề xuất không cần đăng nhập — Kết quả đã đạt

**Ngày tạo**: 05/09/2026
**File**: `docs/5/19.FormDeXuatKhongCanDangNhap_ketquadadat.md`
**Trạng thái**: Hoàn thành 05/09/2026 (chờ user check thủ công)
**Các bước**: `19.FormDeXuatKhongCanDangNhap_cacbuoccanlam.md`

---

## Bước 1 — Database

**Trạng thái**: Hoàn thành 05/09/2026

### Files đã tạo/sửa

| File | Thay đổi |
|---|---|
| `database/24-guest-proposals.sql` (mới) | `user_id` NULL + `submission_source`/`tracking_code`/`submitter_ip` + 3 index (`source`, `tracking` unique, `phone`) + `files.submitter_ip` |

### Kết quả test

- [x] `DESCRIBE` đủ cột mới đúng kiểu (script `database/24`, apply qua `docker cp`)
- [x] Row cũ đọc/ghi bình thường (list proposals + stations OK, count giữ nguyên)

### Kết quả đã đạt

- [x] Xong, không mất dữ liệu

---

## Bước 2 — Endpoint guest + tra cứu

**Trạng thái**: Hoàn thành 05/09/2026

### Files đã tạo/sửa

| File | Thay đổi |
|---|---|
| `backend/src/app.js` | Dùng `middlewares/rateLimits.js` (mới, tách từ app.js) + lịch dọn file mồ côi (startup + 24h) |
| `backend/src/middlewares/rateLimits.js` (mới) | `guestSubmitLimiter` 5/giờ, `guestUploadLimiter` 10/giờ, `guestTrackLimiter` 30/giờ |
| `backend/src/routes/proposals.js` | `POST /proposals/guest` + `POST /check-nearby-public` + `GET /track/:code` + Swagger |
| `backend/src/controllers/proposalController.js` | `createGuest` (map 400), `checkNearbyPublic`, `trackByCode` (404 chung) |
| `backend/src/services/proposalService.js` | `createGuestProposal` (honeypot → captcha → phone VN → desc 2000 → trùng 200m → dedupe phone 1000m → `applyDiaGioi` → verify file → 2 mã, transaction), `trackByCode` (SĐT che giữa, ẩn IP) |
| `backend/src/routes/files.js` | `POST /files/guest-upload` (5MB, ảnh/PDF/Word, lỗi 400 JSON) + `POST /files/cleanup-orphans` (admin) + Swagger |
| `backend/src/controllers/fileController.js` | `guestUpload`, `cleanupOrphans` |
| `backend/src/services/fileService.js` | `uploadFile` thêm `submitter_ip`; `cleanupOrphanGuestFiles` |
| `docker-compose.yml` | `CAPTCHA_ENABLED=false`, `TURNSTILE_SECRET_KEY`, `ORPHAN_FILE_TTL_HOURS=24` |

### Kết quả test

- [x] Submit đủ → 201 + `DX-7D3465` + `NQ_HNO_0001` (chung dãy user) + `PENDING` + `user_id NULL` + `guest`
- [x] Thiếu/tỉnh lạ → 400 + rollback; SĐT sai (`021...`) → 400; mô tả 2001 ký tự → 400; honeypot → 400 chung
- [x] Captcha bật + không token → 400 (tắt ở dev để test được)
- [x] Trùng vị trí (0m/100m) → 400; cùng SĐT 890m → 400 (bán kính phone 1000m, tách khỏi case 1 để có tác dụng độc lập); cùng SĐT 55km → 201
- [x] Quá 5 submit/giờ → 429 (test xong restart backend để xóa quota)
- [x] Tra cứu đúng → 200 + SĐT `09***0001` + đủ 2 mã, không lộ IP; sai → 404
- [x] Upload png → 201; exe/quá 5MB/6 file → 400
- [x] Dọn file (backdate -25h): mồ côi mất (DB + ổ đĩa), đã gắn còn, chạy 2 lần không lỗi; endpoint tay 200, không auth → 401
- [x] Swagger hiện 5 endpoint mới; `npm run build` OK, backend chạy ổn định

### Kết quả đã đạt

- [x] Xong toàn bộ Bước 2 theo checklist

### Ghi chú

- Rebuild image backend 1 lần (mất `mathjs` do exec-install cũ khi recreate — bài học: cài package phải rebuild image).
- `node --watch` thỉnh thoảng miss reload → restart container sau loạt sửa backend.
- Test API tiếng Việt bằng python (PowerShell encode sai UTF-8 — lỗi tool test, không phải server).

---

## Bước 3 — LEFT JOIN + hiển thị "Khách"

**Trạng thái**: Hoàn thành 05/09/2026

### Files đã tạo/sửa

| File | Thay đổi |
|---|---|
| `backend/src/services/proposalService.js` | `JOIN users` → `LEFT JOIN` (list/byId/sau create) |
| `backend/src/services/adminProposalService.js` | `JOIN users` → `LEFT JOIN` (list + count + `getProposalWithUser`) |
| `frontend/src/components/MapView.jsx` | Popup `Người đề xuất` fallback "Khách" |

### Kết quả test

- [x] Guest hiện ở admin list (`user_name: null`, total đủ), public map list OK, search OK
- [x] Duplicates/check-nearby không JOIN nên tự bao gồm guest (không cần sửa)

### Kết quả đã đạt

- [x] Xong (table admin không có cột người tạo nên chỉ cần fallback ở popup map)

### Ghi chú

- AGENTS.md rule #4 cập nhật ở bước này (`user_id` được NULL với guest).

---

## Bước 4 — Trang `/de-xuat` + localStorage

**Trạng thái**: Hoàn thành 05/09/2026 (build OK; click-through trình duyệt + mobile để user check thủ công)

### Files đã tạo/sửa

| File | Thay đổi |
|---|---|
| `frontend/src/pages/user/GuestProposalPage.jsx` (mới) | Form + vị trí của tôi + link Google Maps + Turnstile + honeypot + cảnh báo trùng sớm + màn hình 2 mã + copy + tra cứu + "Đề xuất của bạn" + xóa mã |
| `frontend/src/layouts/GuestLayout.jsx` (mới) | Layout public riêng (không redirect login, full width — `PublicLayout` redirect + bó hẹp, không dùng được) |
| `frontend/src/App.jsx` | Route `/de-xuat` |
| `frontend/src/services/api.js` | `createGuest`, `checkNearbyPublic`, `trackByCode` |
| `frontend/src/components/dynamic/DynamicForm.jsx` | Prop `guestMode` → upload guest |
| `frontend/src/components/dynamic/DynamicField.jsx` | Prop `uploadUrl` → `FileUpload` |
| `frontend/src/components/dynamic/FileUpload.jsx` | Prop `uploadUrl` + fetch không token khi guest |

### Kết quả test

- [x] E2E không login qua API: nearby-public → upload guest → submit kèm file → track ra mã (build frontend OK)
- [x] Lấy vị trí 2 cách (geolocation theo mẫu `MapView`, link Google Maps qua endpoint public đã kiểm chứng `21.002966,105.794764`); không nhúng map click theo yêu cầu user
- [x] Upload sai loại/quá 5MB/quá số lượng → lỗi đúng (đã test ở Bước 2)
- [x] localStorage list + nhập tay + xóa (code review; chạy thử trình duyệt để user check)
- [x] Mobile/responsive (classes responsive; chạy thử trình duyệt để user check)
- [x] `npm run build` OK, backend khởi động không lỗi

### Kết quả đã đạt

- [x] Xong code (05/09/2026): endpoint public `GET /api/data-lists/:id` (route `dataListsPublic.js`, limiter 120/phút) để form guest tải options select/datalist — nguyên nhân select/multiselect trống; `getById` frontend tự fallback khi không token
- [x] Fix tên file tiếng Việt bị mojibake (`Buffer latin1→utf8` trong `uploadFile`) + còn click-through trình duyệt cho user

---

## Ghi chú chung

- Dữ liệu test đã dọn (proposals guest + file test + sequences).
- Không push GitHub khi chưa được bảo.

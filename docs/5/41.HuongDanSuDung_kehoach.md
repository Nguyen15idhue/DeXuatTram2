# 41. Trang Hướng dẫn sử dụng (User + Super Admin) với ảnh annotate — Kế hoạch chi tiết

**Ngày tạo**: 14/09/2026
**Cập nhật**: 14/09/2026 — chuyển sang mô hình **fork–join (song song)**; tách kết quả theo lane.
**File**: `docs/5/41.HuongDanSuDung_kehoach.md` (master)
**Trạng thái**: Đang triển khai
**Phụ thuộc**: `@playwright/test` + browser (đã có, dùng cho E2E), `RoleRoute`, `UserSidebar`/`AdminSidebar`, feature bản đồ docs 36–40. **Không dùng migration DB.**
**Nguồn yêu cầu**: hội thoại trực tiếp 14/09/2026 — xác nhận 3 điểm + mô hình song song.

---

## 1. Tổng quan yêu cầu

| # | Hạng mục | Mô tả |
|---|----------|-------|
| A | Trang Hướng dẫn trong app | 1 trang `/huong-dan`: số thứ tự + tiêu đề + mô tả + ảnh chú thích |
| B | Nhóm "Người dùng" | Mọi role đã đăng nhập đều xem được |
| C | Nhóm "Quản trị (Super Admin)" | Chỉ `SUPER_ADMIN` xem; role khác **ẩn tab** |
| D | Ảnh mũi tên/khoanh vùng | Tự động bằng Playwright (overlay SVG) |
| E | Nội dung | **Đầy đủ toàn bộ** chức năng |
| F | Bảo mật ảnh | Chỉ **ẩn tab**; ảnh trong `public/help/` |
| G | Chạy song song | 3 lane A/B/C độc lập, chạy đồng thời |
| H | Không thêm dependency | React + JSON tĩnh + ảnh tĩnh |

**Nguyên tắc**: script chụp ảnh **read-only**; tách artifact theo lane để chạy song song không tranh chấp.

---

## 2. Quyết định đã chốt

| # | Vấn đề | Quyết định |
|---|--------|-----------|
| 1 | Cách tạo ảnh | Playwright + overlay SVG (khoanh/mũi tên/số thứ tự) |
| 2 | Bảo mật ảnh Super Admin | Chỉ ẩn tab; ảnh công khai |
| 3 | Phạm vi | Đầy đủ toàn bộ |
| 4 | Mô hình thực hiện | **Fork–join**: T0 (chuẩn bị tuần tự) → 3 lane song song → T1 (ghép tuần tự) |
| 5 | Ghi kết quả | Mỗi lane ghi **file riêng**; master gộp ở T1 |
| 6 | Tài nguyên | Máy đủ mạnh → chạy cả 3 song song |

---

## 3. Phương pháp thực hiện song song (fork–join)

```
T0  (tuần tự, ngắn): đóng băng "hợp đồng" dùng chung
    - annotate.mjs (helper)
    - guideData.user.js / guideData.adminData.js / guideData.adminConfig.js + guideData.js (index)
    - quy ước tên ảnh, viewport, thư mục output, format status
         │
         ├── Lane A (script + ảnh user/)          ─┐
         ├── Lane B (script + ảnh admin-data/)    ─┤  SONG SONG, ghi file riêng
         └── Lane C (script + ảnh admin-config/)  ─┘
         │
T1  (tuần tự): aggregate status → HelpPage + route/menu → test quyền → build/e2e → cập nhật 41 + AGENTS → commit
```

**Vì sao tách file:** 3 tiến trình ghi song song vào 1 file sẽ đè/conflict. Mỗi lane sở hữu đường dẫn tuyệt đối riêng (script, ảnh, file kết quả, status).

### Artifact theo lane

| Lane | Script | Ảnh | Kết quả | Trạng thái (JSON) |
|---|---|---|---|---|
| A | `frontend/scripts/help/gen-images-user.mjs` | `frontend/public/help/user/` | `docs/5/41.HuongDan_nhomA_ketqua.md` | `docs/5/41.status.A.json` |
| B | `frontend/scripts/help/gen-images-admin-data.mjs` | `frontend/public/help/admin-data/` | `docs/5/41.HuongDan_nhomB_ketqua.md` | `docs/5/41.status.B.json` |
| C | `frontend/scripts/help/gen-images-admin-config.mjs` | `frontend/public/help/admin-config/` | `docs/5/41.HuongDan_nhomC_ketqua.md` | `docs/5/41.status.C.json` |
| — | `frontend/scripts/help/aggregate.mjs` | — | `docs/5/41.HuongDan_tonghop.md` | — |

### Chạy song song (không thêm dependency)
```powershell
Start-Job { node frontend/scripts/help/gen-images-user.mjs }
Start-Job { node frontend/scripts/help/gen-images-admin-data.mjs }
Start-Job { node frontend/scripts/help/gen-images-admin-config.mjs }
Get-Job | Wait-Job | Receive-Job
node frontend/scripts/help/aggregate.mjs
```

---

## 4. Kiến trúc & điểm chạm code

| Thành phần | Đường dẫn | Việc cần làm |
|---|---|---|
| Trang Hướng dẫn | `frontend/src/pages/HelpPage.jsx` (mới) | 2 tab theo quyền; render từ `guideData` |
| Data index | `frontend/src/help/guideData.js` | Re-export 2 nhóm từ 3 file dữ liệu |
| Data lane | `frontend/src/help/guideData.user.js` / `.adminData.js` / `.adminConfig.js` | Nội dung từng nhóm |
| Ảnh | `frontend/public/help/{user,admin-data,admin-config}/*.jpg` | Sinh bằng script |
| Helper | `frontend/scripts/help/annotate.mjs` | Overlay khoanh/mũi tên/số |
| Script ảnh | `frontend/scripts/help/gen-images-*.mjs` | 3 lane + aggregate |
| Route | `frontend/src/App.jsx` | Thêm `/huong-dan` (UserLayout) |
| Menu | `UserSidebar.jsx`, `AdminSidebar.jsx` | Thêm "Hướng dẫn" |

---

## 5. Phân nhóm 3 lane

| Lane | Phạm vi |
|---|---|
| **A** | Đăng nhập/Đăng ký/Hồ sơ, Bản đồ (marker, lọc, chuyển layer, nhãn HC mới/cũ, 3D, tạo đề xuất, vị trí tôi, tạo trạm nhanh), Đề xuất của tôi, Guest đề xuất, Thông báo |
| **B** | Dashboard, Users (+import/export/cây), Trạm (+import/export), Đề xuất (+duyệt/1Office), Audit Log, File bản ghi |
| **C** | Fields, Forms+Builder, Views+Builder, Data Lists, Map Config, Geocode, Phân quyền, API Configs (+Mapping/Template/Sync), Swagger |

---

## 6. Giai đoạn T0 — Đóng băng hợp đồng (tuần tự)

**Nội dung**
- Tạo `annotate.mjs` (helper khoanh/mũi tên/số) — sau đó coi như read-only.
- Tạo 3 file `guideData.*.js` + index `guideData.js`.
- Tạo thư mục `frontend/public/help/{user,admin-data,admin-config}/`.
- Chốt viewport 1366×768, JPEG quality 82, tên ảnh `<lane><so>_<slug>.jpg`, format `status.<lane>.json`.

**Yêu cầu cần đạt**
- Helper + schema + quy ước cố định; 3 lane không cần sửa file chung.

**Checklist test**
- [ ] `guideData.js` import OK (không lỗi cú pháp).
- [ ] `annotate.mjs` export đủ hàm.
- [ ] 3 thư mục ảnh tồn tại.

**Kết quả đã đạt**
- [x] `frontend/scripts/help/annotate.mjs` + `shared.mjs` (login/goto có tự khôi phục token).
- [x] `frontend/src/help/guideData.user.js` / `.adminData.js` / `.adminConfig.js` + `guideData.js`.
- [x] Thư mục `frontend/public/help/{user,admin-data,admin-config}/`.
- [x] Viewport 1366×768, JPEG q82, quy ước tên ảnh, `41.status.<lane>.json`.

**Ghi chú**
- Không để lane nào sửa `annotate.mjs` hay `guideData.js` sau khi fork.

---

## 7. Giai đoạn song song — 3 lane (chi tiết trong file kết quả từng lane)

Mỗi lane tự: viết script → sinh ảnh → verify → ghi `41.status.<lane>.json` + `41.HuongDan_nhom<X>_ketqua.md`.

- Lane A → `docs/5/41.HuongDan_nhomA_ketqua.md`
- Lane B → `docs/5/41.HuongDan_nhomB_ketqua.md`
- Lane C → `docs/5/41.HuongDan_nhomC_ketqua.md`

**Yêu cầu cần đạt (toàn bộ lane)**: đủ ảnh theo `guideData`, không ảnh trắng/0 byte, tổng < ~6MB.

---

## 8. Giai đoạn T1 — Ghép & nghiệm thu (tuần tự)

**Nội dung**
- `aggregate.mjs` đọc 3 status → sinh `docs/5/41.HuongDan_tonghop.md` (bảng).
- `HelpPage.jsx` + route `/huong-dan` + menu links.
- Test quyền (CTV không thấy tab Quản trị; SUPER thấy đủ 2 tab) — dùng Playwright.
- `vite build` + `npm run test:e2e`.
- Cập nhật master 41 + `AGENTS.md`.

**Yêu cầu cần đạt**
- Trang chạy end-to-end; quyền đúng; build pass; e2e 18/18.

**Checklist test**
- [ ] aggregate đọc đủ 3 status.
- [ ] CTV: ẩn tab Quản trị; SUPER: đủ 2 tab.
- [ ] 100% ảnh load (`naturalWidth>0`).
- [ ] build pass; e2e 18/18; 0 lỗi console.

**Kết quả đã đạt**
- [x] `aggregate.mjs` → `docs/5/41.HuongDan_tonghop.md` (A 14/14, B 11/11, C 12/12).
- [x] `HelpPage.jsx` + route `/huong-dan` + menu (UserSidebar, AdminSidebar).
- [x] Test quyền: SUPER thấy 2 tab; CTV chỉ tab "Người dùng".
- [x] `vite build` pass; `npm run test:e2e` 18/18; ảnh user 14/0 hỏng, admin 23/0 hỏng.

---

## 9. Definition of Done

- [x] `/huong-dan` chạy; tab "Người dùng" mọi role, tab "Quản trị" chỉ SUPER_ADMIN.
- [x] Nội dung đầy đủ toàn bộ chức năng (37 bước, 37 ảnh).
- [x] 100% step có ảnh annotate load được (user 14/0 hỏng, admin 23/0 hỏng).
- [x] 3 script chạy độc lập & song song, read-only, idempotent.
- [x] Không thêm dependency; build pass; e2e 18/18; 0 lỗi console.
- [x] 3 file lane + tonghop + AGENTS cập nhật.

## 12. Phát hiện ngoài phạm vi (ghi nhận)

- **Bug tiềm ẩn**: `/de-xuat` (guest) gọi `/api/field-definitions?entity=station_proposals` → **401** → `api.js` interceptor redirect `/login` ⇒ **form đề xuất cho khách chưa đăng nhập bị chặn**. Cần kiểm tra/nhường endpoint public cho guest (ngoài phạm vi kế hoạch 41).
- **Fix kèm theo**: `backend/src/routes/tiles.js` — bản retry trước gây crash `ERR_HTTP_HEADERS_SENT` (khi tile lỗi liên tiếp); đã viết lại `fetchTile`/`sendFallback` có guard `headersSent/writableEnded` + cờ `committed`/`finished` (không còn crash, retry 1 lần an toàn).

## 10. Rủi ro & xử lý

| Rủi ro | Mức | Xử lý |
|---|---|---|
| Ghi chồng file giữa lane | Thấp | Đường dẫn/tiền tố riêng mỗi lane |
| Ảnh bản đồ trắng | Trung bình | Chờ `idle` + timeout; retry 1 lần |
| Selector đổi | Trung bình | Selector ổn định/title; chạy lại script |
| 3 Chromium nặng | Thấp (máy đủ mạnh) | Có thể hạ còn 2 nếu cần |
| Vô tình lưu cấu hình | Thấp | Chỉ mở panel, không submit |

## 11. Ghi chú chung

- Ảnh lưu `frontend/public/help/` → phục vụ `/help/...`.
- Có thể chạy lại từng lane bất kỳ lúc nào khi UI đổi; ảnh cập nhật theo.
- Kết quả chi tiết xem 3 file lane + file tổng hợp.

---

# PHẦN BỔ SUNG (15/09/2026) — Tái cấu trúc theo Nhóm chức năng + Luồng thực hiện

**Trạng thái**: Chưa triển khai
**Nguyên tắc chốt với user**: KHÔNG code thêm tính năng (ô ID 1Office formula `{id_1office}` đã có —
`backend/src/services/formulaService.js:157`, `dynamicEngineService.js:237`); chỉ bổ sung/sắp lại hướng dẫn.
Mọi bước gom vào đúng **1 trong 3 đầu mục I / II / III**, không section lẻ 1 bước.

## 12. Cấu trúc mới (thay 2 tab user/admin)

`GUIDE = { groups (I), flows (II), integrations (III) }` — Flow reference step Nhóm bằng `ref`, không copy ảnh.

### I. NHÓM CHỨC NĂNG (6 nhóm, 38 bước)

| Nhóm | Bước | Ảnh |
|---|---|---|
| I.1 Tài khoản (5) | G01 Đăng nhập; G02 Đăng ký; G03 Đăng xuất (mới); G04 Hồ sơ; G05 Đổi mật khẩu (mới) | Giữ a01,a02,a03 + mới G03,G05 |
| I.2 Bản đồ & xem dữ liệu (6) | G06 Marker + chú thích; G07 Bộ lọc; G08 Chuyển nền; G09 Nhãn HC mới/cũ; G10 3D; G11 Bản đồ lân cận 5/10/20/50km (mới) | Giữ a04–a08 + mới G11 |
| I.3 Đề xuất của tôi (5) | G12 Danh sách + tìm kiếm; G13 Xem chi tiết; G14 Sửa + Gửi lại; G15 Xóa (mới); G16 Chuông thông báo (nhập A14, không để lẻ) | Giữ a12,a14 + mới G15 |
| I.4 Khách không đăng nhập (2) | G17 Gửi form `/de-xuat` + CAPTCHA; G18 Tra cứu tracking + check trùng (mới) | Giữ a13 + mới G18 |
| I.5 Quản trị dữ liệu (8) | G19 Dashboard; G20 Users + cây SALES→CTV; G21 Tạo user; G22 Import/Export; G23 Stations list; G24 Thêm trạm (admin); G25 Proposals list; G26 Duyệt/Từ chối + lý do | Giữ b01–b08 |
| I.6 Cấu hình hệ thống (12) | G27 Fields; G28 Forms; G29 Form Builder; G30 Views; G31 View Builder; G32 Data Lists; G33 Map Config; G34 Geocode; G35 Phân quyền; G36 API Configs; G37 Mapping/Template/Sync NS; G38 Swagger + File bản ghi (nhập B10,B11) | Giữ c01–c12,b10,b11 |

### II. LUỒNG THỰC HIỆN (3 flow, thanh Bước x/y + Prev/Next, ảnh dùng lại mục I)

| Flow | Chặng |
|---|---|
| F1. Tạo đề xuất — 2 cách | F1.1 Cách 1 tại `/map` (`+` → 3 cách chọn tọa độ, `MapPage.jsx:161`) → F1.2 Form tự điền → F1.3 Cách 2 tại `/my-proposals` (nút Tạo đề xuất → click mini map, `MyProposalsPage.jsx:318,369`, mới) → F1.4 Kiểm tra trùng |
| F2. Vòng đời đề xuất | F2.1 Xem → F2.2 Sửa → F2.3 Gửi lại → F2.4 Xóa → F2.5 Admin duyệt/từ chối → F2.6 Nhận thông báo |
| F3. Tạo trạm — 2 cách | F3.1 Cách 1 tại `/map` (FAB Tạo trạm nhanh) → F3.2 Cách 2 tại `/admin/stations` (Thêm trạm, `AdminStationsPage.jsx:280`) → F3.3 Kiểm tra trùng + `loai_uu_tien`/`mo_hinh_tram` |

### III. TÍCH HỢP 1OFFICE (flow F4, 5 chặng)

F4.1 Push (`SyncPanel.jsx:182`) → F4.2 Pull + Link tay mã contact (`AdminProposalsPage.jsx:553`) / Unlink (`:419`) →
F4.3 Ô ID 1Office formula `{id_1office}` click mở sang 1Office (đã có tính năng, chỉ hướng dẫn) →
F4.4 Audit log (`AdminAuditLogPage.jsx`, Retry/Cancel chỉ SUPER) → F4.5 Mapping + Desc Template + Sync nhân sự.

## 13. Việc làm & điểm chạm code

- Data: giữ `guideData.{user,adminData,adminConfig}.js` làm kho step thô; thêm `guideData.groups.js` (I),
  `guideData.flows.js` (II), `guideData.integrations.js` (III) + index mới; step schema thêm optional `route, roles, tags`.
- `HelpPage.jsx` viết lại: 3 tab I/II/III (gating SUPER cho I.5, I.6, III); sidebar TOC sticky + scrollspy +
  anchor `#G12/#F4.3` + copy-link; search realtime + phím `/`; ảnh `aspect-video object-contain` + Lightbox
  (zoom/Esc/←→); back-to-top; mobile TOC drawer.
- Ảnh mới (7): G03 đăng xuất, G05 đổi MK, G11 bản đồ lân cận, G15 xóa đề xuất, G18 tra cứu tracking,
  F1.3 tạo từ my-proposals, F4.3 ô ID 1Office. Chuẩn cũ: viewport 1366×768, JPEG q82, tổng < 6MB.

## 14. Tiến độ (rà soát tại đây)

| Chặng | Nội dung | Trạng thái | Ghi chú |
|---|---|---|---|
| T0 | Menu Hướng dẫn ở cả user + admin | ✅ Xong 15/09/2026 | Thêm route `/admin/huong-dan` (`App.jsx`) để admin vẫn ở AdminLayout; `AdminSidebar` trỏ `/admin/huong-dan` + active state; user giữ `/huong-dan` |
| T1 | Viết `guideData.groups/flows/integrations.js` + index mới | ✅ Xong 15/09/2026 | 6 nhóm I (38 bước G01–G38) + 3 flow II (F1–F3) + flow III (F4); `resolveFlowStep()`; giữ file cũ tương thích |
| T2 | Chụp 7 ảnh mới + verify `naturalWidth>0` | ✅ Xong 15/09/2026 | Script `gen-images-groups.mjs` 7/7 OK (g03, g05, g11, g15, g18, f13, f43); đã gắn vào groups/flows/integrations; G15 chỉ mở dialog rồi Hủy — DB còn đủ 23 proposals |
| T3 | Viết lại HelpPage (TOC/search/lightbox/flow) | ✅ Xong 15/09/2026 | 3 tab I/II/III + gating SUPER (I.6, III); sidebar TOC sticky + scrollspy + mobile select; search + phím `/`; deep-link `?tab=` + `#id`; lightbox zoom/Esc/←→; nút Mở trang/Copy link; back-to-top |
| T4 | `vite build` + `test:e2e` + cập nhật 41 + AGENTS | 🟡 Dở dang | esbuild parse OK 7/7 file; `vite build` fail sẵn do `maplibre-gl` khai báo nhưng chưa install (không liên quan HelpPage) — cần `npm install` rồi build lại + e2e |

## 15. Viết lại nội dung nhóm user theo mẫu 7 mục (chờ duyệt)

Trạng thái: ✅ Xong 15/09/2026 — chờ user duyệt rồi mới làm nhóm admin (I.5, I.6, III/F4).

- Schema step mới (tương thích ngược): `purpose, audience, prereq, substeps[], expected, errors[], related[]`;
  `StepCard` render đầy đủ + nút nhảy bước liên quan; search gồm cả trường mới.
- Mục 0 `Bắt đầu tại đây` (`guideData.start.js`, đứng đầu TOC): S01 toàn cảnh + hành trình đề xuất,
  S02 vai trò, S03 thuật ngữ + màu marker, S04 vòng đời trạng thái.
- Viết lại I.1–I.4 (18 bước G01–G18) + ghi chú F1–F3 theo mẫu mới.
- Verify 9/9: TOC 7 mục, mặc định mở Bắt đầu, substeps/errors/related render + nhảy đúng, 0 page error.

## 16. Definition of Done (phần bổ sung)

- [ ] Không section 1 bước; mọi bước thuộc đúng 1 đầu mục I/II/III.
- [ ] F1/F3 thể hiện rõ 2 cách tạo (map vs my-proposals / map vs admin/stations).
- [ ] F4 hướng dẫn đủ Push → Pull/Link/Unlink → click ID 1Office → Audit log → Mapping.
- [ ] Deep-link `?tab=&s=#` hoạt động; search + lightbox + mobile drawer OK.
- [ ] Build pass; e2e pass; 0 console error; ảnh mới đủ 7/7 load được.

## 17. Duyệt tự đẩy 1Office (tính năng mới 15/09/2026)

- Backend: `updateStatus → APPROVED` (lần đầu) tự enqueue push qua config contact active mặc định
  (`apiConfigService.getDefaultPushConfig`); lỗi push không làm fail duyệt; trả `autoPush` về FE.
  Đẩy thủ công giữ nguyên. Đã test API: approve #377 → job push → cancel → revert PENDING.
- Frontend `AdminProposalsPage`: chọn Duyệt mở popup xác nhận (không hoàn tác + tự gửi 1Office +
  chưa thành trạm thật); duyệt xong toast lệnh chờ + poll job như đẩy thủ công.
- Hướng dẫn: S01/S04/G26/F2/F4.1 sửa "duyệt → tự gửi 1Office, chưa thành trạm thật". AGENTS 4.2 cập nhật.
- Verify UI 6/6 (modal, cảnh báo, Hủy đóng); DB nguyên vẹn (23 proposals, 0 pending job).

## 18. Đợt chỉnh 15/09/2026 (theo feedback)

- (0) Popup duyệt gọn lại 1 câu, bỏ khung alert (alert flex làm vỡ chữ thành cột).
- (1) Thêm bước G12B Kiểm tra trùng lặp (ảnh `g12b_check-trung.jpg`) vào Đề xuất của tôi.
- (2) TOC submenu 2 tầng: nhóm `Quản lý hệ thống` (Dashboard, Users, Trạm, Đề xuất) +
  `Quản lý cấu hình` (Cấu hình trường, Forms, Views, Data Lists, Bản đồ & Địa chỉ, Phân quyền & API);
  mobile dùng optgroup. Đổi tên sections theo đúng từ user.
- (3) Fix nút related: bỏ related trong tab Luồng/Tích hợp (trỏ bước nhóm không có trong pane);
  tab Nhóm tự nhảy sang section chứa bước (S02 bỏ related G35 vì CTV không thấy); DOM id duy nhất
  trong flow (anchor `F1-0...`). Verify submenu 5/5 + CTV 5 mục.
- (4) TOC + nội dung cùng nằm trong container khóa chiều cao, mỗi bên cuộn riêng, trang ngoài không cuộn
  (TOC bottom 740/900, mục cuối tới được, 0 page error).
- (5) Viết lại 19 bước admin (G19–G38) theo đúng mẫu 7 mục của user; chiều cao container đo theo từng
  layout (user 836+64=900, admin 852+48=900) + `lg:-mb-6` che kín padding; fix clipboard unhandled rejection.
  Verify 5/5: related liên section (G25/S04/S02) đáp đúng, gap 24px = padding chuẩn của main.

## 19. Highlight menu + fix desc lồng desc (15/09/2026)

- Menu: viền RGB gradient động (`hl-rgb`) quanh Hướng dẫn ở UserHeader/UserSidebar
  (sau feedback chỉ giữ bên user, admin về mặc định);
  viền đứt xanh pulse (`hl-dash-pulse`) quanh Xem bản đồ ở AdminSidebar (`index.css`). Verify 3/3.
- 1Office desc: thiếu `charset=UTF-8` gây mojibake + Mô tả dính bản render cũ gây lồng desc
  (5 proposals 386–388, 403–404). Fix: header charset + `sanitizePriorDesc` trong `templateService.render`;
  khôi phục Mô tả gốc từ payload push đầu tiên. AGENTS cập nhật.

## 20. Chặn đẩy 1Office khi thiếu người phụ trách (15/09/2026)

- Yêu cầu chốt: (1) cả 2 đường đẩy đều popup xác nhận; (2) `nguoi_phu_trach`/`nguoi_giao_phu_trach`
  phải có giá trị; (3) thiếu → chặn đẩy; (4) popup dùng `label` động (đổi tên field vẫn đúng);
  (5) bỏ qua rủi ro đề xuất cũ.
- BE: `syncService.getMissingPushUserFieldLabels` (label từ `field_definitions`) dùng chung cho
  `pushTo1Office` (lỗi từng đề xuất) và `adminProposalService.updateStatus` (chặn duyệt, 400).
- FE `AdminProposalsPage`: `blockModal` cảnh báo khi duyệt thiếu field (label động qua `getFieldLabel`);
  `pushConfirm` popup xác nhận cho nút "Đẩy sang 1Office" + liệt kê đề xuất sẽ bị chặn.
- Verify: API chặn 391 (approve + push), cho phép 394 (duyệt + auto-push + revert), 0 job treo;
  UI 10/10 (2 modal, label, đóng modal, 0 lỗi trang).
- Bổ sung sau feedback: popup Duyệt và popup Đẩy sang 1Office đều có khối "Thông tin gửi sang 1Office"
  hiển thị label động + giá trị 2 trường (thiếu thì hiện "Thiếu" màu đỏ). Verify UI 10/10.

# 28. Trích luân yêu cầu — Phân quyền, Mã đề xuất, Tick chọn, Push/Pull

**Cập nhật**: 09/09/2026

---

## Tổng quan yêu cầu

| # | Vấn đề | Mô tả |
|---|--------|-------|
| 1 | Audit Log vị trí | Chuyển khỏi nhóm "Cấu hình", đặt ngay dưới "Quản lý Đề xuất" |
| 2 | Audit Log cột mới | Thêm "Mã đề xuất" (`ma_de_xuat`) + "Người thực hiện" (`created_by → full_name`) |
| 3 | Audit Log phân quyền | Admin/Super thấy tất cả. Sales chỉ thấy của mình (`created_by = user.id`) |
| 4 | Proposals nút Push/Pull/Link | Thêm 3 nút��作 đồng batch. Vietnamese: "Đẩy", "Lấy về", "Liên kết" |
| 5 | Proposals phân quyền nút | Admin/Super: 3 nút. Sales: chỉ push + pull |
| 6 | Proposals mã đề xuất | Bug: tạo đề xuất qua API không tạo `tracking_code`. Cần fix `createProposal` |
| 7 | Tick chọn tất cả table | Checkbox ở cột đầu, header "chọn tất cả", bottom bar "Đã chọn: N" |
| 8 | Search theo ID | Input search Accept `#123` hoặc `123` để tìm theo ID |
| 9 | Mobile responsive | Nút group dropdown trên mobile, tick chọn vẫn có |

---

## Bước 1 — Audit Log: Vị trí + Cột mới + Phân quyền

### 1.1: Di chuyển sidebar

**File:** `frontend/src/components/layout/AdminSidebar.jsx`

**Thay đổi:** Moving `audit-log` menu item from config group to top-level (right below "Quản lý Đề xuất").

### 1.2: Backend — Phân quyền cho Sales

**File:** `backend/src/routes/queueLogs.js`

**Thay đổi:**
- Replace `requireSuperAdmin` → `requireAdmin` cho GET endpoints
- GET `/`: Thêm logic Sales filter `WHERE created_by = user.id`
- GET `/stats`: Tương tự
- POST retry/cancel: Giữ `requireSuperAdmin` (chỉ admin mới được retry/cancel)

**File:** `backend/src/services/queueService.js`

**Thay đổi:**
- `getAll()`: Thêm param `createdBy` để filter
- `getStats()`: Thêm param `createdBy`

### 1.3: Backend — Audit Log join users + proposals

**File:** `backend/src/routes/queueLogs.js`

**Thay đổi:** GET `/` query JOIN `users` + `station_proposals` để lấy `full_name` + `ma_de_xuat`.

### 1.4: Frontend — Audit Log table columns mới

**File:** `frontend/src/pages/admin/AdminAuditLogPage.jsx`

**Thay đổi:**
- Thêm cột "Mã đề xuất" (index 4, sau Action)
- Thêm cột "Người thực hiện" (index 5, sau Mã đề xuất)
- Fetch data includes `full_name`, `ma_de_xuat`

### Checklist test Bước 1
- [x] Sidebar: Audit Log hiển thị ngay dưới "Quản lý Đề xuất" ✅ (8/8 PASS)
- [x] Admin login: Thấy tất cả logs ✅
- [x] Sales login: Chỉ thấy logs của mình ✅ (phân quyền requireUserManager + filter created_by)
- [x] Cột "Mã đề xuất" hiển thị đúng ✅ (JOIN station_proposals → ma_de_xuat)
- [x] Cột "Người thực hiện" hiển thị đúng ✅ (JOIN users → full_name)
- [x] Sales không retry/cancel được (giữ requireSuperAdmin) ✅

---

## Bước 2 — Proposals: Mã đề xuất + Push/Pull/Link + Tick chọn

### 2.1: Fix tạo mã đề xuất

**File:** `backend/src/services/proposalService.js`

**Thay đổi:** `createProposal()` gọi `generateTrackingCode()` trước khi INSERT, lưu vào `ma_de_xuat` field trong `custom_data`.

**Lưu ý:** `ma_de_xuat` là field động (`source_type=json`), lưu trong `custom_data` JSON.

### 2.2: Backend — Push/Pull/Link batch endpoints

**File:** `backend/src/routes/oneOfficeSync.js`

**Thay đổi:**
- POST `/push` hiện tại nhận `proposalIds` (array) — đã hỗ trợ batch
- POST `/pull` hiện tại nhận `proposalIds` (array) — cần verify
- POST `/link` hiện tại nhận `proposalIds` + `contactCode` — cần verify
- Thêm auth: `requireAdmin` (Sales + Admin đều push/pull được)

### 2.3: Frontend — Proposals page nút Push/Pull/Link

**File:** `frontend/src/pages/admin/AdminProposalsPage.jsx`

**Thay đổi:**
- Thêm 3 nút trên header (cùng hàng với filter/search):
  - "Đẩy sang 1Office" (icon: Upload) — Admin/Super/Sales
  - "Lấy về" (icon: Download) — Admin/Super/Sales
  - "Liên kết" (icon: Link) — Admin/Super only
- Nút disable khi không có record tick chọn
- Click → gọi API batch với danh sách IDs đã chọn

### 2.4: Frontend — Tick chọn records

**File:** `frontend/src/components/dynamic/DynamicTable.jsx`

**Thay đổi:**
- Thêm cột checkbox ở đầu mỗi row
- Header: checkbox "chọn tất cả" (chỉ selects trang hiện tại)
- Bottom bar: "Đã chọn: N" + các nút thao tác
- State: `selectedIds` array

### Checklist test Bước 2.1
- [x] Tạo đề xuất qua API → có `tracking_code` tự动生成 (format `DX-XXXXXX`) ✅ (ID=394, DX-DAF3F9)
- [x] `ma_de_xuat` lưu trong `custom_data` ✅
- [x] `ma_de_xuat_gen` (generated column) tự cập nhật ✅

### Checklist test Bước 2.2
- [x] Proposals page: Hiển thị mã đề xuất ✅
- [x] Admin: Thấy 3 nút Push/Pull/Link ✅ (13/13 PASS)
- [x] Sales: Chỉ thấy Push + Pull (Link ẩn khi !isAdmin) ✅
- [x] Tick chọn header → chọn tất cả trang ✅ (10 selected)
- [x] Tick chọn 1 record → nút enable ✅
- [x] Tick chọn nhiều records → hiển thị count ✅
- [x] Bỏ chọn → nút ẩn ✅
- [x] Search theo mã đề xuất ✅

---

## Bước 3 — Tất cả table: Search ID + Tick chọn

### 3.1: Component reusable — RowSelect

**File:** `frontend/src/components/common/RowSelect.jsx` (tạo mới)

**Features:**
- Checkbox column component
- Props: `selectedIds`, `onToggle`, `onToggleAll`, `data`, `disabled`
- Header checkbox: "chọn tất cả trang hiện tại"

### 3.2: Component reusable — SearchById

**File:** `frontend/src/components/common/SearchById.jsx` (tạo mới)

**Features:**
- Input Accept `#123` hoặc `123`
- Props: `value`, `onChange`, `placeholder`
- onBlur: parse input, call `onSearch(id)`

### 3.3: Áp dụng cho các trang

**Các trang cần sửa:**
- `AdminProposalsPage.jsx` — tick chọn + search ID
- `AdminStationsPage.jsx` — tick chọn + search ID
- `AdminUsersPage.jsx` — tick chọn + search ID
- `AdminAuditLogPage.jsx` — search ID (tick chọn không cần vì đây là log)

**Lưu ý:** DynamicTable hiện tại không có tick chọn. Cần sửa DynamicTable hoặc wrap bằng component mới.

### 3.4: Mobile responsive

**File:** `frontend/src/components/dynamic/DynamicTable.jsx`

**Thay đổi:**
- Desktop: Hiển thị đầy đủ checkbox + columns
- Mobile: Ẩn bớt columns, hiện checkbox + action dropdown

### Checklist test Bước 3
- [x] Proposals: Tick chọn + select all ✅ (10 rows, select all 10)
- [x] Stations: Tick chọn + select all ✅ (10 rows, select all 10)
- [x] Users: Tick chọn + select all ✅ (17 rows, select all 17)
- [x] Search theo mã đề xuất ✅
- [x] Không có console errors ✅
- [x] DynamicTable hỗ trợ selectedIds + onSelectionChange ✅

---

## API Changes

### GET /api/admin/queue-logs

**Query params mới:**
- `created_by` (number) — Filter theo người tạo (Sales tự động filter)

**Response data mới:**
```json
{
  "id": 1,
  "action": "push",
  "status": "completed",
  "ma_de_xuat": "DX-ABC123",
  "owner_name": "Nguyễn Văn A",
  "full_name": "Admin User",
  ...
}
```

### POST /api/proposals

**Request body mới:** Không thay đổi

**Response:**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "ma_de_xuat": "DX-ABC123",
    ...
  }
}
```

---

## Files cần tạo/sửa

| # | File | Action | Mô tả |
|---|------|--------|-------|
| 1 | `frontend/src/components/layout/AdminSidebar.jsx` | Sửa | Di chuyển Audit Log menu |
| 2 | `backend/src/routes/queueLogs.js` | Sửa | Phân quyền Sales + JOIN users/proposals |
| 3 | `backend/src/services/queueService.js` | Sửa | Thêm createdBy filter + topErrors |
| 4 | `frontend/src/pages/admin/AdminAuditLogPage.jsx` | Sửa | Thêm cột mã đề xuất + người thực hiện |
| 5 | `backend/src/services/proposalService.js` | Sửa | Fix createProposal tạo tracking_code |
| 6 | `frontend/src/pages/admin/AdminProposalsPage.jsx` | Sửa | Thêm Push/Pull/Link + tick chọn |
| 7 | `frontend/src/components/dynamic/DynamicTable.jsx` | Sửa | Thêm tick chọn + search ID |
| 8 | `frontend/src/components/common/RowSelect.jsx` | Tạo mới | Checkbox column component |
| 9 | `frontend/src/components/common/SearchById.jsx` | Tạo mới | Search by ID component |

---

## Test Plan

### Backend tests
- [x] GET /api/admin/queue-logs — Admin thấy tất cả, Sales chỉ thấy của mình ✅
- [x] POST /api/proposals — Tạo proposal có `tracking_code` + `ma_de_xuat` ✅ (ID=394, DX-DAF3F9)
- [ ] POST /api/admin/1office/push — Batch push với proposalIds array (chưa test vì cần 1Office token)
- [ ] POST /api/admin/1office/pull — Batch pull với proposalIds array (chưa test vì cần 1Office token)

### Frontend Playwright tests
- [x] Audit Log: Sidebar vị trí đúng ✅ (8/8 PASS)
- [x] Audit Log: Cột mã đề xuất + người thực hiện ✅
- [x] Proposals: 3 nút Push/Pull/Link (Admin), 2 nút (Sales) ✅ (13/13 PASS)
- [x] Proposals: Tick chọn + batch buttons ✅
- [x] Proposals: Tạo proposal có mã đề xuất ✅
- [x] All tables: Tick chọn records ✅ (16/16 PASS)
- [x] Không có console errors ✅
- [ ] Mobile responsive (chưa test)

### Tổng kết
- **Backend:** 2/4 PASS, 2 chưa test (cần 1Office)
- **Frontend:** 37/37 PASS

---

## Phần 2 — 5 yêu cầu mới (10/09/2026)

### YC1: Mã dùng formula + auto-update + cascading tỉnh
- **Bug gốc:** `dynamicEngineService` tạo `math` riêng không có custom functions (CONCAT/IF/LPAD/SEQ) → mọi post-formula lỗi câm → fix bằng cách dùng lại `formulaService.math` (export thêm `math`).
- Bỏ `generateTrackingCode()` random (`DX-XXXXXX`); `tracking_code` = kết quả formula `ma_de_xuat` (`TDT_HCM_0001`).
- `updateProposal` (admin + my) và `updateStation` trước đây xóa post-keys nhưng KHÔNG recompute → đã thêm `computePostFormulas` sau save (mã đổi theo khi sửa tỉnh/mô hình).
- Fuzzy match tỉnh trong `getDiaGioiByTenTinh` (bỏ tiền tố Thành phố/Tp/Tỉnh, không phân biệt hoa thường) → `"Hà Nội"` khớp `"Thành phố Hà Nội"`.
- Migration: 16 proposals + 24 stations recompute mã; chuẩn hóa tên tỉnh ngắn → đầy đủ.
- `tdt_tong_cong` có `formula_config NULL` — bỏ qua theo yêu cầu user.
- Test: tạo `TDT_HCM_0001` → sửa sang LK → `LK_HCM_0001` ✅

### YC2: Tick chọn bo tròn
- `DynamicTable.jsx`: `checkbox-xs` → `checkbox-sm` (DaisyUI vuông bo góc).

### YC3: Push/Pull/Link báo thật
- Push: toast theo kết quả từng dòng (`Đã tạo X lệnh đẩy (theo dõi trong Audit Log)` + liệt kê dòng lỗi), thay vì `Đẩy thành công N`.
- Pull: chuyển thành action toàn cục (không cần tick chọn, backend vốn bỏ qua `proposalIds`).
- Link batch (gửi `{proposalIds}` trong khi backend cần `{proposalId, contactCode}` → 400) → chuyển thành nút Link/Hủy link theo dòng + modal nhập mã; toast kết quả thật.
- `getAllProposals` trả thêm `contact_1office_code`, `sync_status` để render nút theo dòng.

### YC4: Audit Log + field ID 1Office
- Fix 500 list: JSON path `$?.ma_de_xuat` → `$.ma_de_xuat` (`queueService.getAll`).
- Fix 500 stats: thiếu `WHERE` khi không có filter (`getStats`).
- Fix flag worker `isRunning` luôn false (tách biến `started`).
- Field mới `id_1office` (id=158, formula post `IF(LEN(id_1office) > 0, id_1office, 'Chưa liên kết')`) + metadata `id_1office` trong `computePostFormulas` (mirror `sales_name`, đọc `contact_1office_code`) + gắn form 13 / view 8 (script `database/41-add-id-1office-field.sql`).
- Recompute `id_1office` sau link/unlink/push (loại trừ `ma_de_xuat` để không đốt SEQ).
- Bổ sung `id_1office` vào `POST_METADATA` validate + `buildPostScope` preview + list `POST_METADATA` trong `FormulaEditor.jsx` (giờ 10 biến).
- Demo push thật proposal #403 → contact 1Office `TDT_HCM_0005` (`view?ID=212`), `id_1office` tự điền; backfill 28 proposals cũ.
- Verify: list 200 (33 jobs), stats 200; Playwright Audit Log 20 rows ✅

### YC5: Gom nút vào Vertical Ellipsis
- Dropdown `...` luôn hiển thị (Push cần tick chọn, Pull toàn cục); bỏ batch Link; thêm đóng menu bằng Escape (fix backdrop chặn click).

### Files thay đổi (Phần 2)
| File | Thay đổi |
|---|---|
| `backend/src/services/formulaService.js` | Export `math`; `POST_METADATA` + `buildPostScope` thêm `id_1office` |
| `backend/src/services/dynamicEngineService.js` | Dùng lại `formulaService.math`; metadata `id_1office` |
| `backend/src/services/proposalService.js` | Bỏ random code; `tracking_code` từ formula |
| `backend/src/services/adminProposalService.js` | Recompute post-formula khi update; SELECT thêm link columns |
| `backend/src/services/myProposalService.js` | Recompute post-formula khi update |
| `backend/src/services/stationService.js` | Recompute post-formula khi update |
| `backend/src/services/dataListService.js` | Fuzzy match tên tỉnh |
| `backend/src/services/queueService.js` | Fix JSON path + fix WHERE stats |
| `backend/src/services/syncService.js` | Recompute `id_1office` sau link/unlink |
| `backend/src/workers/queueWorker.js` | Flag `started`; recompute `id_1office` sau push |
| `backend/src/routes/queueLogs.js` | (giữ nguyên phân quyền Bước 1) |
| `frontend/src/components/dynamic/DynamicTable.jsx` | checkbox-sm |
| `frontend/src/components/dynamic/FormulaEditor.jsx` | Thêm `{id_1office}` vào metadata list |
| `frontend/src/pages/admin/AdminProposalsPage.jsx` | Ellipsis menu + Escape; handlers thật; Link/Unlink theo dòng + modal |
| `frontend/src/pages/admin/AdminStationsPage.jsx` | selectedIds |
| `frontend/src/pages/admin/AdminUsersPage.jsx` | selectedIds |
| `database/41-add-id-1office-field.sql` | Mới: field + form 13 + view 8 |

### Test tổng hợp (Playwright, 10/09/2026)
12/12 PASS (11/12 + 1 modal verify riêng 4/4): checkbox, menu, Escape, Link/Unlink dòng, modal, cột Mã/ID 1Office, mã formula, Audit Log 20 rows, mã trạm, 0 console errors.

---

## Phần 3 — ID 1Office phải là số + click qua link (10/09/2026)

**Vấn đề:** field `ID 1Office` hiển thị `TDT_HCM_0005` — đó là mã mình GỬI đi, không phải ID thật. ID thật là số trong URL 1Office (`view?ID=212`); contact pull về có dạng `{ID: 2, code: '2', ...}`.

**Fix:**
- Worker push: parse `ID=(\d+)` từ `result.data.url` → lưu `contact_1office_id` (giữ `contact_1office_code` như cũ).
- `linkProposal`: resolve ID số qua `getContactDetail(apiConfigId, code)` (thất bại thì bỏ qua, giữ NULL).
- Metadata `id_1office`: ưu tiên số, fallback mã.
- Field 158: `outputType: 'url'` + `url_template: https://egr.1office.vn/apps/customer-contact-contact/view?ID={value}` (script `database/42-id-1office-url-template.sql`).
- `FieldRenderer`: có template + giá trị là số → render `<a>` text=số, href=template, mở tab mới; chưa liên kết/mã chữ → text thường (không link gãy).
- `FormulaEditor`: thêm ô nhập URL template khi outputType=url.
- Backfill proposal #403 → `contact_1office_id=212`, `id_1office='212'`.

**Verify:**
- Bảng proposals: `212` là link → `https://egr.1office.vn/apps/customer-contact-contact/view?ID=212` (target blank) ✅
- Push mới proposal #404 → `contact_1office_id=218`, `id_1office='218'` tự động ✅

---

## Phần 4 — Phân quyền Link + upsert khi push trùng mã (10/09/2026)

**Q1: Link đã khóa admin chưa?** Chưa — route `/link` và `/unlink` chỉ có `requireAuth` (frontend giấu nút nhưng SALES/CTV gọi API trực tiếp vẫn lọt). Fix: thêm `requireAdmin` cho cả 2 route. Verify: sales gọi link/unlink → 403 ✅; sales push → vẫn 200 (đúng yêu cầu Sales được push/pull) ✅.

**Q2: Hủy link rồi push lại báo thành công nhưng 1Office không có mới — có phải lỗi?** Đúng là lỗi. Nguyên nhân: push gửi cùng `code` cũ, 1Office dedupe theo mã → job `completed` giả, không bản ghi mới. Test thực tế: proposal #405 push → contact 221 → unlink → push lại → job 49 `completed` nhưng không có contact mới.
Fix (upsert trong `processPushJob`): trước insert, GET chi tiết contact theo mã (`getContactDetail` sửa từ POST sang GET — endpoint `/item` chỉ nhận GET); nếu đã tồn tại → gọi `updateContact` (đồng bộ dữ liệu mới lên contact cũ) thay vì insert; job ghi `contact_updated: true`. Verify: unlink #403 → push lại → job `completed` + `contact_updated: true`, `id_1office` giữ `212` ✅.

---

## Phần 5 — Đồng bộ xuôi/ngược cho bản đã liên kết (10/09/2026)

**Xuôi (push bản đã link = cập nhật):** bỏ chặn `Đề xuất đã liên kết` ở `pushTo1Office`; queue lệnh bình thường, khóa `contactData.code` = mã đã link (định danh 1Office không đổi), gắn cờ `isUpdate` (toast: `X đẩy mới, Y cập nhật đã liên kết`). Worker upsert sẵn → UPDATE contact cũ.

**Ngược (pull = cập nhật proposal từ contact):** worker `processPullJob` trước đây chỉ fetch, logic ghi trong `syncService.processPull` không bao giờ chạy. Đã nối worker → `processPull` và viết lại:
- Khớp contact với proposal theo mã HOẶC id số; contact chưa link → skip (không tự tạo proposal rác).
- Update theo đúng mappings pull/both (cột fixed whitelist + custom_data), chạy `applyDiaGioi` khi có tỉnh, recompute formula TRỪ `ma_de_xuat` (giữ mã ổn định), stamp `sync_status`/`last_synced_at`/`last_synced_data`.
- Pull phân trang (trước đây chỉ lấy 50 dòng đầu).

**Fix kèm (lệch mã sau sửa):** edit recompute vô điều kiện làm `ma_de_xuat` đổi nhưng cột `tracking_code` giữ → lệch (403: `...0005` vs `...0006`). Giờ: chỉ recompute mã khi tỉnh/mô hình đổi HOẶC mã cũ hỏng, đóng băng mã với bản đã link, và đồng bộ `tracking_code` khi mã đổi.

**Verify:** đổi tên contact 212 trên 1Office → pull → local `owner_name` đổi theo, mã + `id_1office` giữ nguyên ✅ (fetch 75, update 8, skip 67).

---

## Phần 6 — Toast cụ thể: link treo + tạo lại contact (10/09/2026)

**Yêu cầu:** user phải được báo rõ chuyện gì xảy ra (xóa contact bên 1Office thì pull/push ra sao).

**Hành vi đã xác nhận bằng test thật:**
- Push bản bị xóa contact → worker không thấy mã → insert mới cùng mã → tự lành, ID số đổi thầm lặng.
- Pull → bản link treo không được đụng tới, im lặng (link gãy).

**Fix:**
- Pull (chỉ khi pull toàn bộ): proposal đã link mà mã/ID không có trong danh sách contacts → `sync_status='error'` + message trong `last_synced_data`, liệt kê trong `details.dangling` của job.
- Push: payload gắn `was_linked`/`previous_contact_id`; worker trả `contact_recreated: true` khi tạo lại sau xóa.
- `GET /queue-logs/:id` mở cho sales xem job của mình (trước chỉ super) để poll được.
- Frontend poll job mỗi 3s (tối đa 90s) rồi toast kết quả thật:
  - Push xong: `X tạo mới, Y cập nhật, Z tạo lại (contact từng bị xóa), W thất bại`.
  - Pull xong: `X cập nhật, Y bỏ qua, Z link treo (mã #id)`.

**Verify:** link mã ảo → pull → `sync_status='error'` + job liệt kê dangling ✅; link mã ảo → push (tạo contact) → xóa contact bên 1Office → push lại → `contact_recreated: true`, ID mới ✅ (dọn sạch contact test + unlink local). Regression UI 5/5, 0 console errors ✅.

---

## Phần 7 — Bulk action Xóa cho /admin/stations và /admin/users (10/09/2026)

**Yêu cầu:** tick chọn record để xóa hàng loạt (bulk action đầu tiên).

**Triển khai** (2 trang cùng pattern):
- Thanh bulk bar hiện khi tick chọn (`Đã chọn: N trạm/user` + nút `Xóa (N)` + `Bỏ chọn`), đặt sau filter bar.
- Dialog xác nhận riêng (`Xóa nhiều trạm/user`).
- Xóa lặp từng ID qua API đơn hiện có, toast trung thực (`Đã xóa X...`, `warning` khi có dòng lỗi, `setError` khi không xóa được dòng nào), xong clear chọn + reload.
- Stations: thanh bulk ẩn với sales (đồng bộ nút Xóa theo dòng `!isSales`).
- Users: lọc trước các dòng bảo vệ (role ADMIN/SUPER_ADMIN, chính mình — khớp luật ẩn nút Xóa theo dòng) → báo `bỏ qua N (admin/tự)`; backend cũng chặn nên lỗi từng dòng vẫn được báo thật.

**Verify (Playwright 8/8):** tạo 2 trạm + 2 CTV test → tìm → tick tất cả → bulk xóa → toast `Đã xóa 2 trạm` / `Đã xóa 2 user`; tick đúng admin → bulk xóa → admin còn nguyên; 0 page errors; dữ liệu test đã dọn sạch.

---

## Phần 8 — Bulk action Xóa cho /admin/proposals (10/09/2026)

Mở rộng pattern Phần 7 sang trang proposals (đã có tick chọn + `selectedIds`): thanh bulk bar (`Đã chọn: N đề xuất` + `Xóa (N)` + `Bỏ chọn`) trước bảng, dialog xác nhận riêng, xóa lặp qua `adminProposalService.delete`, toast trung thực.

**Verify (Playwright 5/5):** tạo 2 proposals test → tìm → tick tất cả → bulk xóa → toast `Đã xóa 2 đề xuất`; 0 page errors; dữ liệu test đã dọn sạch.

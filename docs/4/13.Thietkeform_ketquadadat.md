# Kết quả đạt được: FormBuilder Layout System + Conditional Visibility

## Trạng thái

| Phase | Status | Ghi chú |
|-------|--------|---------|
| FormBuilder Layout System | ✅ Hoàn thành | Layout rows + preview + drag-drop + field config |
| Conditional Visibility | ✅ Hoàn thành | AND/OR logic, field conditions |
| AdminStationsPage Create | ✅ Hoàn thành | DynamicForm formId=7 + map picker |
| AdminUsersPage Create | ✅ Hoàn thành | DynamicForm formId=8 |
| MyProposalsPage Create | ✅ Hoàn thành | DynamicForm formId=9 + map picker |
| MapPage (đã có) | ✅ Hoàn thành | Đã dùng DynamicForm |

---

## Kết quả từng bước

### Bước 1: DB Migration
- [x] File `database/15-add-layout-config-to-forms.sql` tạo xong
- [x] Migration chạy thành công trên Docker MySQL
- [x] Column `layout_config` tồn tại trong bảng `forms`

### Bước 2: Backend — Return layout_config
- [x] `dynamicEngineService.getFormConfig` trả về `layout_config`
- [x] `formService.createForm/updateForm` xử lý `layout_config`
- [x] `formController.create/update` truyền `layout_config`
- [x] Test API: `GET /api/dynamic/stations/form/7` có `form.layout_config` ✅
- [x] Test API: `PUT /api/forms/7` lưu `layout_config` thành công ✅

### Bước 3: Backend — Swagger docs
- [x] Swagger UI hiển thị `layout_config` trong POST/PUT request body
- [x] Backend restart không lỗi

### Bước 4-7: FormBuilder UI
- [x] Layout rows: dropdown columns ratio (1:1, 1:2, 2:1, 2:2)
- [x] Add/remove/reorder rows
- [x] Preview toggle (Desktop/Mobile)
- [x] Drag-drop from available fields to form cells
- [x] Field config panel (label, placeholder, required, readonly, rowSpan)
- [x] Conditions builder (field, operator, value, AND/OR logic)
- [x] Save layout_config to backend

### Bước 8-10: DynamicForm Layout + Conditional Visibility
- [x] Render fields in layout rows (rowId + colIndex from form_fields.config)
- [x] Fallback flat render when no layout_config
- [x] Conditional visibility (isFieldVisible) with AND/OR logic
- [x] Field overrides (labelOverride, placeholderOverride, requiredOverride, readonly)

### Bước 11: AdminStationsPage — Create form
- [x] DynamicForm formId=7 replacing hardcoded form
- [x] Map picker for lat/lng (click to set coordinates)
- [x] Submit creates station via stationService.create

### Bước 12: AdminUsersPage — Create button + form
- [x] "+ Tạo user" button in page header
- [x] Modal with DynamicForm formId=8
- [x] Submit creates user via adminUserService.create

### Bước 13: MyProposalsPage — Create button + form
- [x] "+ Tạo đề xuất" button in page header
- [x] Modal with DynamicForm formId=9 + map picker
- [x] Submit creates proposal via myProposalService.create (POST /api/proposals)

### Bước 14: CSS Layout system
- [x] Layout section styles
- [x] Preview toggle styles
- [x] Form cell / drop zone styles
- [x] Field config panel styles
- [x] Condition builder styles
- [x] Field chip / unassigned styles

### Bước 15: Test + Fix
- [x] Frontend build passes
- [x] Backend starts without errors
- [x] All 3 form APIs return correct data
- [x] Layout config saves/loads correctly

---

## Files thay đổi

| File | Trạng thái | Mô tả |
|------|------------|-------|
| `database/15-add-layout-config-to-forms.sql` | ✅ | Thêm column layout_config |
| `backend/src/services/dynamicEngineService.js` | ✅ | Return layout_config |
| `backend/src/services/formService.js` | ✅ | Handle layout_config in create/update |
| `backend/src/controllers/formController.js` | ✅ | Pass layout_config |
| `backend/src/routes/forms.js` | ✅ | Swagger docs cho layout_config |
| `frontend/src/components/admin/FormBuilder.jsx` | ✅ | Full rewrite: layout rows, drag-drop, field config, conditions |
| `frontend/src/components/dynamic/DynamicForm.jsx` | ✅ | Layout rendering, conditional visibility, field overrides |
| `frontend/src/pages/admin/AdminStationsPage.jsx` | ✅ | DynamicForm formId=7 + map picker |
| `frontend/src/pages/admin/AdminUsersPage.jsx` | ✅ | DynamicForm formId=8 + create button |
| `frontend/src/pages/user/MyProposalsPage.jsx` | ✅ | DynamicForm formId=9 + map picker + create button |
| `frontend/src/services/api.js` | ✅ | Added myProposalService.create |
| `frontend/src/App.css` | ✅ | Layout system CSS (~200 lines) |

---

## Git commits

| # | Commit | Mô tả |
|---|--------|-------|
| 1 | — | DB migration + backend layout_config |
| 2 | — | FormBuilder UI: layout rows + drag-drop + field config |
| 3 | — | DynamicForm: layout rendering + conditional visibility |
| 4 | — | AdminStationsPage: create form với DynamicForm |
| 5 | — | AdminUsersPage + MyProposalsPage: create forms |
| 6 | — | CSS + responsive + test fixes |

---

## Post-completion fixes

### Fix STT (row number) pagination
- [x] DynamicTable accepts `startIndex` prop (default 0)
- [x] STT = `startIndex + idx + 1` instead of `idx + 1`
- [x] AdminUsersPage, AdminStationsPage, AdminProposalsPage, MyProposalsPage pass `startIndex={(pagination.page - 1) * pagination.limit}`

### Column ratio simplified
- [x] Only `1:1` (1 column all) and `1:2` (2 columns desktop) supported
- [x] Removed `2:1` and `2:2` options

### Modal UX improvements
- [x] All form modals have ✕ close button + Hủy cancel button
- [x] DynamicForm shows admin configure message when no layout_config

### Backend custom_data fix
- [x] adminUserController accepts `custom_data` in create/update
- [x] adminUserService saves `custom_data` as JSON in users table
- [x] RecordDetailPopup.handleSave separates json-type fields into custom_data

### Public image endpoint
- [x] `GET /api/files/:id/image` added in routes/files.js (no auth required)
- [x] FieldRenderer avatar uses `/files/{id}/image` URL

### Select/multiselect click-outside fix
- [x] DynamicField uses useRef + mousedown listener for click-outside close

---

## Lưu ý sau khi hoàn thành

- `RecordDetailPopup.jsx` không thay đổi (VIEW/EDIT dùng View-based)
- `field_definitions` table không thay đổi
- `form_fields` table không thay đổi (lưu config JSON với rowId, colIndex, conditions)
- MapPage.jsx tự động hưởng layout mới
- API endpoints không thay đổi (chỉ thêm Swagger docs)
- FormBuilder chỉ dùng cho CREATE (không dùng cho RecordDetailPopup VIEW/EDIT)
- DynamicTable giờ nhận prop `startIndex` để tính STT đúng trên mọi trang
